---
phase: 01-fork-bootstrap-storageadapter-interface-skeleton-markdownada
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/DECISIONS.md
  - scripts/leak-grep.cjs
  - tests/leak-grep/fixtures/clean-skill.md
  - tests/leak-grep/fixtures/leaky-skill-context.md
  - tests/leak-grep/fixtures/leaky-workflow-tools.md
  - tests/leak-grep/fixtures/leaky-workflow-shell.md
  - tests/leak-grep.test.cjs
autonomous: true
requirements:
  - ADAPTER-05
tags: [upstream-reconciliation, leak-detection, infrastructure]

must_haves:
  truths:
    - "D-12: Four new ADR entries exist in .planning/DECISIONS.md (D-2026-04-30-07 through -10), one per upstream PR (#2898, #2901, #2908, #2909)"
    - "D-12: Each ADR records verdict (REUSE/COORDINATE/DIVERGE/IRRELEVANT), contract impact, rebase risk per the locked verdicts: #2898=IRRELEVANT, #2901=REUSE, #2908=COORDINATE, #2909=COORDINATE"
    - "D-14: scripts/leak-grep.cjs exists and detects all R5 patterns (Read/Write/Edit against .planning/, cp/mv/rm -rf/>> against .planning/) plus <context>-block @.planning/ frontmatter scanner"
    - "D-14: leak-grep produces machine-readable output (file:line:pattern) suitable for the Phase 4 LEAKS-04 CI gate to parse"
    - "Known-clean fixture passes (zero leaks reported); known-bad fixtures (3) each produce at least one matched pattern"
  artifacts:
    - path: ".planning/DECISIONS.md"
      provides: "Per-PR ADRs reconciling upstream seam work with the StorageAdapter contract"
      contains: "D-2026-04-30-07"
    - path: "scripts/leak-grep.cjs"
      provides: "Direct-I/O leak detection script (R5 patterns + <context>-block scanner)"
      contains: "TOOL_PATTERNS"
    - path: "tests/leak-grep/fixtures/clean-skill.md"
      provides: "Negative-control fixture: a skill with no .planning/ leaks"
    - path: "tests/leak-grep/fixtures/leaky-skill-context.md"
      provides: "Positive-control fixture: skill with @.planning/ in <context> block"
    - path: "tests/leak-grep/fixtures/leaky-workflow-tools.md"
      provides: "Positive-control fixture: workflow with Read/Write/Edit against .planning/"
    - path: "tests/leak-grep/fixtures/leaky-workflow-shell.md"
      provides: "Positive-control fixture: workflow with cp/mv/rm/>> against .planning/"
    - path: "tests/leak-grep.test.cjs"
      provides: "Test that runs leak-grep against the four fixtures and asserts pass/fail"
  key_links:
    - from: "scripts/leak-grep.cjs"
      to: "tests/leak-grep/fixtures/*.md"
      via: "test invocation"
      pattern: "leak-grep.cjs.*fixtures"
    - from: ".planning/DECISIONS.md"
      to: "01-CONTEXT.md D-12"
      via: "documentary cross-reference"
      pattern: "D-2026-04-30-(07|08|09|10)"
---

<objective>
Ship two pieces of upstream-divergence-management infrastructure that gate the lock of the StorageAdapter contract:
(1) Four per-PR ADRs in `.planning/DECISIONS.md` recording the reconciliation verdict for upstream PRs #2898, #2901, #2908, #2909 (D-12 / ADAPTER-05);
(2) `scripts/leak-grep.cjs` covering Rubric R5 patterns plus the `<context>`-block @-reference scanner, with fixture-based test coverage proving correctness (D-14).

Purpose: ADAPTER-05 explicitly requires reconciliation against upstream PRs before the contract is locked. The verdicts are pre-determined by RESEARCH.md (Upstream PR Classification section); this plan transcribes them into DECISIONS.md as durable ADRs. The leak-grep script ships the engine that the Phase 4 LEAKS-04 CI gate will eventually wire — Phase 1 ships only the engine + correctness tests, no CI wiring yet.
Output: 4 new D-2026-04-30-NN entries in DECISIONS.md, leak-grep.cjs script, 4 fixture files, 1 test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/DECISIONS.md
@.planning/phases/01-fork-bootstrap-storageadapter-interface-skeleton-markdownada/01-CONTEXT.md
@.planning/phases/01-fork-bootstrap-storageadapter-interface-skeleton-markdownada/01-RESEARCH.md
@.planning/research/fork-investigation/SYNTHESIS.md
@CLAUDE.md
@scripts/run-tests.cjs

<interfaces>
<!-- Existing scripts/ style — leak-grep.cjs MUST match: -->

scripts/ contains ONLY .cjs files (verified). Format conventions from neighboring scripts:
- Shebang: `#!/usr/bin/env node`
- Strict mode: `'use strict';`
- Sync I/O OK for top-level scripts (matches scripts/run-tests.cjs, lint-descriptions.cjs)
- Exit code: 0 on no leaks, 1 on leaks found (CI-gate-friendly)

<!-- Test runner discovery (from scripts/run-tests.cjs): -->

The fork's tests/ directory already contains 200+ `.test.cjs` files. They are run via `node scripts/run-tests.cjs` which uses Node's built-in `--test` runner.

A new `tests/leak-grep.test.cjs` will be discovered automatically by the existing runner. The test file uses Node's `node:test` API (matches existing tests' style — verified by inspection of e.g. tests/state.test.cjs).

<!-- Existing DECISIONS.md format (from .planning/DECISIONS.md): -->

Each entry follows this structure:
```markdown
## D-YYYY-MM-DD-NN — Title

**Date:** YYYY-MM-DD
**Decision:** [one-paragraph summary]

**Alternatives considered:** [...]

**Implication:** [...]
```

The existing file ends at D-2026-04-30-06 with the open-questions table. New entries (07-10) get appended BEFORE the "Open questions deferred to v1.0 milestone phases" section header.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 4 per-PR ADRs to .planning/DECISIONS.md (D-12 / ADAPTER-05)</name>
  <files>.planning/DECISIONS.md</files>
  <read_first>
    - /home/ellio/code/get-shit-done/.planning/DECISIONS.md (current state — entries D-2026-04-30-01 through -06)
    - /home/ellio/code/get-shit-done/.planning/phases/01-fork-bootstrap-storageadapter-interface-skeleton-markdownada/01-CONTEXT.md §"D-12"
    - /home/ellio/code/get-shit-done/.planning/phases/01-fork-bootstrap-storageadapter-interface-skeleton-markdownada/01-RESEARCH.md §"Upstream PR Classification (ADAPTER-05)" (the four verdicts are LOCKED there)
    - /home/ellio/code/get-shit-done/.planning/STATE.md §"Recent upstream signal" (PR commit hashes for cross-reference)
  </read_first>
  <action>
    Append four new ADR entries to `.planning/DECISIONS.md` BEFORE the line `# Open questions deferred to v1.0 milestone phases`. Each entry follows the existing format pattern (## header, Date, Decision, Alternatives if applicable, Implication). The verdicts are FIXED from RESEARCH.md — do NOT re-derive or investigate the PRs in this task.

    **D-2026-04-30-07 — Upstream PR #2898 reconciliation (durable planning runtime)**

    - Date: 2026-04-30
    - Decision: **IRRELEVANT** to the StorageAdapter contract. PR #2898 lands `PlanningRuntime`, `PlanningJournal`, `RuntimeGate` classes that append JSONL events to `.planning/.journal/` for plan-execution event sourcing. This is a different layer (plan lifecycle events, not planning content) and operates on a separate path tree (`.planning/.journal/`).
    - Contract impact: NONE. The journal does not touch STATE.md, ROADMAP.md, or any document the StorageAdapter owns.
    - Rebase risk: LOW. Separate files, separate directory.
    - Future consideration: should journal writes eventually flow through `adapter.putRecord`? Deferred — append-only JSONL semantics do not fit `putRecord/getRecord` cleanly.

    **D-2026-04-30-08 — Upstream PR #2901 reconciliation (planning-workspace seam)**

    - Date: 2026-04-30
    - Decision: **REUSE**. PR #2901 extracts `planning-workspace.cjs` exposing `planningDir(cwd)`, `planningRoot()`, `planningPaths()`, `withPlanningLock()` from core.cjs. This is exactly the path-resolution seam MarkdownAdapter (Plan 03) needs. We import `planningDir` directly inside MarkdownAdapter to resolve `.planning/`-relative paths.
    - Contract impact: NONE on the StorageAdapter type signature. Implementation impact: MarkdownAdapter constructor calls `planningDir(projectDir)` to compute its path base.
    - Rebase risk: LOW. We consume the existing module, do not modify it.

    **D-2026-04-30-09 — Upstream PR #2908 reconciliation (manifest-backed routing seam)**

    - Date: 2026-04-30
    - Decision: **COORDINATE**. PR #2908 adds `CommandManifestEntry` types, `COMMAND_MANIFEST` array, and `command-seam-coverage.test.ts`. The manifest covers command-routing inspection (`state | verify | init | phase | phases | validate | roadmap` families); our adapter seam covers storage pluggability. They are complementary, not competing.
    - Contract impact: NONE on the StorageAdapter shape. Plan 04 must update the one site (`registry-canonical-commands.ts:10`) where the manifest coverage test calls `createRegistry()` — no semantic change, just signature update.
    - Rebase risk: LOW-MEDIUM. Future upstream may add new manifest entries that need monitoring via leak-grep (this plan's deliverable).

    **D-2026-04-30-10 — Upstream PR #2909 reconciliation (golden parity matrix)**

    - Date: 2026-04-30
    - Decision: **COORDINATE**. PR #2909 adds golden integration tests comparing SDK registry dispatch to CJS gsd-tools.cjs output for `phases.*`, `validate.*`, `roadmap.*`. Per CONTEXT.md D-13, Phase 1's CI does NOT need to pass the #2909 parity matrix — that is **Phase 8 (DIST-04)**. Phase 1 only updates the `createRegistry()` call signatures inside these tests via Plan 04.
    - Contract impact: NONE on the StorageAdapter type signature.
    - Rebase risk: MEDIUM. Future #2909 expansions add more `createRegistry()` call sites that need adapter injection. Plan 04 establishes the pattern; rebase script (D-14, this plan) catches new call sites.
    - Note: this entry locks D-13's deferral. The empirical match against #2909's parity matrix is owned by Phase 8 (DIST-04).

    Use the Read tool to load `.planning/DECISIONS.md` first, then the Edit tool to append the four entries with proper Markdown formatting (`---` separator between entries, matching existing style). Insert the new entries between the existing `## D-2026-04-30-06` block and the `# Open questions deferred...` header line.

    Do NOT re-write the existing decisions. Do NOT alter the open-questions table — those are deferred per CONTEXT.md.
  </action>
  <verify>
    <automated>cd /home/ellio/code/get-shit-done && grep -c "^## D-2026-04-30-0[7-9]" .planning/DECISIONS.md | grep -q "^3$" && grep -c "^## D-2026-04-30-10" .planning/DECISIONS.md | grep -q "^1$" && grep -q "IRRELEVANT" .planning/DECISIONS.md && grep -q "REUSE" .planning/DECISIONS.md && grep -c "COORDINATE" .planning/DECISIONS.md | awk '$1 >= 2 {exit 0} {exit 1}' && grep -q "#2898" .planning/DECISIONS.md && grep -q "#2901" .planning/DECISIONS.md && grep -q "#2908" .planning/DECISIONS.md && grep -q "#2909" .planning/DECISIONS.md && grep -q "DIST-04" .planning/DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## D-2026-04-30-0[7-9]" .planning/DECISIONS.md == 3` (entries -07, -08, -09)
    - `grep -c "^## D-2026-04-30-10" .planning/DECISIONS.md == 1`
    - `grep -q "IRRELEVANT" .planning/DECISIONS.md` succeeds (verdict for #2898)
    - `grep -q "REUSE" .planning/DECISIONS.md` succeeds (verdict for #2901)
    - `grep -c "COORDINATE" .planning/DECISIONS.md` returns at least 2 (verdicts for #2908 and #2909)
    - `grep -q "#2898" && grep -q "#2901" && grep -q "#2908" && grep -q "#2909"` all succeed against `.planning/DECISIONS.md`
    - `grep -q "DIST-04" .planning/DECISIONS.md` succeeds (D-2026-04-30-10 references the Phase 8 deferral per D-13)
    - The existing 6 entries (D-2026-04-30-01..06) remain unmodified: `grep -c "^## D-2026-04-30-0[1-6]" .planning/DECISIONS.md == 6`
    - The "Open questions deferred to v1.0 milestone phases" header still exists and appears AFTER the new entries: `grep -n "Open questions deferred" .planning/DECISIONS.md` returns a line number greater than the last `## D-2026-04-30-10` line
  </acceptance_criteria>
  <done>
    Four ADR entries (D-2026-04-30-07 through -10) appended to .planning/DECISIONS.md with the locked verdicts (IRRELEVANT/REUSE/COORDINATE/COORDINATE), each recording contract impact and rebase risk. The open-questions section is preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scripts/leak-grep.cjs (R5 + <context>-block scanner) with fixtures and test</name>
  <files>scripts/leak-grep.cjs, tests/leak-grep/fixtures/clean-skill.md, tests/leak-grep/fixtures/leaky-skill-context.md, tests/leak-grep/fixtures/leaky-workflow-tools.md, tests/leak-grep/fixtures/leaky-workflow-shell.md, tests/leak-grep.test.cjs</files>
  <read_first>
    - /home/ellio/code/get-shit-done/scripts/run-tests.cjs (test runner — discovers tests/*.test.cjs automatically)
    - /home/ellio/code/get-shit-done/scripts/lint-descriptions.cjs (representative scripts/ style — Node CJS, sync I/O, exit-code semantics)
    - /home/ellio/code/get-shit-done/.planning/phases/01-fork-bootstrap-storageadapter-interface-skeleton-markdownada/01-CONTEXT.md §"D-14"
    - /home/ellio/code/get-shit-done/.planning/phases/01-fork-bootstrap-storageadapter-interface-skeleton-markdownada/01-RESEARCH.md §"Pattern 7" + §"Pitfall 6"
    - /home/ellio/code/get-shit-done/.planning/research/fork-investigation/SYNTHESIS.md §2 R5 (the R5 pattern catalog) + §9 risk row "<context>-block leaks evade workflow refactor"
    - /home/ellio/code/get-shit-done/tests/state.test.cjs (representative tests/*.test.cjs style for Node's `node:test` API — for fixture test structure)
  </read_first>
  <behavior>
    - Test 1 (clean fixture, zero leaks): run leak-grep over `tests/leak-grep/fixtures/clean-skill.md` → exit code 0, stdout reports zero matches
    - Test 2 (skill <context> @-reference leak): run leak-grep over `tests/leak-grep/fixtures/leaky-skill-context.md` → exit code 1, stdout includes a line matching `leaky-skill-context.md:.*@\\.planning/` and tags it as `context-block` category
    - Test 3 (workflow tool leaks): run leak-grep over `tests/leak-grep/fixtures/leaky-workflow-tools.md` → exit code 1, stdout reports at least 3 matches (Read/Write/Edit pattern hits)
    - Test 4 (shell pattern leaks): run leak-grep over `tests/leak-grep/fixtures/leaky-workflow-shell.md` → exit code 1, stdout reports matches for `cp ... .planning/`, `mv ... .planning/`, `rm -rf .planning/`, `>> .planning/`
    - Test 5 (machine-readable output): leak-grep stdout for any leaky fixture has at least one line matching the regex `^[^:]+:\\d+:` (file:line:pattern format — CI-gate-parseable)
  </behavior>
  <action>
    **Step 1: Create `scripts/leak-grep.cjs`** as a Node CJS script. CLI shape:

    ```
    Usage: node scripts/leak-grep.cjs <path> [<path>...]
    Exit codes: 0 = no leaks, 1 = leaks found, 2 = no input paths
    Output (stdout): file:line:category:matched_text  (one line per match)
    Output (stderr): summary (count of matches)
    ```

    Required pattern set (per D-14 / RESEARCH.md Pattern 7 / SYNTHESIS R5):

    ```javascript
    // Tool patterns — Read/Write/Edit invocations against .planning/
    // Match heuristic: tool keyword followed by an arg referencing .planning/
    const TOOL_PATTERNS = [
      { name: 'Read-tool',  re: /\bRead\b[^\n]{0,200}["'`]\.planning\// },
      { name: 'Write-tool', re: /\bWrite\b[^\n]{0,200}["'`]\.planning\// },
      { name: 'Edit-tool',  re: /\bEdit\b[^\n]{0,200}["'`]\.planning\// },
    ];

    // Shell patterns — cp/mv/rm/>> against .planning/
    const SHELL_PATTERNS = [
      { name: 'cp-shell',     re: /\bcp\b[^\n;]{0,200}\.planning\// },
      { name: 'mv-shell',     re: /\bmv\b[^\n;]{0,200}\.planning\// },
      { name: 'rm-rf-shell',  re: /\brm\s+-rf\b[^\n;]{0,200}\.planning\// },
      { name: 'append-shell', re: />>\s*\.planning\// },
    ];

    // <context>-block @.planning/ frontmatter scan (the new leak class from SYNTHESIS §1 #4)
    // Heuristic: detect @.planning/ inside a YAML frontmatter <context> block
    // Per Pitfall 6: scan ANY .planning/ inside <context>...</context>, not just @-prefixed
    const CONTEXT_BLOCK_RE = /<context>([\s\S]*?)<\/context>/g;
    const CONTEXT_PATH_RE = /\.planning\//;
    ```

    Implementation outline:

    ```javascript
    #!/usr/bin/env node
    'use strict';
    const fs = require('node:fs');
    const path = require('node:path');

    // ... pattern definitions above ...

    function scanFile(filePath) {
      const matches = [];
      const text = fs.readFileSync(filePath, 'utf-8');
      const lines = text.split(/\r?\n/);

      // Per-line patterns (TOOL_PATTERNS + SHELL_PATTERNS)
      lines.forEach((line, i) => {
        for (const { name, re } of [...TOOL_PATTERNS, ...SHELL_PATTERNS]) {
          if (re.test(line)) {
            matches.push({ file: filePath, line: i + 1, category: name, text: line.trim() });
          }
        }
      });

      // Whole-text scan for <context>-block leaks (multi-line)
      let m;
      const contextRe = /<context>([\s\S]*?)<\/context>/g;
      while ((m = contextRe.exec(text)) !== null) {
        const block = m[1];
        if (CONTEXT_PATH_RE.test(block)) {
          // Compute the line number of the match within the file
          const offsetBeforeBlock = text.slice(0, m.index).split(/\r?\n/).length;
          matches.push({
            file: filePath,
            line: offsetBeforeBlock,
            category: 'context-block',
            text: block.split(/\r?\n/).find(l => CONTEXT_PATH_RE.test(l))?.trim() ?? '<context>-block .planning/ ref',
          });
        }
      }

      return matches;
    }

    function main(argv) {
      const inputs = argv.slice(2);
      if (inputs.length === 0) {
        console.error('Usage: node scripts/leak-grep.cjs <path> [<path>...]');
        process.exit(2);
      }

      // Walk directories, collect files; for plain files just include them.
      const files = [];
      for (const input of inputs) {
        const stat = fs.statSync(input);
        if (stat.isDirectory()) {
          // Recursive walk — include only .md / .ts / .js / .cjs / .mjs
          (function walk(dir) {
            for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
              const p = path.join(dir, entry.name);
              if (entry.isDirectory()) walk(p);
              else if (/\.(md|ts|js|cjs|mjs)$/.test(entry.name)) files.push(p);
            }
          })(input);
        } else {
          files.push(input);
        }
      }

      let total = 0;
      for (const f of files) {
        const ms = scanFile(f);
        for (const m of ms) {
          process.stdout.write(`${m.file}:${m.line}:${m.category}:${m.text}\n`);
          total += 1;
        }
      }

      process.stderr.write(`leak-grep: ${total} match(es) across ${files.length} file(s)\n`);
      process.exit(total > 0 ? 1 : 0);
    }

    main(process.argv);
    ```

    Make the script executable: `chmod +x scripts/leak-grep.cjs`.

    **Step 2: Create the four fixture files.**

    `tests/leak-grep/fixtures/clean-skill.md` (negative control — no leaks, all SDK-routed):
    ```markdown
    ---
    name: clean-skill
    description: A skill that uses only SDK calls — no leaks
    ---

    # Clean Skill

    All planning state access goes through the SDK:
    `gsd-sdk query state.json` returns project state.

    No <context> block. No Read/Write/Edit against .planning/.
    No cp/mv/rm against .planning/.
    ```

    `tests/leak-grep/fixtures/leaky-skill-context.md` (positive — <context>-block leak):
    ```markdown
    ---
    name: leaky-skill-context
    description: A skill that loads .planning/STATE.md at activation via <context>
    <context>
    @.planning/STATE.md
    @.planning/ROADMAP.md
    </context>
    ---

    # Leaky Skill (frontmatter <context> @ leak)

    This skill triggers Claude Code to read .planning/ files at activation,
    bypassing any runtime adapter.
    ```

    `tests/leak-grep/fixtures/leaky-workflow-tools.md` (positive — Read/Write/Edit):
    ```markdown
    # Leaky Workflow

    ## Step 1
    Run the Read tool on `.planning/STATE.md` to see project state.

    ## Step 2
    Use the Write tool to write to `.planning/phases/X-foo/PLAN.md`.

    ## Step 3
    Edit the file `.planning/ROADMAP.md` to mark progress.
    ```

    `tests/leak-grep/fixtures/leaky-workflow-shell.md` (positive — shell leaks):
    ```markdown
    # Leaky Workflow (shell)

    ## Step 1
    Backup: `cp -r .planning/ /tmp/backup/`

    ## Step 2
    Archive: `mv .planning/phases/old-phase /tmp/`

    ## Step 3
    Reset: `rm -rf .planning/tmp/`

    ## Step 4
    Append: `echo "fixture" >> .planning/SCRATCH.md`
    ```

    **Step 3: Create `tests/leak-grep.test.cjs`** using Node's built-in `node:test` runner (matches existing tests/*.test.cjs convention):

    ```javascript
    'use strict';
    const test = require('node:test');
    const assert = require('node:assert/strict');
    const { execFileSync } = require('node:child_process');
    const path = require('node:path');

    const SCRIPT = path.resolve(__dirname, '..', 'scripts', 'leak-grep.cjs');
    const FIXTURES = path.resolve(__dirname, 'leak-grep', 'fixtures');

    function runLeakGrep(fixture) {
      try {
        const stdout = execFileSync('node', [SCRIPT, path.join(FIXTURES, fixture)], { encoding: 'utf-8' });
        return { exitCode: 0, stdout };
      } catch (err) {
        return { exitCode: err.status, stdout: err.stdout?.toString() ?? '' };
      }
    }

    test('clean-skill.md produces zero leaks', () => {
      const { exitCode, stdout } = runLeakGrep('clean-skill.md');
      assert.equal(exitCode, 0, `expected 0, got ${exitCode}; stdout: ${stdout}`);
      assert.equal(stdout.trim(), '');
    });

    test('leaky-skill-context.md detects <context>-block leak', () => {
      const { exitCode, stdout } = runLeakGrep('leaky-skill-context.md');
      assert.equal(exitCode, 1);
      assert.match(stdout, /context-block/);
      assert.match(stdout, /\.planning\//);
    });

    test('leaky-workflow-tools.md detects Read/Write/Edit leaks', () => {
      const { exitCode, stdout } = runLeakGrep('leaky-workflow-tools.md');
      assert.equal(exitCode, 1);
      assert.match(stdout, /Read-tool/);
      assert.match(stdout, /Write-tool/);
      assert.match(stdout, /Edit-tool/);
    });

    test('leaky-workflow-shell.md detects cp/mv/rm/>> leaks', () => {
      const { exitCode, stdout } = runLeakGrep('leaky-workflow-shell.md');
      assert.equal(exitCode, 1);
      assert.match(stdout, /cp-shell/);
      assert.match(stdout, /mv-shell/);
      assert.match(stdout, /rm-rf-shell/);
      assert.match(stdout, /append-shell/);
    });

    test('machine-readable output format (file:line:category:text)', () => {
      const { stdout } = runLeakGrep('leaky-workflow-tools.md');
      const firstLine = stdout.split('\n').filter(Boolean)[0];
      assert.match(firstLine, /^[^:]+:\d+:[a-z-]+:/);
    });
    ```
  </action>
  <verify>
    <automated>cd /home/ellio/code/get-shit-done && test -x scripts/leak-grep.cjs && test -f tests/leak-grep/fixtures/clean-skill.md && test -f tests/leak-grep/fixtures/leaky-skill-context.md && test -f tests/leak-grep/fixtures/leaky-workflow-tools.md && test -f tests/leak-grep/fixtures/leaky-workflow-shell.md && node --test tests/leak-grep.test.cjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/leak-grep.cjs` exists and is executable: `test -x scripts/leak-grep.cjs` succeeds
    - `grep -c "TOOL_PATTERNS\\|SHELL_PATTERNS\\|CONTEXT_BLOCK" scripts/leak-grep.cjs` returns at least 3 (all three pattern categories present)
    - `grep -q "Read-tool\\|Read\\\\b" scripts/leak-grep.cjs` succeeds (Read pattern declared)
    - `grep -q "Write-tool\\|Write\\\\b" scripts/leak-grep.cjs` succeeds
    - `grep -q "Edit-tool\\|Edit\\\\b" scripts/leak-grep.cjs` succeeds
    - `grep -q "cp\\b" scripts/leak-grep.cjs` succeeds
    - `grep -q "mv\\b" scripts/leak-grep.cjs` succeeds
    - `grep -q "rm.*-rf" scripts/leak-grep.cjs` succeeds
    - `grep -q ">>" scripts/leak-grep.cjs` succeeds (append-redirect pattern)
    - `grep -q "<context>" scripts/leak-grep.cjs` succeeds (<context>-block scanner)
    - All four fixture files exist under `tests/leak-grep/fixtures/`
    - `node scripts/leak-grep.cjs tests/leak-grep/fixtures/clean-skill.md` exits 0 and produces empty stdout
    - `node scripts/leak-grep.cjs tests/leak-grep/fixtures/leaky-skill-context.md` exits 1 and stdout includes `context-block`
    - `node scripts/leak-grep.cjs tests/leak-grep/fixtures/leaky-workflow-tools.md` exits 1 and stdout includes `Read-tool`, `Write-tool`, `Edit-tool`
    - `node scripts/leak-grep.cjs tests/leak-grep/fixtures/leaky-workflow-shell.md` exits 1 and stdout includes `cp-shell`, `mv-shell`, `rm-rf-shell`, `append-shell`
    - `node --test tests/leak-grep.test.cjs` exits 0 (all five tests pass)
    - The new test file is picked up by the existing runner: `node scripts/run-tests.cjs 2>&1 | grep -q "leak-grep"` succeeds (informational — does not block this task if the runner skips by default)
  </acceptance_criteria>
  <done>
    `scripts/leak-grep.cjs` implements all R5 patterns plus <context>-block scanning, with machine-readable file:line:category:text output. Four fixtures exist (1 clean, 3 leaky covering all categories). `tests/leak-grep.test.cjs` passes via `node --test`. Phase 4 LEAKS-04 can wire this engine into a CI gate; Phase 1's job ends with the engine + correctness tests.
  </done>
</task>

</tasks>

<verification>
1. `cd /home/ellio/code/get-shit-done && grep -c "^## D-2026-04-30-1" .planning/DECISIONS.md` returns 1 (just D-10) and `grep -c "^## D-2026-04-30-0[7-9]" .planning/DECISIONS.md` returns 3 — total 4 new ADRs
2. `cd /home/ellio/code/get-shit-done && node --test tests/leak-grep.test.cjs` exits 0
3. `cd /home/ellio/code/get-shit-done && node scripts/leak-grep.cjs tests/leak-grep/fixtures/clean-skill.md` exits 0
4. `cd /home/ellio/code/get-shit-done && node scripts/leak-grep.cjs tests/leak-grep/fixtures/leaky-skill-context.md` exits 1 and matches `context-block`
5. The existing fork test suite still runs: `cd /home/ellio/code/get-shit-done && node scripts/run-tests.cjs 2>&1 | tail -5` shows pass/fail summary unchanged from pre-plan baseline (the new test does not break the runner)
</verification>

<success_criteria>
- ADAPTER-05 satisfied: per-PR ADRs for #2898/#2901/#2908/#2909 exist in DECISIONS.md with verdicts IRRELEVANT/REUSE/COORDINATE/COORDINATE
- D-12 honored: contract impact + rebase risk recorded for each PR
- D-13 cross-referenced: D-2026-04-30-10 explicitly notes Phase 1 defers #2909 parity to Phase 8 (DIST-04)
- D-14 honored: leak-grep.cjs covers full R5 pattern set + <context>-block scanner
- Fixture-based correctness tests pass; CI-gate-parseable output format
- Existing test suite is not broken by the new test file
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-bootstrap-storageadapter-interface-skeleton-markdownada/01-02-SUMMARY.md` summarizing:
- Verdicts recorded (D-2026-04-30-07 through -10) and the locked verdict per PR
- Pattern coverage in leak-grep (R5 set + context-block)
- Fixture pass/fail matrix
- Note that LEAKS-04 (Phase 4) wires the gate; Phase 1 ships the engine only
</output>
