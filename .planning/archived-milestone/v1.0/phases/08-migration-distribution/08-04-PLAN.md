<!-- leak-grep-allow file — generated PLAN.md artifact; @.planning/* context refs and shell-pattern examples in prose are consumed by executor via SDK, not auto-loaded by skills -->
---
phase: 08-migration-distribution
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - docs/UPSTREAM-REBASE.md
  - scripts/sync-upstream.sh
autonomous: true
requirements: [DIST-03]
tags: [phase-8, dist-03, rebase-playbook, leak-grep-integration]

must_haves:
  truths:
    - "`docs/UPSTREAM-REBASE.md` + `scripts/sync-upstream.sh` exist (D-12: DIST-03 ships as prose playbook + helper script — `docs/UPSTREAM-REBASE.md` + `scripts/sync-upstream.sh` are the two required artifacts)"
    - "`docs/UPSTREAM-REBASE.md` documents the on-demand upstream sync flow with a conflict taxonomy covering BOTH seam AND business-logic conflicts (D-14: taxonomy covers both; business-logic = RED FLAG INVESTIGATE workflow, not a routine resolution path)"
    - "Business-logic conflicts are framed as RED FLAGS with an INVESTIGATE workflow (inspect -> file upstream issue if missed leak -> resolve), NOT as routine resolutions (D-14)"
    - "`scripts/sync-upstream.sh` is a POSIX `#!/bin/sh` script and invokes `scripts/leak-grep.cjs` on the post-rebase diff as ADVISORY output (script exit 0 even when leaks found, per D-13)"
    - "`bash -n scripts/sync-upstream.sh` exits 0 (syntax valid)"
    - "Script passes the list of changed file paths (via `git diff --name-only`) to `xargs node scripts/leak-grep.cjs`, NOT a unified diff — leak-grep reads file content not patches"
    - "Cadence is on-demand only (no cron, no GHA trigger) per D-15"
  artifacts:
    - path: "docs/UPSTREAM-REBASE.md"
      provides: "Rebase playbook + conflict taxonomy"
      contains: "INVESTIGATE"
      min_lines: 60
    - path: "scripts/sync-upstream.sh"
      provides: "Automated fetch + rebase + leak-grep invocation"
      contains: "leak-grep.cjs"
      min_lines: 20
  key_links:
    - from: "scripts/sync-upstream.sh"
      to: "scripts/leak-grep.cjs"
      via: "xargs node scripts/leak-grep.cjs"
      pattern: "node .*leak-grep\\.cjs"
    - from: "scripts/sync-upstream.sh"
      to: "git rebase upstream/main"
      via: "git plumbing commands"
      pattern: "git rebase"
    - from: "docs/UPSTREAM-REBASE.md"
      to: "scripts/sync-upstream.sh"
      via: "Quick-path reference"
      pattern: "scripts/sync-upstream\\.sh"
---

<objective>
DIST-03: Ship the upstream-sync playbook + helper script so fork
maintainers can rebase against upstream on-demand, catch any new direct-
I/O that lands upstream via advisory leak-grep output, and resolve
conflicts using documented taxonomies for BOTH expected seam conflicts
AND red-flag business-logic conflicts.

Purpose: Without a documented rebase flow, upstream syncs become ad-hoc
and high-risk. The leak-grep sweep on the post-rebase diff is the
critical step — if upstream introduces a new `.planning/` I/O leak that
our seam architecture missed, we need to detect and route it through the
adapter before the leak ships on our branch.

Output:
  - New `docs/UPSTREAM-REBASE.md` (playbook + conflict taxonomy)
  - New `scripts/sync-upstream.sh` (POSIX sh, automated flow, non-
    blocking leak scan)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-migration-distribution/08-CONTEXT.md
@.planning/phases/08-migration-distribution/08-RESEARCH.md
@.planning/phases/08-migration-distribution/08-PATTERNS.md

<interfaces>
<!-- Patterns the executor must replicate. Extracted from existing scripts. -->

## install-hooks.sh pattern (scripts/install-hooks.sh lines 1-19)

```sh
#!/bin/sh
# Install git hooks for the get-shit-done fork
# Usage: ./scripts/install-hooks.sh
set -e

HOOK_DIR="$(git rev-parse --show-toplevel)/.git/hooks"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

# Handle worktree case: .git may be a file pointing to the real gitdir
if [ -f "$(git rev-parse --show-toplevel)/.git" ]; then
  HOOK_DIR="$(git rev-parse --git-dir)/hooks"
fi

# Ensure hooks directory exists
mkdir -p "$HOOK_DIR"

# Pre-commit: leak gate
ln -sf "$SCRIPT_DIR/pre-commit-leak-gate.sh" "$HOOK_DIR/pre-commit"
echo "Installed pre-commit hook: leak-grep gate" >&2
```

Replicate the conventions: `#!/bin/sh`, `set -e`, `SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)`, stderr status messages (`>&2`).

## Leak-grep invocation contract

`scripts/leak-grep.cjs` accepts file paths as argv. It reads file content,
not diffs. Correct invocation pattern:

```sh
CHANGED_FILES=$(git diff --name-only "${PRE_REBASE}..HEAD" 2>/dev/null || true)
if [ -n "$CHANGED_FILES" ]; then
  echo "$CHANGED_FILES" | xargs node "$SCRIPT_DIR/leak-grep.cjs" || true
fi
```

The trailing `|| true` is MANDATORY (D-13: non-blocking advisory output).

## Prior rebase history (RESEARCH.md §DIST-03)

Real conflicts from the 228-commit rebase (commits `6d5889a9`, `970d1ab7`,
`be8cdae6`):
- Signature changes to `createRegistry()` call sites (seam)
- Test-expectation drift from upstream test changes
- Adapter-optional pattern: upstream adds a `GSDTools`-like function
  without adapter injection (seam)
- NO business-logic conflicts observed in first rebase (consistent with
  architecture - but the playbook must still cover the red-flag case)

## Locked decisions to implement

- D-12: Ships as `docs/UPSTREAM-REBASE.md` + `scripts/sync-upstream.sh`
  (prose playbook + helper script — the two artifacts this plan produces)
- D-13: Leak-grep advisory only; script exit 0 even when leaks found
- D-14: Conflict taxonomy covers seam AND business-logic; business-logic
  is INVESTIGATE red flag (missed leak -> file upstream issue)
- D-15: On-demand cadence only; no cron/GHA trigger
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write scripts/sync-upstream.sh</name>
  <files>scripts/sync-upstream.sh</files>
  <read_first>
    - scripts/install-hooks.sh (pattern source - replicate conventions exactly: shebang, set -e, SCRIPT_DIR, stderr messages)
    - scripts/leak-grep.cjs first 30 lines (confirm argv contract: file paths as positional args)
    - scripts/pre-commit-leak-gate.sh (see how leak-grep.cjs is invoked elsewhere in the repo)
    - .planning/phases/08-migration-distribution/08-PATTERNS.md §scripts/sync-upstream.sh (full pattern spec)
    - .planning/phases/08-migration-distribution/08-RESEARCH.md §DIST-03 Deep Dive (script design)
  </read_first>
  <action>
    Per D-12, this helper script is one of two DIST-03 artifacts (the
    other being `docs/UPSTREAM-REBASE.md` in Task 2).

    Create `scripts/sync-upstream.sh` with this exact content (preserving
    shebang, indentation, `|| true` on the leak-grep line, and stderr redirects):

    --- BEGIN scripts/sync-upstream.sh ---
    #!/bin/sh
    # Sync fork with upstream and scan for new leaks after rebase.
    #
    # Usage: ./scripts/sync-upstream.sh [upstream-ref]
    #   upstream-ref defaults to upstream/main
    #
    # The leak-grep step is advisory (non-blocking): the script exits 0
    # even when leaks are found in the post-rebase diff. Treat the output
    # as a checklist of sites that may need adapter routing.
    #
    # See docs/UPSTREAM-REBASE.md for the conflict taxonomy.
    set -e

    UPSTREAM_REF="${1:-upstream/main}"
    SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

    echo "Fetching upstream..." >&2
    git fetch upstream

    PRE_REBASE=$(git rev-parse HEAD)
    echo "Pre-rebase HEAD: $PRE_REBASE" >&2
    echo "Rebasing onto $UPSTREAM_REF..." >&2
    git rebase "$UPSTREAM_REF"

    CHANGED_FILES=$(git diff --name-only "${PRE_REBASE}..HEAD" 2>/dev/null || true)
    if [ -n "$CHANGED_FILES" ]; then
      echo "" >&2
      echo "--- Post-rebase leak scan (advisory, non-blocking) ---" >&2
      echo "$CHANGED_FILES" | xargs node "$SCRIPT_DIR/leak-grep.cjs" || true
      echo "--- End leak scan ---" >&2
    else
      echo "No changed files after rebase." >&2
    fi

    echo "" >&2
    echo "Rebase complete. See docs/UPSTREAM-REBASE.md for conflict guidance." >&2
    --- END scripts/sync-upstream.sh ---

    After writing, make the script executable: `chmod +x scripts/sync-upstream.sh`.

    CRITICAL: The leak-grep line MUST end with `|| true` (D-13). Without
    it, `set -e` would exit the script non-zero when leak-grep finds any
    leak, converting the advisory output into a blocking gate.

    Do NOT add a `git fetch origin` — the script assumes the caller's
    branch is checked out and up-to-date with its remote; that's the
    maintainer's pre-flight responsibility documented in UPSTREAM-REBASE.md.

    Do NOT add colored output or fancy formatting; match install-hooks.sh's
    plain stderr convention.
  </action>
  <verify>
    <automated>cd /Volumes/code/get-shit-done && test -f scripts/sync-upstream.sh && test -x scripts/sync-upstream.sh && bash -n scripts/sync-upstream.sh && grep -c "leak-grep.cjs" scripts/sync-upstream.sh | awk '{exit ($1 >= 1) ? 0 : 1}' && grep -c "|| true" scripts/sync-upstream.sh | awk '{exit ($1 >= 1) ? 0 : 1}' && grep -c "git rebase" scripts/sync-upstream.sh | awk '{exit ($1 >= 1) ? 0 : 1}' && head -1 scripts/sync-upstream.sh | grep -c "#!/bin/sh" | awk '{exit ($1 == 1) ? 0 : 1}' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/sync-upstream.sh` passes
    - `test -x scripts/sync-upstream.sh` passes (executable bit set)
    - `bash -n scripts/sync-upstream.sh` exits 0 (syntax valid)
    - First line is exactly `#!/bin/sh` (not bash)
    - `grep -c "set -e" scripts/sync-upstream.sh` equals 1
    - `grep -c "leak-grep.cjs" scripts/sync-upstream.sh` >= 1
    - `grep -c "|| true" scripts/sync-upstream.sh` >= 1 (non-blocking sentinel)
    - `grep -c "git rebase" scripts/sync-upstream.sh` >= 1
    - `grep -c "SCRIPT_DIR=" scripts/sync-upstream.sh` equals 1
    - `grep -c "git diff --name-only" scripts/sync-upstream.sh` equals 1
    - `grep -c "PRE_REBASE=" scripts/sync-upstream.sh` equals 1
    - File size >= 20 lines (`wc -l scripts/sync-upstream.sh` >= 20)
  </acceptance_criteria>
  <done>
    sync-upstream.sh is syntactically valid, executable, invokes leak-grep non-blocking.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write docs/UPSTREAM-REBASE.md</name>
  <files>docs/UPSTREAM-REBASE.md</files>
  <read_first>
    - docs/CONFIGURATION.md (style reference)
    - scripts/sync-upstream.sh (just created - the script the doc references)
    - .planning/phases/08-migration-distribution/08-CONTEXT.md D-12..D-15 (source of decisions)
    - .planning/phases/08-migration-distribution/08-RESEARCH.md §DIST-03 Deep Dive (conflict taxonomy source + prior rebase observations)
    - .planning/phases/08-migration-distribution/08-PATTERNS.md §docs/UPSTREAM-REBASE.md (section list)
    - scripts/leak-grep.cjs (briefly - doc should accurately describe what a "leak" looks like in its output)
  </read_first>
  <action>
    Per D-12, this prose playbook is one of two DIST-03 artifacts (paired
    with `scripts/sync-upstream.sh`). Per D-14, the conflict taxonomy MUST
    cover BOTH seam AND business-logic conflicts, and business-logic
    conflicts MUST be framed as RED FLAGS with an INVESTIGATE workflow
    (not routine resolutions).

    Create `docs/UPSTREAM-REBASE.md` with the structure below. Render
    shell commands in triple-backtick code blocks where appropriate.
    Match the heading style of docs/CONFIGURATION.md.

    Required sections and content (use these H2 headings verbatim):

    --- BEGIN docs/UPSTREAM-REBASE.md ---

    # Upstream Rebase Playbook

    This fork (`feat/storage-adapter`) tracks upstream
    `gsd-build/get-shit-done` via periodic rebase. This playbook documents
    the on-demand sync flow, the expected conflict surface, and the
    post-rebase leak-scan step that catches new direct-I/O introductions.

    ## When to sync

    On-demand only. There is no cron schedule and no GitHub Actions
    trigger on upstream releases (decision `D-2026-05-13-D-15`). Sync
    when:

    - You need a new upstream feature
    - Before cutting a release of the fork
    - After an upstream security or correctness fix landed

    The first user of a new upstream feature pays the catch-up cost.
    Divergence can grow between runs; the conflict taxonomy below scales
    roughly linearly with the commit count.

    ## Quick path

    From the fork root, with a clean working tree and `feat/storage-adapter`
    (or a feature branch) checked out:

    `./scripts/sync-upstream.sh`

    This runs `git fetch upstream`, rebases onto `upstream/main`, then
    runs `scripts/leak-grep.cjs` over the post-rebase changed files as an
    advisory scan (exit 0 even if leaks found, per
    `D-2026-05-13-D-13`).

    To pin against a specific upstream ref instead of `upstream/main`:

    `./scripts/sync-upstream.sh upstream/release/v1.40.0`

    ## Manual path

    If the script fails partway (e.g. rebase conflicts), finish manually:

    ```sh
    # 1. Save pre-rebase HEAD for the leak scan
    PRE_REBASE=$(git rev-parse HEAD)

    # 2. Fetch + rebase
    git fetch upstream
    git rebase upstream/main

    # 3. Resolve conflicts per the taxonomy below, then:
    git rebase --continue

    # 4. Run the leak scan over changed files
    git diff --name-only "${PRE_REBASE}..HEAD" | xargs node scripts/leak-grep.cjs || true
    ```

    ## Conflict taxonomy

    Conflicts fall into three categories. Only categories 1 and 3 are
    routine. Category 2 is a red flag.

    ### 1. Seam conflicts (expected, mechanical)

    **Files typically affected**:
    - `sdk/src/gsd-tools.ts`
    - `sdk/src/cli.ts`
    - `sdk/src/index.ts`
    - `sdk/src/query/query-cli-adapter.ts`
    - `sdk/src/query-gsd-tools-runtime.ts`
    - `sdk/src/golden/registry-canonical-commands.ts`
    - Any new file upstream adds that calls `createRegistry()`

    **What happens**: Upstream modifies a function signature or adds a
    new `createRegistry()` call site without our adapter-injection
    parameter. Our fork requires either `adapter:` passed through or the
    factory called.

    **Resolution pattern**: Accept upstream's new arguments AND inject
    our adapter construction on top. Example:

    - Upstream adds: `createRegistry({ newParam: 'x' })`
    - Fork resolution: `createRegistry({ newParam: 'x', adapter: createStorageAdapter(projectDir, { adapter: config.storage?.adapter }) })`

    If upstream adds a direct `new MarkdownAdapter(...)` construction in
    core code (a NEW I/O leak), that is NOT a seam conflict — it is a
    business-logic conflict (category 2).

    ### 2. Business-logic conflicts (RED FLAG - INVESTIGATE)

    **Trigger**: Upstream modifies a file that our leak-grep would flag
    (e.g. a new `readFileSync('.planning/...')` in SDK core, a new
    `gsd-tools.cjs` handler that bypasses the adapter, a new direct
    `fs.writeFile` in state-mutation).

    **This is not a routine conflict**. If business logic conflicts
    appear, it means either:

    a) Our adapter seam is incomplete (the I/O leak should have been
       adapter-routed when the seam was designed), OR
    b) Upstream has introduced a new I/O surface that should go through
       adapter dispatch.

    Either way, this is a defect in the fork's adapter coverage, not a
    merge event.

    **INVESTIGATE workflow**:

    1. Inspect the conflict site carefully. Is it a new adapter route
       that should go through `adapter.*`?
    2. If yes: route through the adapter, document the routing in the
       commit message. Consider filing an upstream issue pointing out
       that this new surface is adapter-relevant and proposing a
       reusable injection point.
    3. If no (e.g. it is truly not a `.planning/` path, or it is a
       genuinely-orthogonal concern that the fork is out of scope for):
       resolve normally and note in the commit message why the path
       is not adapter-relevant.
    4. Do NOT blindly accept upstream's change. A business-logic
       conflict is the ONLY place where the mechanical rebase discipline
       breaks down.

    ### 3. CJS binary conflicts (expected, mechanical)

    **Files affected**: `get-shit-done/bin/lib/*.cjs` — compiled CJS
    routing layer outputs.

    **Resolution pattern**: Always take the upstream version, then
    re-apply any fork-side CJS patches on top. The alias-generator and
    config-schema mirrors are regenerated rather than hand-merged.

    ## Leak-grep output interpretation

    `scripts/leak-grep.cjs` scans file content for direct-I/O patterns
    (`Read`, `Write`, `Edit`, `fs.readFileSync`, `fs.writeFile`, `cp`,
    `mv`, `rm -rf` targeting `.planning/`). Its output after a rebase is
    a checklist:

    - Each match is a site that may need adapter routing
    - Sites inside `sdk/src/` or workflow sources are most likely to
      actually need routing
    - Sites inside `get-shit-done/bin/lib/*.cjs` are usually compiled
      output of routed SDK code — treat as informational, not actionable
    - Sites inside documentation, tests, or explicitly-allowed files
      (see `leak-grep-ignore` directives in the allowlist) are noise

    The list is advisory. The script exits 0 regardless. Convert the
    actionable items into a follow-up plan (see
    `.planning/phases/*/XX-PLAN.md` naming) or file issues.

    ## Prior rebase history

    The fork has been rebased against upstream once (a 228-commit batch),
    producing three fixup commits visible in `git log`:

    - `6d5889a9` — fix 13 test failures after the rebase
    - `970d1ab7` — make adapter optional with MarkdownAdapter default
    - `be8cdae6` — reconcile adapter DI with upstream's registry-assembly refactor

    No business-logic conflicts were observed in that rebase, consistent
    with the adapter-seam architecture. Future rebases are expected to
    show similar shapes: seam + test-expectation + CJS-binary. If a
    business-logic conflict DOES appear, treat it with the category 2
    workflow.

    ## Troubleshooting

    - **Merge conflict markers remain after `--continue`**: Run
      `git status` to find unresolved files; they were not staged.
    - **Leak scan shows hundreds of matches**: Likely a large upstream
      batch. Filter to `sdk/src/` first (these are most action-relevant);
      ignore `get-shit-done/bin/lib/*.cjs` compiled output unless it
      shows an unusual pattern.
    - **Script exits with `upstream: not found`**: The `upstream` remote
      is not configured. Run `git remote add upstream https://github.com/gsd-build/get-shit-done.git`.
    - **Script exits after rebase without running leak scan**: The
      `git diff --name-only` produced no output (empty rebase) and the
      `|| true` short-circuited. This is normal.

    --- END docs/UPSTREAM-REBASE.md ---

    Render as idiomatic markdown with fenced code blocks for shell
    commands. Do NOT add sections not listed above. Do NOT soften the
    "RED FLAG / INVESTIGATE" framing for category 2 conflicts - D-14
    locks this as the intended framing.
  </action>
  <verify>
    <automated>cd /Volumes/code/get-shit-done && test -f docs/UPSTREAM-REBASE.md && awk 'END{exit (NR >= 60) ? 0 : 1}' docs/UPSTREAM-REBASE.md && grep -c "INVESTIGATE" docs/UPSTREAM-REBASE.md | awk '{exit ($1 >= 1) ? 0 : 1}' && grep -c "RED FLAG\|red flag" docs/UPSTREAM-REBASE.md | awk '{exit ($1 >= 1) ? 0 : 1}' && grep -c "Seam conflicts" docs/UPSTREAM-REBASE.md | awk '{exit ($1 >= 1) ? 0 : 1}' && grep -c "Business-logic conflicts" docs/UPSTREAM-REBASE.md | awk '{exit ($1 >= 1) ? 0 : 1}' && grep -c "CJS binary" docs/UPSTREAM-REBASE.md | awk '{exit ($1 >= 1) ? 0 : 1}' && grep -c "scripts/sync-upstream.sh" docs/UPSTREAM-REBASE.md | awk '{exit ($1 >= 1) ? 0 : 1}' && grep -c "advisory\|non-blocking" docs/UPSTREAM-REBASE.md | awk '{exit ($1 >= 1) ? 0 : 1}' && grep -c "On-demand" docs/UPSTREAM-REBASE.md | awk '{exit ($1 >= 1) ? 0 : 1}' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/UPSTREAM-REBASE.md` with >= 60 lines
    - Contains H1 `# Upstream Rebase Playbook`
    - Contains all H2 headings: `## When to sync`, `## Quick path`, `## Manual path`, `## Conflict taxonomy`, `## Leak-grep output interpretation`, `## Prior rebase history`, `## Troubleshooting`
    - Contains H3 headings `### 1. Seam conflicts`, `### 2. Business-logic conflicts (RED FLAG - INVESTIGATE)`, `### 3. CJS binary conflicts` (D-14: taxonomy covers both seam AND business-logic; business-logic framed as red flag)
    - `grep -c "INVESTIGATE" docs/UPSTREAM-REBASE.md` >= 1
    - `grep -Ec "RED FLAG|red flag" docs/UPSTREAM-REBASE.md` >= 1
    - `grep -c "scripts/sync-upstream.sh" docs/UPSTREAM-REBASE.md` >= 1
    - `grep -Ec "advisory|non-blocking" docs/UPSTREAM-REBASE.md` >= 1
    - `grep -c "On-demand" docs/UPSTREAM-REBASE.md` >= 1 (cadence section)
    - `grep -c "leak-grep" docs/UPSTREAM-REBASE.md` >= 2 (referenced in multiple sections)
    - `grep -Ec "TODO|TBD|PLACEHOLDER" docs/UPSTREAM-REBASE.md` equals 0
  </acceptance_criteria>
  <done>
    Playbook exists with full conflict taxonomy, business-logic red-flag framing, and references to the helper script.
  </done>
</task>

</tasks>

<threat_model>
Security note (not blocking; security_enforcement=false):
- `scripts/sync-upstream.sh` shells out to `git` with `$UPSTREAM_REF` as
  an argument. The argument is passed through the caller's argv and is
  trusted (maintainer-controlled). No untrusted git output is `eval`ed
  or interpolated into further commands without quoting. `git rebase`
  refuses if the working tree is dirty — no risk of silent data loss.
- `xargs node scripts/leak-grep.cjs` accepts file paths from
  `git diff --name-only`. These paths are git-controlled (not user
  argv) and are passed verbatim. leak-grep reads file content with
  `fs.readFileSync`; a malicious upstream could include a file with an
  unusual name, but xargs handles whitespace correctly and leak-grep
  runs as the local user — no privilege escalation path.
</threat_model>

<verification>
After both tasks:
- `bash -n scripts/sync-upstream.sh` exits 0
- `test -x scripts/sync-upstream.sh` passes
- `test -f docs/UPSTREAM-REBASE.md` passes with >= 60 lines
- `grep -c "INVESTIGATE" docs/UPSTREAM-REBASE.md` >= 1
- Smoke: `./scripts/sync-upstream.sh HEAD` from a clean tree runs the
  rebase loop against current HEAD (no-op rebase), exercises the diff
  path (empty), prints "No changed files after rebase." and exits 0.
  Document this smoke test in the SUMMARY.
</verification>

<success_criteria>
1. A fork maintainer can run `./scripts/sync-upstream.sh` on a clean
   tree and get a rebase + advisory leak scan without a gate that
   blocks ordinary syncs (D-13).
2. The playbook distinguishes mechanical (seam + CJS) conflicts from
   red-flag business-logic conflicts (D-14), with an explicit
   INVESTIGATE workflow for the red-flag case.
3. Cadence is explicitly on-demand (D-15); no cron or GHA trigger is
   added anywhere in this plan.
4. Running `bash -n scripts/sync-upstream.sh` exits 0 (syntax gate).
</success_criteria>

<output>
After completion, create `.planning/phases/08-migration-distribution/08-04-SUMMARY.md` describing:
- Script design choices (shebang, set -e, || true on leak-grep)
- Confirmation that `bash -n` is clean
- Result of the no-op smoke test against HEAD (if run)
- How the doc frames business-logic conflicts relative to seam conflicts
</output>
