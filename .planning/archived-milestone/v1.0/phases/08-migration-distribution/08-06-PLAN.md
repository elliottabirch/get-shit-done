<!-- leak-grep-allow file — generated PLAN.md artifact; @.planning/* context refs and shell-pattern examples in prose are consumed by executor via SDK, not auto-loaded by skills -->
---
phase: 08-migration-distribution
plan: 06
type: execute
wave: 3
depends_on: [02, 03, 04, 05]
files_modified:
  - .planning/DECISIONS.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [DIST-05]
tags: [phase-8, dist-05, adr-placeholder, phase-close]

must_haves:
  truths:
    - "An ADR record for DIST-05 is appended to `.planning/DECISIONS.md` at phase-close, citing upstream's reception of #2898/#2901/#2908 and the fork's own Phase 8 execution outcome"
    - "The ADR records the distribution decision: submit upstream as PR, maintain long-lived fork, or hybrid"
    - "`.planning/REQUIREMENTS.md` DIST-05 status row is flipped from `Pending` to `Complete (Phase 8)`"
    - "This is a PLACEHOLDER plan — its body is written at phase-close, NOT during initial plan execution (D-02 locked)"
    - "No task executes until the other five Phase 8 plans have shipped and the phase-close context is available"
  artifacts:
    - path: ".planning/DECISIONS.md"
      provides: "Appended ADR for DIST-05 distribution decision"
      contains: "DIST-05"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DIST-05 traceability row flipped to Complete"
      contains: "DIST-05 | Phase 8 — Migration + distribution | both | Complete"
  key_links:
    - from: ".planning/DECISIONS.md DIST-05 ADR"
      to: "upstream PRs #2898, #2901, #2908 reception"
      via: "prose citation of upstream maintainer response (merged / rejected / stalled)"
      pattern: "#2898|#2901|#2908"
    - from: ".planning/REQUIREMENTS.md DIST-05 row"
      to: "Phase 8 shipping status"
      via: "status column flip from Pending to Complete"
      pattern: "DIST-05.*Complete"
---

<objective>
DIST-05: Record the distribution decision (submit upstream as PR vs
maintain long-lived fork vs hybrid) in `.planning/DECISIONS.md` at
phase-close, and flip the DIST-05 row in `.planning/REQUIREMENTS.md`
to Complete. This is a PLACEHOLDER plan per CONTEXT.md D-02: the ADR
body is filled in only after DIST-01..04 have shipped, because the
inputs (upstream's reception of #2898/#2901/#2908 + fork's Phase 8
execution outcome) are not stable until then.

Purpose: The strict-superset invariant and the rebase playbook mean
this fork CAN live indefinitely as a fork OR be proposed upstream as a
PR. Which path is right depends on signal that does not exist at
Phase 8 kickoff — upstream's recent response to seam-abstraction PRs,
the fork's own rebase pain curve, and whether external adopters have
appeared. DIST-05 is the artifact that captures the answer, not a
pre-decided outcome.

Output:
  - New ADR section in `.planning/DECISIONS.md` (written at phase-close)
  - DIST-05 row in `.planning/REQUIREMENTS.md` flipped to `Complete (Phase 8)`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/DECISIONS.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-migration-distribution/08-CONTEXT.md

<interfaces>
<!-- This is a placeholder plan - most "inputs" are generated DURING phase 8 execution, not pre-defined here. -->

## Inputs that must exist before the ADR can be written

Before starting Task 1, confirm ALL of the following are true:

- All five other Phase 8 plans (01 Pre-0, 02 DIST-01, 03 DIST-02,
  04 DIST-03, 05 DIST-04) have shipped and their `*-SUMMARY.md`
  files exist.
- At least one green `upstream-parity` CI run exists (from plan 05
  checkpoint).
- `.planning/STATE.md` reflects Phase 8 progress at >= 5/6 plans
  complete.
- Fork's own retrospective notes (execution pain, frequency of direct-
  I/O leaks reintroduced upstream, rebase conflict volume) are
  available — either directly in `STATE.md`, in a
  `.planning/phases/08-migration-distribution/*-RETROSPECTIVE.md`
  draft, or (minimum) as a paragraph the developer writes during this
  plan's execution.

## Existing DECISIONS.md format

The existing ADRs in `.planning/DECISIONS.md` use this structure
(replicate it):

```markdown
## D-YYYY-MM-DD-<SLUG>

**Date:** YYYY-MM-DD
**Phase:** N (name)
**Status:** Accepted

### Context
<what prompted the decision, what inputs were available>

### Decision
<what was decided, in one sentence>

### Rationale
<why this option over the alternatives>

### Consequences
<what follows from this; forward references to future phases if any>
```

## REQUIREMENTS.md traceability table format

`.planning/REQUIREMENTS.md` lines ~184-188 contain the DIST rows:

```markdown
| DIST-05 | Phase 8 — Migration + distribution | both | Pending |
```

The cell to flip is the final `Pending` -> `Complete (Phase 8)`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Gather phase-close signal + decide PR-vs-fork</name>
  <files>.planning/DECISIONS.md</files>
  <read_first>
    - .planning/DECISIONS.md (existing ADR format to replicate)
    - .planning/phases/08-migration-distribution/08-0{1..5}-SUMMARY.md (phase-8 retrospective inputs)
    - .planning/REQUIREMENTS.md §DIST rows + Future Requirements §NPM-01 (external-adopter context)
    - The actual state of upstream PRs #2898, #2901, #2908 at decision time — check via `gh pr view 2898 --repo gsd-build/get-shit-done --json state,mergedAt,closedAt,reviewDecision` (repeat for 2901, 2908)
  </read_first>
  <action>
    This is a decision checkpoint. Claude's role: load the phase-close
    context above and present the option-a / option-b / option-c menu
    (below) to the human with one-line pros/cons distilled from the
    full options block. Do NOT decide. Do NOT simulate a decision.

    Before presenting the menu, gather the three input signals and
    include them in the question:

    1. Upstream PR reception: run the `gh pr view` commands listed in
       read_first for PRs 2898/2901/2908. Report each PR's state
       (merged / closed-unmerged / open / draft), mergedAt/closedAt
       date, and reviewDecision. A 3-row table is sufficient.
    2. Fork's own rebase retrospective: search the fork's own git log
       for invocations of `scripts/sync-upstream.sh` (if any real runs
       have happened since DIST-03 landed) and summarize conflict
       counts. If no real runs exist yet, say so plainly.
    3. External-adopter signal: list recent GitHub activity (issues,
       PRs, forks) from accounts other than the project's primary
       maintainer. If the repo is private or the signal is not
       available, say so.

    After presenting, wait for the human to select option-a, -b, or
    -c and provide their one-paragraph rationale. Task 2 uses both.
  </action>
  <verify>
    <automated>MISSING — this is a decision checkpoint; automated verification is not applicable. Task 2's verify block checks the resulting ADR text.</automated>
  </verify>
  <done>
    Human selects one of {option-a, option-b, option-c} and supplies a
    rationale paragraph. Claude does NOT proceed to Task 2 until the
    selection is explicit.
  </done>
  <decision>
    Record the distribution decision for the v1.0 milestone: submit the
    fork's adapter seam work as a PR upstream, maintain a long-lived
    fork indefinitely, or pursue a hybrid path (submit a subset
    upstream; keep the rest fork-side).
  </decision>
  <context>
    By the time this task runs, Phase 8 plans 01-05 have shipped and
    the fork has empirical data that did not exist at phase kickoff:

    - Whether upstream's maintainers have engaged with, merged,
      modified, or rejected PRs #2898 (durable planning runtime),
      #2901 (planning-workspace seam), and #2908 (manifest-backed
      routing seam). These PRs are the closest analogs to what a
      "submit fork upstream" proposal would look like.
    - The fork's own rebase-conflict curve from running
      `scripts/sync-upstream.sh` (DIST-03) one or more times against
      real upstream batches.
    - Whether external adopters of `gsd-beads` have appeared (noted
      in `.planning/REQUIREMENTS.md` deferred NPM-01).
    - Strict-superset parity (DIST-04) either holding or regressing
      over a meaningful number of PRs.

    This information was not available on 2026-05-13 when Phase 8 was
    planned. It IS available here. Use it.
  </context>
  <options>
    <option id="option-a">
      <name>Submit fork upstream as PR</name>
      <pros>
        - Eliminates the long-term rebase cost curve entirely
        - Benefits the upstream community if the design is well-received
        - Resolves the "two-repo" concern from D-2026-04-30-02 (sibling becomes a plug-in, not a required companion)
      </pros>
      <cons>
        - Requires the upstream maintainers to accept significant
          architectural work they did not initiate
        - If PRs #2898/#2901/#2908 were NOT received well, the probability
          of acceptance is low; effort spent on PR authoring is lost
        - Locks the adapter seam into upstream's review cadence, which
          was the motivation to fork in the first place
      </cons>
    </option>
    <option id="option-b">
      <name>Maintain long-lived fork</name>
      <pros>
        - Zero dependency on upstream acceptance; ship at our cadence
        - The rebase playbook (DIST-03) and parity CI (DIST-04) are
          already in place and shown to work
        - Preserves full control over the adapter-seam roadmap (future
          adapters, capability changes)
      </pros>
      <cons>
        - Ongoing rebase cost grows with upstream divergence
        - External adopters pay an install-from-fork tax
        - If upstream does eventually ship similar work, we duplicate
          effort or face a later-merge cost
      </cons>
    </option>
    <option id="option-c">
      <name>Hybrid: submit foundational pieces upstream; keep adapters fork-side</name>
      <pros>
        - Most of the strict-superset work (createRegistry DI,
          config-schema patterns, leak-grep) is foundational and could
          be upstream-valuable even without adapter plumbing
        - Keeps the BeadsAdapter + capability-negotiation work fork-side
          where it doesn't require upstream buy-in on two-repo models
        - Lower PR-authoring cost than option-a (smaller surface)
      </pros>
      <cons>
        - Higher coordination complexity; requires the fork to stay in
          sync with upstream's acceptance of the foundational pieces
        - If upstream changes the accepted pieces during review, the
          fork-side adapter code needs corresponding edits
        - "Partial" shipping has a fuzzier success criterion than "all
          or nothing"
      </cons>
    </option>
  </options>
  <resume-signal>
    Select option-a, option-b, or option-c. Include a one-paragraph
    rationale citing:
    1. The actual reception of upstream PRs #2898/#2901/#2908 (merged,
       stalled, rejected, or actively-reviewing as of the decision
       date)
    2. Fork's own Phase 8 retrospective (rebase cost, regression count,
       external-adopter signal)
    3. Any carry-forward commitments (sibling repo `gsd-beads` state)
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append ADR to DECISIONS.md + flip REQUIREMENTS.md row</name>
  <files>
    .planning/DECISIONS.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/DECISIONS.md (entire file - inspect existing ADR section formatting so the new entry matches)
    - .planning/REQUIREMENTS.md lines 184-202 (the DIST traceability rows; identify exact Pending-cell location for DIST-05)
    - `.planning/phases/08-migration-distribution/*-SUMMARY.md` files for plans 01-05 (context for the Rationale section)
    - `.planning/phases/08-migration-distribution/08-05-SUMMARY.md` (DIST-04 summary - contains the first green parity CI URL, cite in Rationale)
  </read_first>
  <action>
    Two-file atomic change:

    **1. Append to `.planning/DECISIONS.md`:**

    Add a new ADR section AT THE END of the file, matching the existing
    DECISIONS.md format. Use today's date (the date at which this
    plan's Task 2 is executed, NOT 2026-05-13 — the placeholder date).
    The chosen option from Task 1 populates the Decision field.

    Template (substitute the bracketed fields based on the Task 1
    decision signal):

    ```markdown

    ## D-<YYYY-MM-DD>-DIST-05

    **Date:** <YYYY-MM-DD (date of Task 2 execution)>
    **Phase:** 8 (migration-distribution)
    **Status:** Accepted

    ### Context

    By Phase 8 close, the fork has shipped the `StorageAdapter` seam,
    BeadsAdapter implementation (sibling), conformance test suite,
    migration docs, rebase playbook, and strict-superset parity CI
    gate. The v1.0 milestone's DIST-05 requirement is to record
    whether this work is proposed upstream as a PR or maintained as
    a long-lived fork.

    Inputs available at decision time:

    - Upstream PRs #2898 / #2901 / #2908 status as of <YYYY-MM-DD>:
      <merged | stalled | rejected | actively-reviewing>
    - Fork's own Phase 8 rebase cost: <describe quantitatively if
      possible — number of sync-upstream.sh runs, conflicts per run,
      leak-grep hits per sync>
    - First-green `upstream-parity` CI run: <URL from
      08-05-SUMMARY.md>
    - External adopter signal: <describe — npm-install count,
      issues/PRs from non-fork contributors, or "none"; cite
      REQUIREMENTS.md NPM-01>
    - Sibling `gsd-beads` state: <currently on feat/phase-6-reset;
      graduation to main tracked as a post-v1.0 deferred item>

    ### Decision

    <The option chosen in Task 1 — option-a: "Submit upstream as PR",
    option-b: "Maintain long-lived fork", or option-c: "Hybrid:
    foundational pieces upstream, adapters fork-side". Render the
    chosen option as a clear single-sentence statement.>

    ### Rationale

    <One paragraph citing the three inputs called out in Task 1's
    resume-signal: upstream reception, fork retrospective, carry-
    forward commitments. Do NOT soften the decision with qualifiers;
    the ADR records what WAS decided, not what may still change.>

    ### Consequences

    - <Forward-reference for what happens next under the chosen path,
      e.g. "Open upstream PR against main at <tracked-ref>", or
      "Document fork-maintenance cadence in PROJECT.md; graduate
      sibling to main", or "Stage foundational PRs X, Y, Z and file
      follow-up issues for adapter discussions">
    - <Any deferred items being unblocked or newly deferred by this
      decision (cite by REQ-ID: NPM-01, OTHER-ADAPTERS-01, etc.)>
    - <Any DIST-02 / DIST-03 doc updates that follow from this
      decision — e.g. if option-a, docs/MIGRATION.md references shift
      from "fork" to "upstream-merged release X.Y.Z">

    ```

    **2. Edit `.planning/REQUIREMENTS.md`:**

    Find the row:
    `| DIST-05 | Phase 8 — Migration + distribution | both | Pending |`

    Replace `Pending` with `Complete (Phase 8)` so the row reads:
    `| DIST-05 | Phase 8 — Migration + distribution | both | Complete (Phase 8) |`

    Also flip DIST-01, DIST-02, DIST-03, DIST-04 in the same traceability
    table IF they are still `Pending` — by the time Task 2 runs, plans
    01-05 have shipped; all five DIST rows should flip to
    `Complete (Phase 8)` together. Verify each DIST-N row's status.

    Also update the Active Requirements section (lines 80-84) - change
    `- [ ] **DIST-0N**:` to `- [x] **DIST-0N**:` for each of DIST-01,
    DIST-02, DIST-03, DIST-04, DIST-05.

    Do NOT modify any non-DIST requirements. Do NOT edit any prior
    phase's completion records.
  </action>
  <verify>
    <automated>cd /Volumes/code/get-shit-done && grep -c "DIST-05" .planning/DECISIONS.md | awk '{exit ($1 >= 1) ? 0 : 1}' && grep -c "DIST-05 | Phase 8 .* Complete" .planning/REQUIREMENTS.md | awk '{exit ($1 >= 1) ? 0 : 1}' && bash -c 'pending=$(grep -c "DIST-0. .*Pending" .planning/REQUIREMENTS.md); if [ "$pending" = "0" ]; then echo "PASS: all DIST rows flipped"; else echo "FAIL: $pending DIST rows still Pending"; grep "DIST-0..*Pending" .planning/REQUIREMENTS.md; exit 1; fi' && grep -c "^- \[x\] \*\*DIST-05\*\*" .planning/REQUIREMENTS.md | awk '{exit ($1 >= 1) ? 0 : 1}' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/DECISIONS.md` contains a new `## D-<YYYY-MM-DD>-DIST-05` section
    - The ADR has all four subheadings: `### Context`, `### Decision`, `### Rationale`, `### Consequences`
    - `grep -c "#2898" .planning/DECISIONS.md` >= 1 (upstream PR reception is cited)
    - `grep -c "#2901" .planning/DECISIONS.md` >= 1
    - `grep -c "#2908" .planning/DECISIONS.md` >= 1
    - `.planning/REQUIREMENTS.md` `DIST-05` row reads `| DIST-05 | Phase 8 — Migration + distribution | both | Complete (Phase 8) |`
    - `grep -c "DIST-0..*Pending" .planning/REQUIREMENTS.md` equals 0 (all DIST rows flipped to Complete)
    - `grep -c "^- \[x\] \*\*DIST-01\*\*" .planning/REQUIREMENTS.md` >= 1
    - `grep -c "^- \[x\] \*\*DIST-05\*\*" .planning/REQUIREMENTS.md` >= 1
    - No TODO/PLACEHOLDER/TBD markers in the new ADR section:
      `grep -c "TODO\|PLACEHOLDER\|TBD\|<.*>" .planning/DECISIONS.md | tail -20` should show only legitimate angle-bracketed identifiers that were explicitly intended (e.g., none in the new ADR)
  </acceptance_criteria>
  <done>
    DIST-05 ADR appended with real signal; all DIST requirement rows flipped to Complete.
  </done>
</task>

</tasks>

<threat_model>
Not applicable. This is a documentation-only plan that appends
decision records to project planning files. No runtime code paths, no
trust boundaries, no user input processed.
</threat_model>

<verification>
After Task 2:
- `grep -A 30 "DIST-05" .planning/DECISIONS.md` shows the complete ADR
  with populated Context / Decision / Rationale / Consequences
- `grep "DIST-0" .planning/REQUIREMENTS.md | grep "Pending"` returns no
  matches
- `wc -l .planning/DECISIONS.md` has grown by >= 25 lines vs the
  pre-Task-2 baseline (documented in the plan 06 SUMMARY)
</verification>

<success_criteria>
1. DIST-05 is resolved with a recorded decision citing real inputs
   (upstream PR reception, fork retrospective, carry-forward state).
2. The v1.0 milestone is fully traceable: every REQ-ID has status
   Complete in REQUIREMENTS.md.
3. The ADR does not hedge — it records a concrete answer (option-a,
   option-b, or option-c) even if the answer is "maintain fork
   indefinitely". Placeholder language ("to be decided", "TBD") is
   explicitly forbidden.
</success_criteria>

<output>
After completion, create `.planning/phases/08-migration-distribution/08-06-SUMMARY.md` describing:
- The chosen option and one-paragraph rationale
- Inputs gathered (upstream PR state, rebase retrospective, adopter
  signal) at decision time
- Forward references: any follow-up work the decision creates
  (upstream PR branches, fork-maintenance cadence docs, deferred
  milestones)
- Phase 8 close-out confirmation: all DIST rows flipped, Phase 8 row in
  ROADMAP.md ready to flip to [x] (done in the phase-close step,
  not this plan's SUMMARY)
</output>
