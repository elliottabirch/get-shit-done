---
phase: 02-make-the-seam-real
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - sdk/src/query/phase-lifecycle.ts
  - sdk/src/query/spike-sketch.ts
  - sdk/src/query/scratch.ts
  - sdk/src/query/index.ts
autonomous: true
requirements:
  - SEAM-01
  - SEAM-03
tags: [seam, handler-migration, phase-lifecycle, spike-sketch, scratch]

must_haves:
  truths:
    - "phase-lifecycle.ts contains zero adapterFor(projectDir) callsites after this plan"
    - "spike-sketch.ts contains zero adapterFor(projectDir) callsites after this plan"
    - "scratch.ts contains zero adapterFor(projectDir) callsites after this plan"
    - "Every handler exported from these files accepts adapter as first arg"
    - "index.ts uses closure wrappers (or family-dict closures, where alias loops apply) for every handler in these files"
    - "No StateWriteOutcome propagation needed — none of these handlers call recordState* (per RESEARCH.md inventory)"
    - "Per-task per-commit gate green (build:sdk-only + test:unit) for each of the 3 atomic per-file commits"
    - "D-18 honored: every commit in this plan is preceded by `npm run build:sdk-only && cd sdk && npm run test:unit` exiting 0; gate failure → revert/fix in a new commit, never --amend or --no-verify past the gate"
  artifacts:
    - path: "sdk/src/query/phase-lifecycle.ts"
      provides: "10 handlers receive adapter as first arg; no internal adapterFor calls"
      contains: "adapter: StorageAdapter"
    - path: "sdk/src/query/spike-sketch.ts"
      provides: "7 handlers receive adapter as first arg"
      contains: "adapter: StorageAdapter"
    - path: "sdk/src/query/scratch.ts"
      provides: "6 handlers receive adapter as first arg"
      contains: "adapter: StorageAdapter"
    - path: "sdk/src/query/index.ts"
      provides: "Closure wrappers for phase-lifecycle, spike-sketch, scratch handlers"
      contains: "adapter, args"
  key_links:
    - from: "sdk/src/query/index.ts"
      to: "sdk/src/query/phase-lifecycle.ts"
      via: "closure wrapper threading adapter"
      pattern: "phase-lifecycle handler.*adapter"
    - from: "sdk/src/query/index.ts"
      to: "sdk/src/query/spike-sketch.ts"
      via: "closure wrapper threading adapter"
      pattern: "spike.*adapter|sketch.*adapter"
---

<objective>
Migrate three handler-family files in atomic per-file commits (per D-15 / D-17 — though without the StateWriteOutcome split because none of these handlers call `recordState*`):

- `sdk/src/query/phase-lifecycle.ts` (10 callsites — phase lifecycle handlers like `initExecutePhase`)
- `sdk/src/query/spike-sketch.ts` (7 callsites — `spikeGetManifest`, `spikePutWrapUp`, `sketchGetManifest`, etc.)
- `sdk/src/query/scratch.ts` (6 callsites — `discussCheckpointPut/Get/Delete`, `discussQuestionsPut/Get/Delete`)

Three tasks, three atomic commits, one per file. Each commit is gated by `build:sdk-only && test:unit` per D-18. NEVER bundled. The pattern is identical to Plan 03 Commit 1 but without the StateWriteOutcome layer (RESEARCH.md inventory confirms none of these handlers call `recordState*` — they call `putRecord`/`getRecord`/`putNamedDoc`/etc. instead).

Output:
- Each of the three files has zero `adapterFor(projectDir)` callsites; every exported handler accepts adapter as first arg.
- `sdk/src/query/index.ts` uses closure wrappers for each migrated handler.

This plan does NOT migrate other handler-family files (Plans 05, 06). It does NOT delete `adapterFor` from helpers.ts (Plan 07).
</objective>

<execution_context>
@/Volumes/code/get-shit-done/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/code/get-shit-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-make-the-seam-real/02-CONTEXT.md
@.planning/phases/02-make-the-seam-real/02-RESEARCH.md
@.planning/phases/02-make-the-seam-real/02-PATTERNS.md
@.planning/phases/02-make-the-seam-real/02-VALIDATION.md
@.planning/phases/02-make-the-seam-real/02-03-SUMMARY.md

<interfaces>
<!-- Migration pattern is identical to Plan 03 Commit 1 (adapter threading); see 02-03-PLAN.md for the canonical transformation. -->

From RESEARCH.md §"Handler Family Inventory":
- phase-lifecycle.ts: 10 callsites; handlers `initExecutePhase` (and other init.* phase lifecycle handlers); read+write mix; no recordState* calls
- spike-sketch.ts: 7 callsites; handlers `spikeGetManifest`, `spikeGetConventions`, `spikePutWrapUp`, `spikePutConventions`, `sketchGetManifest`, `sketchGetConventions`, `sketchPutWrapUp`; mixed; uses putRecord/getRecord
- scratch.ts: 6 callsites; handlers `discussCheckpointPut/Get/Delete`, `discussQuestionsPut/Get/Delete`; mixed; uses putRecord/getRecord

From PATTERNS.md §"Handler files" (the canonical Before/After transformation — exactly the same pattern as Plan 03 Commit 1):
- Before: `export const handlerName: QueryHandler = async (args, projectDir, ...) => { const adapter = await adapterFor(projectDir); ... }`
- After: `export async function handlerName(adapter: StorageAdapter, args: string[], projectDir: string, ws?: string): Promise<QueryResult> { ... }` (or const arrow form with adapter as first param)

From sdk/src/query/index.ts (current registration pattern for these handlers):
- phase-lifecycle handlers register via direct ref OR via init.* command family (locate via grep `initExecutePhase\|initPlanPhase` in index.ts)
- spike-sketch handlers register via individual `registry.register('spike.get-manifest', spikeGetManifest)` calls (locate via grep `spike\.\|sketch\.` in index.ts)
- scratch handlers register via individual calls (locate via grep `discuss-checkpoint\|discuss-questions`)

Migration target — closure wrapper for each registration:
```typescript
registry.register('spike.get-manifest', (args, pd, ws) => spikeGetManifest(adapter, args, pd, ws));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate phase-lifecycle.ts (10 callsites) — atomic per-file commit</name>
  <files>sdk/src/query/phase-lifecycle.ts, sdk/src/query/index.ts</files>
  <read_first>
    - .planning/phases/02-make-the-seam-real/02-CONTEXT.md (D-06, D-15, D-17)
    - .planning/phases/02-make-the-seam-real/02-PATTERNS.md (§"Handler files" Before/After; §"Registration pattern change")
    - .planning/phases/02-make-the-seam-real/02-RESEARCH.md (§"Handler Family Inventory" — phase-lifecycle.ts row)
    - sdk/src/query/phase-lifecycle.ts (full file — locate all 10 adapterFor callsites via `grep -n "adapterFor(projectDir)" sdk/src/query/phase-lifecycle.ts`)
    - sdk/src/query/index.ts (registration sites for phase-lifecycle handlers)
    - .planning/phases/02-make-the-seam-real/02-03-PLAN.md Task 1 (the canonical Plan 03 Commit 1 pattern — apply identically)
  </read_first>
  <behavior>
    - phase-lifecycle.ts contains zero `adapterFor(projectDir)` callsites
    - Every exported handler accepts adapter as first arg
    - phase-lifecycle.ts no longer imports adapterFor
    - index.ts wraps every phase-lifecycle handler registration in a closure threading adapter
    - All existing unit tests pass
    - All conformance tests pass
  </behavior>
  <action>
Apply the canonical Plan 03 Commit 1 pattern (adapter-threading) to `sdk/src/query/phase-lifecycle.ts`. NO StateWriteOutcome work — none of these handlers call `recordState*`.

Step 1 — In `sdk/src/query/phase-lifecycle.ts`:
- ADD: `import type { StorageAdapter } from '../../../adapters/types.js';`
- REMOVE: `adapterFor` from the helpers.js import destructure
- For every exported handler, change the signature from `(args, projectDir, ws) => { const adapter = await adapterFor(projectDir); ... }` to `(adapter: StorageAdapter, args: string[], projectDir: string, ws?: string) => { ... }` and DELETE the `adapterFor` line in the body.
- For every internal helper that uses `adapterFor`, change its signature to accept adapter as first arg, then update its callers within the same file.

Step 2 — In `sdk/src/query/index.ts`, locate every registration of a phase-lifecycle handler. The pattern is mostly direct registration (per RESEARCH.md "NOT YET migrated" inventory). For each:

BEFORE:
```typescript
registry.register('phase.something', phaseSomething);
```
AFTER:
```typescript
registry.register('phase.something', (args, pd, ws) => phaseSomething(adapter, args, pd, ws));
```

If any phase-lifecycle handlers use a dict + alias loop pattern (like state-mutation), apply the closure-wrapper-in-dict pattern from Plan 03 Task 1.

Step 3 — Per-commit gate (D-18):
```bash
cd /Volumes/code/get-shit-done && npm run build:sdk-only && cd sdk && npm run test:unit
```

Step 4 — Per-file grep gate:
```bash
[ "$(grep -c 'adapterFor(projectDir)' sdk/src/query/phase-lifecycle.ts)" = "0" ]
```

Step 5 — Commit message: `refactor(02-04): thread adapter through phase-lifecycle.ts handlers (10 callsites)`.
  </action>
  <verify>
    <automated>cd /Volumes/code/get-shit-done && [ "$(grep -c 'adapterFor(projectDir)' sdk/src/query/phase-lifecycle.ts)" = "0" ] && grep -q "import type { StorageAdapter }" sdk/src/query/phase-lifecycle.ts && npm run build:sdk-only && cd sdk && npm run test:unit && cd .. && npm run test:conformance:paired</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adapterFor(projectDir)" sdk/src/query/phase-lifecycle.ts` returns "0"
    - `grep -q "import type { StorageAdapter }" sdk/src/query/phase-lifecycle.ts` exits 0
    - `cd /Volumes/code/get-shit-done && npm run build:sdk-only` exits 0
    - `cd /Volumes/code/get-shit-done/sdk && npm run test:unit` exits 0
    - `cd /Volumes/code/get-shit-done && npm run test:conformance:paired` exits 0
    - Commit lands as a SINGLE atomic commit (not bundled with Tasks 2 or 3)
  </acceptance_criteria>
  <done>
    phase-lifecycle.ts fully migrated; per-file grep returns 0; all gates green; atomic per-file commit landed independently per D-15/D-17.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate spike-sketch.ts (7 callsites) — atomic per-file commit</name>
  <files>sdk/src/query/spike-sketch.ts, sdk/src/query/index.ts</files>
  <read_first>
    - .planning/phases/02-make-the-seam-real/02-CONTEXT.md (D-06, D-15, D-17)
    - .planning/phases/02-make-the-seam-real/02-PATTERNS.md (§"Handler files" Before/After; §"Registration pattern change")
    - .planning/phases/02-make-the-seam-real/02-RESEARCH.md (§"Handler Family Inventory" — spike-sketch.ts row; uses putRecord/getRecord, NOT recordState*)
    - sdk/src/query/spike-sketch.ts (full file — locate all 7 adapterFor callsites)
    - sdk/src/query/index.ts (registration sites for spike-sketch handlers)
  </read_first>
  <behavior>
    - spike-sketch.ts contains zero `adapterFor(projectDir)` callsites
    - 7 handlers (spikeGetManifest, spikeGetConventions, spikePutWrapUp, spikePutConventions, sketchGetManifest, sketchGetConventions, sketchPutWrapUp) accept adapter as first arg
    - index.ts uses closure wrappers
    - All unit tests pass; conformance pass
  </behavior>
  <action>
Apply the canonical adapter-threading pattern to `sdk/src/query/spike-sketch.ts`. Same shape as Task 1.

Step 1 — In spike-sketch.ts: add StorageAdapter type import; remove adapterFor import; migrate all 7 handler signatures; delete the 7 `adapterFor(projectDir)` lines.

Step 2 — In index.ts: wrap every spike.* and sketch.* registration in a closure that threads adapter.

Step 3 — Per-commit gate:
```bash
cd /Volumes/code/get-shit-done && npm run build:sdk-only && cd sdk && npm run test:unit
```

Step 4 — Per-file grep gate:
```bash
[ "$(grep -c 'adapterFor(projectDir)' sdk/src/query/spike-sketch.ts)" = "0" ]
```

Step 5 — Commit message: `refactor(02-04): thread adapter through spike-sketch.ts handlers (7 callsites)`.
  </action>
  <verify>
    <automated>cd /Volumes/code/get-shit-done && [ "$(grep -c 'adapterFor(projectDir)' sdk/src/query/spike-sketch.ts)" = "0" ] && grep -q "import type { StorageAdapter }" sdk/src/query/spike-sketch.ts && npm run build:sdk-only && cd sdk && npm run test:unit && cd .. && npm run test:conformance:paired</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adapterFor(projectDir)" sdk/src/query/spike-sketch.ts` returns "0"
    - `grep -q "import type { StorageAdapter }" sdk/src/query/spike-sketch.ts` exits 0
    - `cd /Volumes/code/get-shit-done && npm run build:sdk-only` exits 0
    - `cd /Volumes/code/get-shit-done/sdk && npm run test:unit` exits 0
    - `cd /Volumes/code/get-shit-done && npm run test:conformance:paired` exits 0
    - Commit lands as a SINGLE atomic commit (separate from Task 1 and Task 3)
  </acceptance_criteria>
  <done>
    spike-sketch.ts fully migrated; per-file grep returns 0; gates green; atomic per-file commit landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate scratch.ts (6 callsites) — atomic per-file commit</name>
  <files>sdk/src/query/scratch.ts, sdk/src/query/index.ts</files>
  <read_first>
    - .planning/phases/02-make-the-seam-real/02-CONTEXT.md (D-06, D-15, D-17)
    - .planning/phases/02-make-the-seam-real/02-PATTERNS.md (§"Handler files" Before/After)
    - .planning/phases/02-make-the-seam-real/02-RESEARCH.md (§"Handler Family Inventory" — scratch.ts row; uses putRecord/getRecord)
    - sdk/src/query/scratch.ts (full file — locate all 6 adapterFor callsites)
    - sdk/src/query/index.ts (registration sites for discuss-checkpoint and discuss-questions)
  </read_first>
  <behavior>
    - scratch.ts contains zero `adapterFor(projectDir)` callsites
    - 6 handlers (discussCheckpointPut, discussCheckpointGet, discussCheckpointDelete, discussQuestionsPut, discussQuestionsGet, discussQuestionsDelete) accept adapter as first arg
    - index.ts uses closure wrappers
    - All unit tests pass; conformance pass
  </behavior>
  <action>
Apply the canonical adapter-threading pattern to `sdk/src/query/scratch.ts`. Same shape as Task 1.

Step 1 — In scratch.ts: add StorageAdapter type import; remove adapterFor import; migrate all 6 handler signatures; delete the 6 `adapterFor(projectDir)` lines.

Step 2 — In index.ts: wrap every discuss-checkpoint.* and discuss-questions.* registration in a closure threading adapter.

Step 3 — Per-commit gate:
```bash
cd /Volumes/code/get-shit-done && npm run build:sdk-only && cd sdk && npm run test:unit
```

Step 4 — Per-file grep gate:
```bash
[ "$(grep -c 'adapterFor(projectDir)' sdk/src/query/scratch.ts)" = "0" ]
```

Step 5 — Commit message: `refactor(02-04): thread adapter through scratch.ts handlers (6 callsites)`.
  </action>
  <verify>
    <automated>cd /Volumes/code/get-shit-done && [ "$(grep -c 'adapterFor(projectDir)' sdk/src/query/scratch.ts)" = "0" ] && grep -q "import type { StorageAdapter }" sdk/src/query/scratch.ts && npm run build:sdk-only && cd sdk && npm run test:unit && cd .. && npm run test:conformance:paired</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adapterFor(projectDir)" sdk/src/query/scratch.ts` returns "0"
    - `grep -q "import type { StorageAdapter }" sdk/src/query/scratch.ts` exits 0
    - `cd /Volumes/code/get-shit-done && npm run build:sdk-only` exits 0
    - `cd /Volumes/code/get-shit-done/sdk && npm run test:unit` exits 0
    - `cd /Volumes/code/get-shit-done && npm run test:conformance:paired` exits 0
    - Commit lands as a SINGLE atomic commit (separate from Task 1 and Task 2 — three commits total in this plan, gated independently per D-18)
  </acceptance_criteria>
  <done>
    scratch.ts fully migrated; per-file grep returns 0; gates green; atomic per-file commit landed. End of Plan 04.
  </done>
</task>

</tasks>

<threat_model>
Per `.planning/config.json` `workflow.security_enforcement: false`. No untrusted-input boundary touched.
</threat_model>

<verification>
After all 3 tasks land:

```bash
cd /Volumes/code/get-shit-done

# All 3 files migrated
[ "$(grep -c 'adapterFor(projectDir)' sdk/src/query/phase-lifecycle.ts)" = "0" ]
[ "$(grep -c 'adapterFor(projectDir)' sdk/src/query/spike-sketch.ts)" = "0" ]
[ "$(grep -c 'adapterFor(projectDir)' sdk/src/query/scratch.ts)" = "0" ]

# Three atomic commits exist
git log --oneline -3 | grep -c "refactor(02-04)"  # must be 3

# Gates green
npm run build:sdk-only && cd sdk && npm run test:unit && cd .. && npm run test:conformance:paired
```
</verification>

<success_criteria>
- 23 callsites migrated (10 + 7 + 6 across 3 files)
- No StateWriteOutcome propagation needed for these files (none call recordState*)
- THREE atomic commits per D-15/D-17 (one per file); build+test gate between each
- All unit tests pass; conformance pass after each commit
- index.ts uses closure-wrapper pattern for every migrated handler
</success_criteria>

<output>
After completion, create `.planning/phases/02-make-the-seam-real/02-04-SUMMARY.md` per standard summary template. Required tags `[seam, handler-migration, phase-lifecycle, spike-sketch, scratch]`. SUMMARY MUST list the 3 commit SHAs (one per file) — proves D-15 atomic-per-file split was honored.
</output>
