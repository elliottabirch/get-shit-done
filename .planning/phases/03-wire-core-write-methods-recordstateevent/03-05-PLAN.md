---
phase: 03-wire-core-write-methods-recordstateevent
plan: "05"
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified:
  - tests/conformance/write-events.test.ts
  - tests/conformance/write-transaction.test.ts
  - tests/conformance/commit-planning-state.test.ts
  - .planning/DECISIONS.md
autonomous: true
requirements: [WRITES-01, WRITES-02, WRITES-03, WRITES-04]
must_haves:
  truths:
    - "Conformance tests verify recordStateAppend round-trips (decision, metric, roadmap_evolution entries appear in STATE.md)"
    - "Conformance tests verify recordStateMutation round-trips (blocker add/resolve modifies STATE.md correctly)"
    - "Conformance tests verify recordStateSignal round-trips (WAITING.json created/removed)"
    - "Conformance tests verify withTransaction atomicity (fn completes or state unchanged)"
    - "Conformance test stub verifies commitPlanningState creates identifiable checkpoint"
    - "OQ-01 resolution documented in DECISIONS.md with ADR"
    - "leak-grep returns 0 matches against state-mutation.ts and phase-lifecycle.ts write surface"
  artifacts:
    - path: "tests/conformance/write-events.test.ts"
      provides: "Round-trip conformance tests for all 3 event families"
      contains: "recordStateAppend"
    - path: "tests/conformance/write-transaction.test.ts"
      provides: "withTransaction atomicity test"
      contains: "withTransaction"
    - path: "tests/conformance/commit-planning-state.test.ts"
      provides: "commitPlanningState checkpoint identification stub (Phase 7 fills paired test)"
      contains: "commitPlanningState"
    - path: ".planning/DECISIONS.md"
      provides: "OQ-01 resolution ADR + commitPlanningState promotion ADR"
      contains: "OQ-01"
  key_links:
    - from: "tests/conformance/write-events.test.ts"
      to: "adapters/markdown/index.ts"
      via: "MarkdownAdapter instantiation for conformance assertions"
      pattern: "new MarkdownAdapter"
    - from: "tests/conformance/write-transaction.test.ts"
      to: "adapters/markdown/index.ts"
      via: "withTransaction + getRecord/putRecord round-trip"
      pattern: "adapter.withTransaction"
---

<objective>
Verify the Phase 3 migration with conformance tests for event families, transaction atomicity, and commitPlanningState checkpoint. Document OQ-01 resolution in DECISIONS.md. Run final leak-grep verification against the write surface.

Purpose: Satisfy SC#2 (one call shape works against any adapter), SC#4 (OQ-01 resolved), and D-13 (conformance test stub for commitPlanningState). Provides the Phase 7 conformance foundation for write-side paired tests.

Output: 3 new conformance test files, updated DECISIONS.md with OQ-01 ADR, leak-grep verification pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-wire-core-write-methods-recordstateevent/03-CONTEXT.md
@.planning/phases/03-wire-core-write-methods-recordstateevent/03-RESEARCH.md
@.planning/phases/03-wire-core-write-methods-recordstateevent/03-01-SUMMARY.md
@.planning/phases/03-wire-core-write-methods-recordstateevent/03-02-SUMMARY.md
@.planning/phases/03-wire-core-write-methods-recordstateevent/03-03-SUMMARY.md
@.planning/phases/03-wire-core-write-methods-recordstateevent/03-04-SUMMARY.md

<interfaces>
From tests/conformance/phase-reads.test.ts (test file structure pattern):
```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { mkdtemp, rm, mkdir } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { MarkdownAdapter } from '../../adapters/markdown/index.js';
import type { StorageAdapter } from '../../adapters/types.js';

describe('...', () => {
  let tmpDir: string;
  let adapter: StorageAdapter;

  beforeEach(async () => {
    tmpDir = await mkdtemp(join(tmpdir(), 'gsd-write-'));
    await mkdir(join(tmpDir, '.planning'), { recursive: true });
    adapter = new MarkdownAdapter(tmpDir);
  });

  afterEach(async () => {
    await rm(tmpDir, { recursive: true, force: true });
  });
});
```

From sdk/src/query/state-event-types.ts:
```typescript
export type AppendEvent = { type: 'decision'; payload: DecisionPayload } | ...;
export type MutationEvent = { type: 'blocker_added'; payload: BlockerAddedPayload } | ...;
export type SignalEvent = { type: 'waiting'; payload: WaitingPayload } | { type: 'resume'; payload: ResumePayload };
```

From adapters/types.ts:
```typescript
recordStateAppend(event: AppendEvent): Promise<void>;
recordStateMutation(event: MutationEvent): Promise<void>;
recordStateSignal(event: SignalEvent): Promise<void>;
withTransaction<T>(fn: () => Promise<T>): Promise<T>;
commitPlanningState(message: string, files?: string[]): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write conformance tests for event families + withTransaction + commitPlanningState</name>
  <files>tests/conformance/write-events.test.ts, tests/conformance/write-transaction.test.ts, tests/conformance/commit-planning-state.test.ts</files>
  <read_first>
    - tests/conformance/phase-reads.test.ts (for test file structure pattern)
    - tests/conformance/adapter.conformance.ts (for conformance harness pattern)
    - adapters/markdown/index.ts (recordStateAppend/Mutation/Signal implementations)
    - sdk/src/query/state-event-types.ts (event payload shapes)
  </read_first>
  <action>
**Create `tests/conformance/write-events.test.ts`:**

```typescript
/**
 * Phase 3 Plan 05 — Conformance tests for recordStateAppend/Mutation/Signal.
 * Verifies event family round-trips produce correct STATE.md content.
 */
```

Test structure:
- `describe('recordStateAppend', () => { ... })` — test each event type:
  - `it('appends decision entry to Decisions section')` — seed STATE.md with `## Decisions\n\n(none)\n`. Call `adapter.recordStateAppend({ type: 'decision', payload: { phase: '03', summary: 'Test decision', rationale: 'Test rationale' } })`. Read back STATE.md. Assert contains `- [Phase 03]: Test decision — Test rationale`.
  - `it('appends metric row to Performance Metrics table')` — seed STATE.md with metrics table header. Call with metric payload. Assert new row appears.
  - `it('appends roadmap evolution entry')` — seed STATE.md with `## Roadmap Evolution` section. Call with roadmap_evolution payload. Assert entry appears with date and action.
  - `it('updates session continuity section')` — seed STATE.md with `## Session Continuity` section. Call with session payload. Assert `Last session:` and `Stopped at:` lines updated.
  - `it('creates section if missing for quick_task')` — seed STATE.md WITHOUT a Quick Tasks section. Call with quick_task payload. Assert section created with entry.

- `describe('recordStateMutation', () => { ... })`:
  - `it('adds blocker to Blockers section')` — seed STATE.md with `## Blockers` section. Call `recordStateMutation({ type: 'blocker_added', payload: { text: 'Need API key' } })`. Assert `- [ ] Need API key` appears.
  - `it('resolves blocker (marks complete)')` — seed with existing `- [ ] Need API key`. Call `recordStateMutation({ type: 'blocker_resolved', payload: { text: 'Need API key' } })`. Assert `- [x] Need API key` or moved to resolved section.
  - `it('updates todo count')` — seed STATE.md with `## Pending todos` section. Call with todo_count_update payload. Assert count updated.

- `describe('recordStateSignal', () => { ... })`:
  - `it('creates WAITING.json on waiting signal')` — call `recordStateSignal({ type: 'waiting', payload: { waitType: 'checkpoint', question: 'Looks good?' } })`. Assert `adapter.getRecord('WAITING.json')` returns valid JSON with `status: 'waiting'`.
  - `it('removes WAITING.json on resume signal')` — seed WAITING.json via `adapter.putRecord`. Call `recordStateSignal({ type: 'resume', payload: {} })`. Assert `adapter.getRecord('WAITING.json')` returns null.

Each test:
- Uses `beforeEach` to create tmpDir + `.planning/` + seed STATE.md via `adapter.putRecord`
- Asserts round-trip correctness by reading back via `adapter.getRecord`

**Create `tests/conformance/write-transaction.test.ts`:**

```typescript
/**
 * Phase 3 Plan 05 — Conformance test for withTransaction atomicity.
 */
```

Tests:
- `it('withTransaction executes function and mutations persist')` — write within transaction, verify content persists after transaction completes.
- `it('withTransaction returns value from callback')` — `const result = await adapter.withTransaction(async () => { return 42; }); expect(result).toBe(42);`
- `it('withTransaction releases lock on success')` — after successful transaction, a second transaction succeeds immediately (doesn't timeout).
- `it('withTransaction releases lock on error')` — throw inside transaction callback. Verify lock is released (second transaction succeeds). Verify the error propagates.
- `it('concurrent transactions serialize (second waits for first)')` — start two transactions. Verify they don't interleave (both complete, output is consistent).

**Create `tests/conformance/commit-planning-state.test.ts`:**

```typescript
/**
 * Phase 3 Plan 05 — Conformance test stub for commitPlanningState (D-13).
 * Phase 7 fills in the full paired test with BeadsAdapter.
 */
```

Tests:
- `it('commitPlanningState creates a git commit (MarkdownAdapter)')` — Initialize a git repo in tmpDir (`git init`). Create STATE.md. Call `adapter.commitPlanningState('test checkpoint')`. Verify `git log --oneline` shows the commit with the message.
- `it('commitPlanningState with specific files only stages those files')` — Create multiple files. Call `commitPlanningState('selective', ['STATE.md'])`. Verify only STATE.md is in the commit.
- `it.todo('commitPlanningState creates identifiable save point on BeadsAdapter')` — stub for Phase 7.

NOTE: The git-based tests need `git init` in beforeEach and may need to configure git user.name/user.email. Use `execSync('git init && git config user.email "test@test.com" && git config user.name "Test"', { cwd: tmpDir })` in beforeEach.
  </action>
  <verify>
    <automated>cd /Volumes/code/get-shit-done && npx vitest run --config vitest.conformance.config.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/conformance/write-events.test.ts contains `describe('recordStateAppend'`
    - tests/conformance/write-events.test.ts contains `describe('recordStateMutation'`
    - tests/conformance/write-events.test.ts contains `describe('recordStateSignal'`
    - tests/conformance/write-events.test.ts contains at least 8 `it(` test cases
    - tests/conformance/write-transaction.test.ts contains `describe(` with at least 4 `it(` test cases
    - tests/conformance/write-transaction.test.ts contains `adapter.withTransaction`
    - tests/conformance/commit-planning-state.test.ts contains `commitPlanningState`
    - tests/conformance/commit-planning-state.test.ts contains `it.todo(` (Phase 7 stub)
    - All conformance tests pass: `npx vitest run --config vitest.conformance.config.ts` exits 0
  </acceptance_criteria>
  <done>Conformance tests written and passing for all 3 event families (round-trip verification), withTransaction atomicity (5 scenarios), and commitPlanningState checkpoint (2 tests + 1 Phase 7 stub). Foundation ready for Phase 7 paired tests.</done>
</task>

<task type="auto">
  <name>Task 2: Document OQ-01 resolution ADR in DECISIONS.md + run final leak-grep verification</name>
  <files>.planning/DECISIONS.md</files>
  <read_first>
    - .planning/DECISIONS.md (current content for ADR format and numbering)
    - .planning/phases/03-wire-core-write-methods-recordstateevent/03-CONTEXT.md (D-11, D-12 for OQ-01 resolution details)
  </read_first>
  <action>
**Append two ADRs to `.planning/DECISIONS.md`:**

1. **OQ-01 Resolution ADR:**
```markdown
### D-2026-05-XX-01: OQ-01 Resolution — commitPlanningState semantics (Phase 3)

**Question:** What does `commitPlanningState` mean for non-git backends?

**Decision:** Every adapter MUST implement `commitPlanningState` as a meaningful
save point. No no-op allowed.

**Semantics per adapter:**
- MarkdownAdapter: `git add` + `git commit` (current behavior, unchanged)
- BeadsAdapter (Phase 6): bead-hash bookmark (record current state hash as a
  named restore point; leverages bd's immutable content-addressed architecture)
- Hypothetical SQLiteAdapter: WAL checkpoint + metadata row

**Rationale:** The "always checkpoint" philosophy ensures that every workflow
step that calls `commitPlanningState` produces an identifiable restore point
regardless of backend. This enables `snapshot()/restore()` in Phase 5 to have
a meaningful target on all adapters.

**Impact:** `commitPlanningState` removed from `Capabilities` enum. All
adapters must implement it. `hasCommitPlanningState` type guard removed.
Call sites simplified (no capability check needed).

**Resolves:** OQ-01 (from SYNTHESIS.md §6 #1)
```

2. **commitPlanningState Promotion ADR:**
```markdown
### D-2026-05-XX-02: commitPlanningState promoted to required method (Phase 3)

**Previous state:** `commitPlanningState` was capability-gated
(`Capabilities.commitPlanningState: boolean`) with a companion type guard
`hasCommitPlanningState()`. Call sites checked the capability before calling.

**New state:** Required method on `StorageAdapter` (like `getRecord`,
`putRecord`). The `commitPlanningState: boolean` field removed from
`Capabilities` interface. The `hasCommitPlanningState` type guard removed.
Call sites call directly without capability check.

**Rationale:** Per OQ-01 resolution (D-2026-05-XX-01), every adapter must
provide meaningful save points. Making it required eliminates dead capability-
check code and ensures adapters fail at compile time (TypeScript) rather than
runtime if they omit the implementation.

**Affected files:** `adapters/types.ts` (interface change),
`adapters/markdown/index.ts` (capabilities field removed), all call sites
that previously used `hasCommitPlanningState()` (simplified to direct call).
```

Use today's date (2026-05-10) for the `XX` placeholder.

**Run final leak-grep verification:**
After writing the ADRs, verify the Phase 3 migration is clean by running:
```bash
node scripts/leak-grep.cjs sdk/src/query/state-mutation.ts sdk/src/query/phase-lifecycle.ts
```

This should return 0 matches for write-side patterns scoped to `.planning/` paths. If any matches remain, they should only be in deprecated function bodies (which is acceptable — document in the summary). The actual verification command is part of the `<verify>` block.
  </action>
  <verify>
    <automated>cd /Volumes/code/get-shit-done && grep -c "OQ-01" .planning/DECISIONS.md && node scripts/leak-grep.cjs sdk/src/query/state-mutation.ts sdk/src/query/phase-lifecycle.ts 2>&1; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/DECISIONS.md contains `OQ-01 Resolution`
    - .planning/DECISIONS.md contains `commitPlanningState promoted to required`
    - .planning/DECISIONS.md contains `Every adapter MUST implement commitPlanningState as a meaningful save point`
    - .planning/DECISIONS.md contains `bead-hash bookmark`
    - `node scripts/leak-grep.cjs sdk/src/query/state-mutation.ts sdk/src/query/phase-lifecycle.ts` exits 0 (or only flags deprecated function bodies which are documented exceptions)
    - `grep -c "OQ-01" .planning/DECISIONS.md` returns >= 1
  </acceptance_criteria>
  <done>OQ-01 resolution documented in DECISIONS.md (always-checkpoint semantics, per-adapter mapping). commitPlanningState promotion ADR recorded. Leak-grep verification confirms clean write surface in state-mutation.ts and phase-lifecycle.ts. WRITES-04 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test tmpDir | Conformance tests use isolated tmpdir; no cross-test contamination |
| git operations in test | commitPlanningState test initializes a fresh git repo per test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Information Disclosure | test tmpDir cleanup | mitigate | afterEach removes tmpDir with `rm -rf`; no sensitive data persists after test |
| T-03-14 | Tampering | git commit in test | accept | Tests run in isolated tmpdir with disposable git config; no real repo affected |
</threat_model>

<verification>
- `npx vitest run --config vitest.conformance.config.ts` exits 0 (all conformance tests pass)
- `grep -c "OQ-01" .planning/DECISIONS.md` >= 1
- `node scripts/leak-grep.cjs sdk/src/query/state-mutation.ts sdk/src/query/phase-lifecycle.ts` exits 0
- Phase 3 SC#1 (zero write-side fs matches), SC#2 (one call shape), SC#4 (OQ-01 resolved) all verified
</verification>

<success_criteria>
- All conformance tests pass (event round-trips + transaction atomicity + commitPlanningState)
- OQ-01 resolved and documented in DECISIONS.md
- Leak-grep returns 0 matches for the SDK write surface
- Phase 7 has stubs to fill (it.todo for BeadsAdapter paired tests)
- All 4 WRITES requirements addressed across Plans 01-05
</success_criteria>

<output>
After completion, create `.planning/phases/03-wire-core-write-methods-recordstateevent/03-05-SUMMARY.md`
</output>
