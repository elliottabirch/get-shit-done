---
phase: 05-foundational-primitive-lift
plan: 07
type: execute
wave: 7
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06"]
files_modified:
  - .planning/DECISIONS.md
autonomous: false
requirements: [PRIMITIVES-07, PRIMITIVES-06, PRIMITIVES-08, PRIMITIVES-09]
must_haves:
  truths:
    - "ADR for OQ-02 (section-vs-whole-file write granularity) appended to .planning/DECISIONS.md (SC#5)"
    - "ADR for OQ-05 (sidecar paths kv-vs-named) appended to .planning/DECISIONS.md (SC#5)"
    - "ADR for OQ-07 (scratch record taxonomy) appended to .planning/DECISIONS.md (SC#5)"
    - "ADR for OQ-10 (multi-author file concurrency) appended to .planning/DECISIONS.md (SC#5)"
    - "Phase 5 ADR for shadow-dir journal upgrade appended (CONTEXT §canonical_refs - DECISIONS.md § 'Phase 5 appends ADRs for OQ-02, OQ-05, OQ-07, OQ-10 and for the shadow-dir journal upgrade')"
    - "Full Phase-5 test suite (conformance + SDK) passes green end-to-end"
    - "Grep-zero gates all hold: legacy kind-tagged names (D-15), raw .next-call-count access (D-21), obsolete pipeline helpers (collectFiles, copyPlanningTree, readPlanningState)"
  artifacts:
    - path: ".planning/DECISIONS.md"
      provides: "5 new ADRs documenting OQ-02/05/07/10 resolutions + shadow-dir journal upgrade"
      contains: "OQ-02"
  key_links:
    - from: ".planning/DECISIONS.md"
      to: "CONTEXT.md D-01..D-21 (resolutions cross-referenced)"
      via: "inline ADR text"
      pattern: "D-0[12][0-9]"
---

<objective>
Close out Phase 5 by appending the required ADRs to
`.planning/DECISIONS.md` resolving OQ-02, OQ-05, OQ-07, OQ-10 (SC#5) plus
a fifth ADR documenting the shadow-dir journal upgrade to
`withTransaction`. Run the full Phase-5 verification suite to confirm all
acceptance gates hold end-to-end.

This plan is a `checkpoint:human-verify` wrapper around two concrete
deliverables:
1. Write the 5 ADRs (auto — Claude drafts text, commits).
2. Run the full verification matrix and present results for user approval
   before marking Phase 5 complete.

Does NOT modify any source code. Does NOT change any test. Does NOT
modify ROADMAP.md or STATE.md — those are handled by
`/gsd-verify-work` / `/gsd-progress` after user approval.

Purpose: resolve the four SYNTHESIS §6 open questions (OQ-02, OQ-05,
OQ-07, OQ-10) with written decisions (SC#5) and gate Phase 5 exit on a
clean green-test + grep-zero state. Unblocks Phase 6 (BeadsAdapter) per
§9 HIGH-severity dry-run gate.
Output: ~5 ADRs appended to DECISIONS.md (~200 LOC); verification
output surfaced as a human-review checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DECISIONS.md
@.planning/phases/05-foundational-primitive-lift/05-CONTEXT.md
@.planning/phases/05-foundational-primitive-lift/05-RESEARCH.md
@.planning/phases/05-foundational-primitive-lift/05-PATTERNS.md
@.planning/phases/05-foundational-primitive-lift/05-VALIDATION.md
@.planning/phases/05-foundational-primitive-lift/05-01-SUMMARY.md
@.planning/phases/05-foundational-primitive-lift/05-02-SUMMARY.md
@.planning/phases/05-foundational-primitive-lift/05-03-SUMMARY.md
@.planning/phases/05-foundational-primitive-lift/05-04-SUMMARY.md
@.planning/phases/05-foundational-primitive-lift/05-05-SUMMARY.md
@.planning/phases/05-foundational-primitive-lift/05-06-SUMMARY.md

<interfaces>
DECISIONS.md format (per existing entries — D-2026-04-30-01 at line 14 onward):

    ## D-2026-05-XX-NN — <short title>

    **Date:** YYYY-MM-DD
    **Trigger:** <what surfaced this decision>
    **Decision:** <the chosen resolution in plain English>

    **Alternatives considered:**
    1. **<alt 1 name>** — <why rejected>
    2. **<alt 2 name>** — <why rejected>

    **Evidence:** <pointer to research/context/pattern files>

    **Implication:** <downstream effect>

New ADR numbering: append chronologically after the last existing D-2026-* entry. Phase 5 ADRs use the form `D-2026-05-NN` where NN is the next integer after the latest existing entry for that month (if any). Planner uses NN=01..05 for the five Phase-5 ADRs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 5 ADRs to .planning/DECISIONS.md (OQ-02, OQ-05, OQ-07, OQ-10 + shadow-dir journal)</name>
  <files>.planning/DECISIONS.md</files>
  <read_first>
    - .planning/DECISIONS.md (full file — understand existing format and numbering)
    - .planning/phases/05-foundational-primitive-lift/05-CONTEXT.md §Decisions D-06/D-09/D-10/D-12/D-19/D-20 (resolutions source material)
    - .planning/phases/05-foundational-primitive-lift/05-RESEARCH.md §Pattern 1 (shadow-dir journal), §Open Questions
    - .planning/phases/05-foundational-primitive-lift/05-03-SUMMARY.md (shadow-dir journal implementation facts)
  </read_first>
  <action>
Read the tail of `.planning/DECISIONS.md` and determine the next ADR identifier. Append the 5 ADRs below, in order, using `---` separators between entries (following existing file convention).

**ADR 1 — OQ-02 resolution (section-vs-whole-file write granularity):**

    ---

    ## D-2026-05-01 — OQ-02: section is the unit of write atomicity (L2/L3/L4 depth-walker)

    **Date:** 2026-05-10
    **Trigger:** SYNTHESIS §6 OQ-02 ("Section-scoped vs whole-file write granularity") requires a locked answer before Phase 6 BeadsAdapter maps updateSection to bd sub-records.

    **Decision:** The SECTION is the unit of write atomicity for canonical
    files (ROADMAP.md, STATE.md, PROJECT.md, AI-SPEC.md, SPEC.md, UAT.md,
    VERIFICATION.md, debug-session docs). `updateSection(path, anchor, body,
    mode)` is the ONLY primitive callers use for per-section writes; it
    supports L2 (`"## Foo"`), L3 (`"### Evidence"`), and L4 (`"#### Sub-point"`)
    anchors via the heading-depth walker shipped in Phase 5 Plan 02. A
    section terminates at the next heading of same-or-shallower depth.
    Whole-file writes (`putRecord`) remain available for one-shot document
    creation or for files outside the section-addressable catalog.

    **Alternatives considered:**
    1. **Whole-file only** (pre-Phase-5 baseline, putRecord for everything) —
       Rejected: forces multi-author workflows to read-modify-write the
       whole file, surfacing the §9 HIGH-severity race observed in the
       AI-SPEC three-author scenario. BeadsAdapter would have to treat
       every file as a single bd record, collapsing issue/sub-record
       semantics.
    2. **L2-only sections** (Phase 1 scope) — Rejected: §4 row 527 targets
       (`### Evidence`, `### Threat Flags`, nested PLAN sections) force
       awkward whole-parent rewrites, re-introducing the race.
    3. **L2+L3 only** — Rejected: nested PLAN artifacts use L4 (`#### ...`)
       for fine-grained evidence points; capping at L3 re-introduces
       multi-author contention on those subsections.
    4. **Arbitrary depth (L1-L6)** — Accepted at L2-L4; L5-L6 available
       via the walker but not surfaced in the SDK handler contract
       until a consuming workflow requires them (deferred until needed).

    **Evidence:** 05-CONTEXT.md §D-06; 05-RESEARCH.md §Pattern 2; Plan 02 shipped
    the depth walker; `tests/conformance/section-depth.test.ts` exercises
    L2/L3/L4 + fenced-code + HTML-comment edge cases + setext warning.

    **Implication:** BeadsAdapter (Phase 6) maps `updateSection` to per-depth
    bd sub-record updates. Conformance suite (Phase 7) parameterizes over
    both adapters asserting equivalent atomic-section semantics per
    (record-type, section-id) tuple.

**ADR 2 — OQ-05 resolution (sidecar paths):**

    ---

    ## D-2026-05-02 — OQ-05: sidecar paths are SDK-typed verbs, adapter stays Bin A

    **Date:** 2026-05-10
    **Trigger:** SYNTHESIS §6 OQ-05 ("Sidecar paths kv-vs-named") must resolve before BeadsAdapter lands: does the adapter expose a generic kv interface for `.planning/.next-call-count`-style files, or does each sidecar get a dedicated named method?

    **Decision:** Sidecar paths get dedicated **SDK verbs** — e.g.
    `next-call-count.get`, `next-call-count.incr` — defined in
    `sdk/src/query/sidecar.ts` (new module in Phase 5 Plan 05). The adapter
    surface stays Bin A: handlers internally call `adapter.getRecord`/
    `putRecord` against known paths centralized in the sidecar module.
    BeadsAdapter path-sniffs a small documented set of 2-3 sidecar keys
    (tracked in BeadsAdapter README, Phase 6).

    **Alternatives considered:**
    1. **Generic `adapter.getSidecar(key)` / `putSidecar(key, value)` methods** —
       Rejected: adds two methods to the adapter interface for a closed set
       of 2-3 keys; pushes domain concern (which keys exist, what their
       lifecycle is) into the adapter. Phase 3 D-04 principle "adapter
       stays thin" disfavors.
    2. **Raw `adapter.getRecord('.next-call-count')` scattered across SDK** —
       Rejected: no single source of truth for sidecar path literals; any
       future move of `.next-call-count` would require N call-site edits.
       Also forces BeadsAdapter to recognize magic path strings at the
       Bin A level.
    3. **Hybrid: generic kv interface PLUS per-key helpers** — Rejected:
       redundant; the helpers alone are sufficient and keep the adapter
       surface frozen for Phase 6.

    **Evidence:** 05-CONTEXT.md §D-19; 05-RESEARCH.md §Pattern/Don't Hand-Roll
    row "Sidecar/scratch as adapter primitives"; Plan 05 shipped
    `sdk/src/query/sidecar.ts` with `nextCallCountGet`/`nextCallCountIncr`;
    Plan 06 migrated `route-next-action.ts:44` to the helper (D-21).

    **Implication:** Adapter `Capabilities` interface gains NO `sidecar`
    member; the flag surface stays at its Phase-1 shape. BeadsAdapter README
    documents its path-sniff map for the known sidecar keys.

**ADR 3 — OQ-07 resolution (scratch taxonomy):**

    ---

    ## D-2026-05-03 — OQ-07: scratch artifacts are first-class SDK verbs at phase-scoped paths

    **Date:** 2026-05-10
    **Trigger:** SYNTHESIS §6 OQ-07 ("Scratch record taxonomy") — the
    `*-DISCUSS-CHECKPOINT.json` and `*-QUESTIONS.json/.html` artifacts were
    written by `/gsd-discuss-phase` and similar workflows via raw fs
    operations prior to Phase 4. Phase 4 plugged the leaks but deferred the
    taxonomy decision here.

    **Decision:** `*-DISCUSS-CHECKPOINT.json` and `*-QUESTIONS.json/.html`
    are FIRST-CLASS SDK verbs with dedicated put/get/delete operations:
    `discuss.checkpoint.put/get/delete` and `discuss.questions.put/get/delete`
    (shipped in Phase 5 Plan 05 via `sdk/src/query/scratch.ts`). They live
    at `.planning/phases/<phaseDir>/` (NOT under `.planning/tmp/`) because
    they are phase-scoped, not tmp-lifecycle. Lifecycle (create mid-workflow,
    read on resume, delete on commit) is orchestrated by the workflow layer
    (`/gsd-discuss-phase` and friends). Adapter only needs getRecord /
    putRecord / removeRecord — no new adapter methods added.

    **Alternatives considered:**
    1. **Leave in `.planning/tmp/` with generic `tmp.put/get`** — Rejected:
       scratch files outlive a single workflow invocation (they persist
       across sessions until the phase commits), so tmp-lifecycle
       semantics are wrong.
    2. **Co-locate with phase artifacts but use raw `tmp.put/get` verb** —
       Rejected: conflates "scratch" as lifecycle with "scratch" as
       category; handlers would need to special-case the path pattern.
    3. **New adapter methods `putScratch(phase, name, body)` / `getScratch`** —
       Rejected: the adapter primitive-surface already covers this via
       putRecord; scratch is a workflow concern (lifecycle), not an
       adapter concern. D-04 from Phase 3 ("adapter stays thin") rules.

    **Evidence:** 05-CONTEXT.md §D-20; 05-PATTERNS.md §sdk/src/query/scratch.ts;
    Plan 05 shipped scratch.ts with 6 handlers + validators; Plan 05 test
    `sdk/src/query/scratch.test.ts` exercises round-trip, delete-on-missing
    (no-op), invalid format rejection, path-traversal guard.

    **Implication:** Workflows (`/gsd-discuss-phase`, others) are expected
    to use `gsd-sdk query discuss.checkpoint.put ...` instead of raw file
    writes. BeadsAdapter maps scratch verbs to typed bd comments with a
    documented `gsd:scratch:checkpoint` label (Phase 6).

**ADR 4 — OQ-10 resolution (multi-author concurrency):**

    ---

    ## D-2026-05-04 — OQ-10: multi-author concurrency via internally-wrapped updateSection

    **Date:** 2026-05-10
    **Trigger:** SYNTHESIS §9 HIGH-severity risk "Multi-author files imply
    concurrency" concretized in the AI-SPEC three-author scenario — three
    subagents (`gsd-domain-researcher`, `gsd-ai-researcher`,
    `gsd-eval-planner`) write three different L2 sections of one AI-SPEC.md
    concurrently. Without serialization, the later writes lose earlier
    writes' content.

    **Decision:** `updateSection` internally wraps `withTransaction` —
    every call acquires the PID lockfile via the Phase-3 lock + shadow-dir
    infrastructure, making parallel callers serialize at the adapter layer
    with zero caller burden. The reentrant-lock guard (D-10) prevents
    deadlock when outer callers (e.g. `recordStateAppend`) already hold
    the lock and internally trigger updateSection. The AI-SPEC three-author
    workflow is safe under arbitrary reordering and interleaving — no
    workflow-layer sequencing required.

    **Alternatives considered:**
    1. **Workflow-layer lock-step sequencing** — Rejected: pushes concurrency
       concern to every call site; requires discipline; fails in the face
       of refactors that change spawn order.
    2. **Per-section lockfiles** — Rejected: N lockfiles per file; complex
       stale-lock recovery; no equivalent mapping to bd native txn.
    3. **Optimistic concurrency (read-then-CAS)** — Rejected: requires a
       retry loop at the caller and a version field on every record;
       incompatible with text-oriented markdown files; brittle.
    4. **updateSection internally wrapped (chosen)** — Accepted: D-09 from
       CONTEXT; ~10 LOC reentrant-lock guard (D-10); AI-SPEC three-author
       test (`tests/conformance/section-depth.test.ts`) passes under
       arbitrary reordering.

    **Evidence:** 05-CONTEXT.md §D-09/D-10; 05-RESEARCH.md §Pattern 4;
    `tests/conformance/section-depth.test.ts:three-author concurrency`;
    `tests/conformance/write-transaction.test.ts:reentrant` + `:updateSection concurrency`.

    **Implication:** BeadsAdapter (Phase 6) maps updateSection to
    per-section sub-record edits inheriting bd's per-issue atomicity — the
    "atomic updateSection" contract holds across both adapters via
    different mechanisms (MarkdownAdapter: file-level PID lock;
    BeadsAdapter: bd issue-edit atomicity). Conformance tests (Phase 7)
    exercise three-author-style concurrency against both backends.

**ADR 5 — Shadow-dir journal upgrade:**

    ---

    ## D-2026-05-05 — withTransaction upgraded to shadow-dir journal with rollback

    **Date:** 2026-05-10
    **Trigger:** SYNTHESIS §9 HIGH-severity risk "Dry-run hoist non-trivial.
    Don't ship Phase 6 until dry-run is stable on MarkdownAdapter." The
    Phase-3 `withTransaction` was lock-only and had no rollback capability;
    `pipeline.ts` dry-run used `cp -r` of the whole `.planning/` tree,
    which is an adapter-bypass that BeadsAdapter cannot replicate.

    **Decision:** `withTransaction(fn, opts?: { dryRun?: boolean })` is
    upgraded to a shadow-dir journal. When a txn is active, all mutating
    adapter methods redirect writes to a per-txn tmpdir at
    `.planning/.tmp-txn-<uuid>/` (same-mount to guarantee POSIX `rename(2)`
    atomicity at commit). Reads merge tmpdir-over-real so callers see their
    own pending writes. Commit renames shadow entries into place; rollback
    `rm -rf`'s the tmpdir. Nested calls JOIN the outer txn via the
    reentrant-lock guard. `dryRun: true` unconditionally rolls back and
    makes `commitPlanningState` a no-op inside the txn.

    **Alternatives considered:**
    1. **Full tmpdir dir-swap at commit** — Rejected: requires moving the
       original `.planning/` aside first, creating a window where
       `.planning/` doesn't exist and breaking concurrent readers. Per-file
       rename on touched paths only is safer and enables incremental commits.
    2. **Shadow under `os.tmpdir()`** — Rejected: may be on a different
       filesystem, triggering EXDEV on rename and falling back to
       copy+unlink (NOT atomic). Same-mount under `.planning/.tmp-txn-*/`
       is the only safe location.
    3. **Keep `cp -r` with adapter-bypass** — Rejected: BeadsAdapter cannot
       `cp -r` a bd store; the dry-run mechanism must live at the adapter
       capability layer, not the filesystem.
    4. **SQLite WAL-style write-ahead log** — Rejected: heavier machinery
       than the 2-3 file mutations a typical GSD transaction touches;
       SQLite's WAL-per-page model doesn't map to file-per-record.

    **Evidence:** 05-CONTEXT.md §D-01/D-02/D-03/D-04/D-05; 05-RESEARCH.md
    §Pattern 1, §Pitfalls 1/2/4; Plan 03 implementation;
    `tests/conformance/write-transaction.test.ts` new cases (dryRun rollback,
    mid-txn failure byte-identical, reentrancy, snapshot/restore, nested
    dryRun propagation); Plan 05 SC#1 pipeline test.

    **Implication:** Pipeline dry-run is now adapter-capability-backed
    (SC#1 satisfied — mid-txn failure leaves `.planning/` byte-identical
    to pre-call). Unblocks Phase 6 BeadsAdapter per §9 dry-run gate.
    `capabilities.snapshot: true` on MarkdownAdapter.
    `commitPlanningState` no-op inside dryRun tracked as OQ-01 follow-up
    (a separate documentation ADR will land in Phase 3's OQ-01 follow-up
    work or at Phase 6 start; not blocking Phase 5).

After appending, commit the file:
```bash
cd /Volumes/code/get-shit-done && git add .planning/DECISIONS.md && git commit -m "docs(05): ADRs for OQ-02/05/07/10 + shadow-dir journal"
```
(Use a commit message consistent with project convention — check recent commits for the exact format if in doubt.)
  </action>
  <verify>
    <automated>grep -cE "^## D-2026-05-0[1-5]" /Volumes/code/get-shit-done/.planning/DECISIONS.md | grep -q "^5$" && grep -cE "OQ-02|OQ-05|OQ-07|OQ-10|shadow-dir journal" /Volumes/code/get-shit-done/.planning/DECISIONS.md | awk '$1 >= 5 {print "OK"}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## D-2026-05-01 —" .planning/DECISIONS.md` returns 1
    - `grep -c "^## D-2026-05-02 —" .planning/DECISIONS.md` returns 1
    - `grep -c "^## D-2026-05-03 —" .planning/DECISIONS.md` returns 1
    - `grep -c "^## D-2026-05-04 —" .planning/DECISIONS.md` returns 1
    - `grep -c "^## D-2026-05-05 —" .planning/DECISIONS.md` returns 1
    - `grep -c "OQ-02" .planning/DECISIONS.md` >= 1
    - `grep -c "OQ-05" .planning/DECISIONS.md` >= 1
    - `grep -c "OQ-07" .planning/DECISIONS.md` >= 1
    - `grep -c "OQ-10" .planning/DECISIONS.md` >= 1
    - `grep -c "shadow-dir journal" .planning/DECISIONS.md` >= 1
    - Each ADR contains `**Alternatives considered:**` with at least 2 numbered alternatives
    - Each ADR contains `**Evidence:**` section with pointers to 05-CONTEXT.md / 05-RESEARCH.md / Plan file / test file
    - Each ADR contains `**Implication:**` section
    - git commit succeeded (next HEAD has DECISIONS.md change)
  </acceptance_criteria>
  <done>
5 ADRs appended to .planning/DECISIONS.md resolving OQ-02, OQ-05, OQ-07,
OQ-10, and the shadow-dir journal upgrade. Each ADR follows the existing
file format (Date / Trigger / Decision / Alternatives / Evidence /
Implication). Changes committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify Phase 5 exit — full verification matrix + SC#1..5 + grep-zero gates</name>
  <action>Automate nothing further — this task is the Phase 5 exit gate. Execute each verification command from `<how-to-verify>` below, capture stdout verbatim, paste the outputs in your response, and wait for the user to reply "approved" or describe failures. Do NOT mark Phase 5 complete, do NOT update STATE.md or ROADMAP.md, do NOT run `/gsd-verify-work` — those run only after explicit user approval here.</action>
  <what-built>
Phase 5 complete — 6 implementation plans shipped across 6 waves:
  - Plan 01: NamedDocCategory + RootNamedDocKey + 5 Wave-0 test scaffolds
  - Plan 02: Heading-depth walker (L2/L3/L4) + fenced-code + HTML-comment + setext warning
  - Plan 03: Shadow-dir journal withTransaction + snapshot/restore + reentrant-lock + capabilities.snapshot=true
  - Plan 04: putNamedDoc/getNamedDoc/writeBinaryAsset real bodies + capabilities.namedDoc/binaryAsset=true
  - Plan 05: pipeline.ts dry-run via withTransaction + sidecar.ts + scratch.ts + 8 new SDK verbs
  - Plan 06: 8 SDK handler migrations to putNamedDoc/getNamedDoc + route-next-action.ts nextCallCountGet migration
  - Plan 07 (this): 5 ADRs for OQ-02/05/07/10 + shadow-dir journal

Five Success Criteria hold:
  - SC#1 (dry-run byte-identical mid-txn failure) — Plan 03 conformance + Plan 05 pipeline test
  - SC#2 (AI-SPEC three-author concurrency) — Plan 03 section-depth test
  - SC#3 (putNamedDoc grep-zero for legacy names) — Plan 04 named-doc.test.ts regression guard
  - SC#4 (binaryAsset capability + graceful degradation) — Plan 04 binary-asset.test.ts
  - SC#5 (ADRs for OQ-02/05/07/10 in DECISIONS.md) — Plan 07 Task 1
  </what-built>
  <how-to-verify>
Run each of these commands from the repo root and verify the expected output:

**1. Full SDK test suite green:**
```bash
cd sdk && npm test
```
Expected: 0 failing tests; the total passing count should equal the pre-Phase-5 baseline PLUS the new tests from Plans 01-05 (sidecar.test.ts, scratch.test.ts, extended pipeline.test.ts).

**2. Full conformance suite green:**
```bash
npx vitest run --config vitest.conformance.config.ts
```
Expected: 0 failing; new suites `section-depth.test.ts`, `named-doc.test.ts`, `binary-asset.test.ts` all pass; extended `write-transaction.test.ts` includes reentrant + dryRun + mid-txn-failure + snapshot/restore cases all passing.

**3. SC#1 byte-identity explicit check:**
```bash
cd sdk && npx vitest run src/query/pipeline.test.ts -t "mid-txn failure"
```
Expected: 1 passing test asserting `preHash === postHash`.

**4. SC#2 three-author concurrency explicit check:**
```bash
npx vitest run --config vitest.conformance.config.ts tests/conformance/section-depth.test.ts -t "three-author"
```
Expected: 1 passing test with all three section bodies present.

**5. SC#3 legacy name grep-zero:**
```bash
grep -rn --include='*.ts' --include='*.cjs' --exclude-dir=node_modules --exclude-dir=dist 'getResearch\|putIntelDoc\|putCodebaseDoc\|getArchivedMilestoneDoc' sdk/src adapters | grep -v 'named-doc.test.ts'
```
Expected: empty output (no matches outside the regression test file).

**6. SC#4 capability flags verification:**
```bash
grep -cE 'binaryAsset: true|namedDoc: true|snapshot: true|transaction: true' adapters/markdown/index.ts
```
Expected: 4 (all four flags true).

**7. SC#5 ADRs present:**
```bash
grep -cE '^## D-2026-05-0[1-5]' .planning/DECISIONS.md
```
Expected: 5.

**8. D-21 path-literal centralization:**
```bash
grep -rn "adapter.getRecord.*'\\.next-call-count'" sdk/src
```
Expected: empty output.

**9. Pipeline obsolete-helpers removed:**
```bash
grep -cE 'collectFiles|copyPlanningTree|readPlanningState' sdk/src/query/pipeline.ts
```
Expected: 0.

**10. Shadow-dir .gitignore entry:**
```bash
grep -cE '\.planning/\.tmp-txn-\*|\.planning/\.tmp-snap-\*' .gitignore
```
Expected: 2.

**11. No UnsupportedCapabilityError stubs for Phase-5 primitives:**
```bash
grep -cE "UnsupportedCapabilityError\('(binaryAsset|snapshot|namedDoc)'" adapters/markdown/index.ts
```
Expected: 0.

Paste the output of each command in your response when approving or blocking.
  </how-to-verify>
  <resume-signal>Reply with "approved" to mark Phase 5 complete, or describe specific failures with command outputs so we can route through `/gsd-plan-phase --gaps`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DECISIONS.md append | Human-readable ADR prose — no code execution surface. Verification checkpoint is the trust boundary for overall Phase 5 exit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Repudiation | ADR decisions lack evidence pointers — future reader can't audit | mitigate | Each ADR includes `**Evidence:**` section pointing to CONTEXT.md / RESEARCH.md / Plan file / test file. Acceptance criteria grep for these sections. |
| T-05-07-02 | Tampering | Verification checkpoint skipped — Phase 5 marked complete with silent failures | mitigate | `autonomous: false` on this plan; `checkpoint:human-verify` task with `gate="blocking"` requires explicit user approval with command output paste; no auto-advance. |
| T-05-07-03 | Information Disclosure | ADR text reveals internal architecture in a public repo | accept | Repo is already public; internal architecture is documented in PROJECT.md and SYNTHESIS.md; ADRs add no new surface. |
</threat_model>

<verification>
1. Task 1: `grep -cE "^## D-2026-05-0[1-5]" .planning/DECISIONS.md` returns 5 (all 5 ADRs present)
2. Task 1: each ADR has Evidence + Alternatives + Implication sections (structural check via grep)
3. Task 2: user pastes command outputs; all 11 commands return the expected value
4. Task 2: user replies "approved" or describes failures with specifics
</verification>

<success_criteria>
- 5 ADRs appended to .planning/DECISIONS.md (D-2026-05-01..05) resolving OQ-02, OQ-05, OQ-07, OQ-10 + shadow-dir journal upgrade
- Each ADR includes Trigger, Decision, 2+ Alternatives considered, Evidence, Implication sections
- Full SDK + conformance test suite green (0 failing)
- All 5 Success Criteria (SC#1..5) verified via explicit grep/test commands
- D-21 grep-zero: no raw `.next-call-count` getRecord calls outside sidecar.ts
- Obsolete pipeline helpers (collectFiles/copyPlanningTree/readPlanningState) absent
- `.gitignore` excludes shadow-tmp dirs
- No Phase-5 primitive stubs remain (snapshot/restore/namedDoc/binaryAsset all real)
- User explicitly approves Phase 5 exit via checkpoint reply
</success_criteria>

<output>
After user approval, create `.planning/phases/05-foundational-primitive-lift/05-07-SUMMARY.md`
with: list of 5 ADRs (IDs + one-line summary each), the 11 verification command
outputs from Task 2, final Phase 5 completion timestamp, and confirmation of
readiness for Phase 6 activation (per §9 dry-run gate: "Don't ship Phase 6
until dry-run is stable on MarkdownAdapter" — SC#1 passing satisfies this).
</output>
