---
phase: 05-foundational-primitive-lift
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - adapters/types.ts
  - adapters/types.test.ts
  - tests/conformance/section-depth.test.ts
  - tests/conformance/named-doc.test.ts
  - tests/conformance/binary-asset.test.ts
  - sdk/src/query/sidecar.test.ts
  - sdk/src/query/scratch.test.ts
autonomous: true
requirements: [PRIMITIVES-04]
must_haves:
  truths:
    - "NamedDocCategory typed union exists at adapters/types.ts with 8 members exactly (D-13)"
    - "RootNamedDocKey literal union exists with 3 keys exactly (D-14)"
    - "putNamedDoc/getNamedDoc use discriminated overloads — 'root' key is compile-time restricted to RootNamedDocKey (D-14)"
    - "All Wave 0 test files exist as failing scaffolds, enforcing Nyquist gate before implementation"
  artifacts:
    - path: "adapters/types.ts"
      provides: "NamedDocCategory, RootNamedDocKey exports + overload signatures"
      contains: "export type NamedDocCategory"
    - path: "tests/conformance/section-depth.test.ts"
      provides: "PRIMITIVES-01/02 test harness (scaffold; empty tests or skip)"
      contains: "describe"
    - path: "tests/conformance/named-doc.test.ts"
      provides: "PRIMITIVES-04 test harness"
      contains: "describe"
    - path: "tests/conformance/binary-asset.test.ts"
      provides: "PRIMITIVES-05 test harness"
      contains: "describe"
    - path: "sdk/src/query/sidecar.test.ts"
      provides: "PRIMITIVES-08 test harness"
      contains: "describe"
    - path: "sdk/src/query/scratch.test.ts"
      provides: "PRIMITIVES-09 test harness"
      contains: "describe"
  key_links:
    - from: "adapters/types.ts"
      to: "adapters/markdown/index.ts (Plan 04 consumer)"
      via: "import { NamedDocCategory, RootNamedDocKey } from '../types.js'"
      pattern: "NamedDocCategory"
    - from: "tests/conformance/*.test.ts"
      to: "adapters/markdown/index.ts"
      via: "new MarkdownAdapter(tmpDir)"
      pattern: "new MarkdownAdapter"
---

<objective>
Foundation plan: lock the `NamedDocCategory` + `RootNamedDocKey` typed unions
and the discriminated `putNamedDoc`/`getNamedDoc` overload signatures on the
`StorageAdapter` interface (D-13, D-14). Create all Wave 0 test scaffolds
(empty `describe` blocks with `it.todo` placeholders) so later plans have
Nyquist-compliant targets to run against. No behavior change — MarkdownAdapter
stubs still throw `UnsupportedCapabilityError` at this point; capability flags
remain `false`. This plan touches ONLY `adapters/types.ts` + its unit test +
new test files; zero changes to `adapters/markdown/index.ts` so Waves 2/3/4
can serialize their index.ts edits without conflict.

Purpose: establish the typed contract before implementation; gate every
downstream PRIMITIVES-* requirement on failing test files existing first.
Output: 7 files created/modified; `adapters/types.ts` interface widened
with overloads; full test-harness skeleton for Phase 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundational-primitive-lift/05-CONTEXT.md
@.planning/phases/05-foundational-primitive-lift/05-RESEARCH.md
@.planning/phases/05-foundational-primitive-lift/05-PATTERNS.md
@.planning/phases/05-foundational-primitive-lift/05-VALIDATION.md

@adapters/types.ts
@adapters/types.test.ts
@tests/conformance/write-transaction.test.ts
@tests/conformance/adapter.conformance.ts
@tests/conformance/commit-planning-state.test.ts
@sdk/src/query/pipeline.test.ts

<interfaces>
<!-- Current StorageAdapter interface shape (adapters/types.ts:27-67). -->
<!-- Plan 01 ONLY modifies putNamedDoc/getNamedDoc lines 59-60 to discriminated overloads -->
<!-- and ADDS new exports NamedDocCategory + RootNamedDocKey. Other signatures untouched. -->

Current lines 59-60:
```typescript
putNamedDoc(category: string, key: string, body: string): Promise<void>;
getNamedDoc(category: string, key: string): Promise<string | null>;
```

Target replacement (D-13, D-14):
```typescript
export type NamedDocCategory =
  | 'research' | 'intel' | 'codebase' | 'archived-milestone'
  | 'reports' | 'sketches' | 'tmp' | 'root';

export type RootNamedDocKey = 'HANDOFF' | 'CONTINUE-HERE' | 'DECISIONS-INDEX';

// In StorageAdapter interface, REPLACE lines 59-60 with discriminated overload pair:
putNamedDoc(category: 'root', key: RootNamedDocKey, body: string): Promise<void>;
putNamedDoc(category: Exclude<NamedDocCategory, 'root'>, key: string, body: string): Promise<void>;
getNamedDoc(category: 'root', key: RootNamedDocKey): Promise<string | null>;
getNamedDoc(category: Exclude<NamedDocCategory, 'root'>, key: string): Promise<string | null>;
```

Existing type-guard pattern to preserve (lines 100-102):
```typescript
export function hasNamedDoc(a: StorageAdapter): a is StorageAdapter & { capabilities: Capabilities & { namedDoc: true } } {
  return a.capabilities.namedDoc;
}
```

Test harness analog (tests/conformance/write-transaction.test.ts:7-27):
```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { mkdtemp, rm, mkdir } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { MarkdownAdapter } from '../../adapters/markdown/index.js';
import type { StorageAdapter } from '../../adapters/types.js';

describe('<PrimitiveName>', () => {
  let tmpDir: string;
  let adapter: StorageAdapter;
  beforeEach(async () => {
    tmpDir = await mkdtemp(join(tmpdir(), 'gsd-<slug>-'));
    await mkdir(join(tmpDir, '.planning'), { recursive: true });
    adapter = new MarkdownAdapter(tmpDir);
  });
  afterEach(async () => { await rm(tmpDir, { recursive: true, force: true }); });
  // it.todo('<test case>');
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NamedDocCategory + RootNamedDocKey exports and discriminated overloads to adapters/types.ts</name>
  <files>adapters/types.ts, adapters/types.test.ts</files>
  <read_first>
    - adapters/types.ts (current interface at lines 27-67; guards at lines 91-105)
    - adapters/types.test.ts (existing guard tests pattern)
    - .planning/phases/05-foundational-primitive-lift/05-CONTEXT.md §Decisions D-13/D-14
    - .planning/phases/05-foundational-primitive-lift/05-PATTERNS.md §adapters/types.ts
  </read_first>
  <action>
Edit adapters/types.ts per D-13/D-14:

1. Directly AFTER the `SectionMode` type export (currently around line 12), add these two new exports (before the `Capabilities` interface):
```typescript
/** D-13 (Phase 5): closed union of named-doc categories. Adding a member is a breaking interface change — intentional gate against typo drift. */
export type NamedDocCategory =
  | 'research'
  | 'intel'
  | 'codebase'
  | 'archived-milestone'
  | 'reports'
  | 'sketches'
  | 'tmp'
  | 'root';

/** D-14 (Phase 5): fixed-key discriminator for the 'root' category singletons at .planning/ root. */
export type RootNamedDocKey = 'HANDOFF' | 'CONTINUE-HERE' | 'DECISIONS-INDEX';
```

2. REPLACE the two existing lines (currently 59-60):
```typescript
putNamedDoc(category: string, key: string, body: string): Promise<void>;
getNamedDoc(category: string, key: string): Promise<string | null>;
```
with the discriminated overload pair (4 lines):
```typescript
// D-14 (Phase 5): discriminated overloads — 'root' narrows key to RootNamedDocKey.
putNamedDoc(category: 'root', key: RootNamedDocKey, body: string): Promise<void>;
putNamedDoc(category: Exclude<NamedDocCategory, 'root'>, key: string, body: string): Promise<void>;
getNamedDoc(category: 'root', key: RootNamedDocKey): Promise<string | null>;
getNamedDoc(category: Exclude<NamedDocCategory, 'root'>, key: string): Promise<string | null>;
```

3. Leave ALL other interface methods, capability fields, guards, and UnsupportedCapabilityError untouched. Do NOT flip any capability flag in Capabilities (that is Plans 03/04's job).

4. Append a new describe block to adapters/types.test.ts covering the typed union:
```typescript
describe('NamedDocCategory / RootNamedDocKey (D-13/D-14)', () => {
  it('NamedDocCategory values compile-check', () => {
    const cats: NamedDocCategory[] = ['research', 'intel', 'codebase', 'archived-milestone', 'reports', 'sketches', 'tmp', 'root'];
    expect(cats.length).toBe(8);
  });
  it('RootNamedDocKey values compile-check', () => {
    const keys: RootNamedDocKey[] = ['HANDOFF', 'CONTINUE-HERE', 'DECISIONS-INDEX'];
    expect(keys.length).toBe(3);
  });
  // Negative test documenting the overload's compile-time rejection. The
  // @ts-expect-error line MUST be present; removing it breaks this assertion.
  it('putNamedDoc("root", ARBITRARY_STRING) is rejected at compile time (D-14)', () => {
    // This block is a type-check-only assertion; no runtime execution.
    const assertOverloadRejectsArbitraryRootKey = (a: StorageAdapter): void => {
      // @ts-expect-error — 'root' category rejects arbitrary string keys per D-14
      a.putNamedDoc('root', 'ARBITRARY_STRING_NOT_IN_UNION', 'body');
    };
    expect(typeof assertOverloadRejectsArbitraryRootKey).toBe('function');
  });
});
```

Add matching imports at top of types.test.ts: `NamedDocCategory`, `RootNamedDocKey`.

After edit, rebuild the adapters dist per RESEARCH §Runtime State Inventory:
```bash
cd adapters && npm run build
```
(If the adapters package has no `build` script, run `npx tsc -p tsconfig.json` from inside adapters/.)
  </action>
  <verify>
    <automated>cd /Volumes/code/get-shit-done/adapters && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "error TS" | wc -l | grep -q "^[[:space:]]*0[[:space:]]*$" && cd /Volumes/code/get-shit-done/adapters && npx vitest run --config vitest.config.ts types.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export type NamedDocCategory" adapters/types.ts` returns 1
    - `grep -c "export type RootNamedDocKey" adapters/types.ts` returns 1
    - `grep -c "category: 'root'" adapters/types.ts` returns at least 2 (one for putNamedDoc overload, one for getNamedDoc)
    - `grep -c "Exclude<NamedDocCategory, 'root'>" adapters/types.ts` returns at least 2
    - `grep -c "@ts-expect-error" adapters/types.test.ts` returns at least 1 (the D-14 negative test)
    - `cd adapters && npx tsc --noEmit` exits 0 (no type errors)
    - `cd adapters && npx vitest run types.test.ts` passes (8 NamedDocCategory values + 3 RootNamedDocKey keys asserted)
    - The Capabilities interface (lines 14-25 pre-edit) is UNCHANGED — `grep "namedDoc: boolean" adapters/types.ts` still returns 1 match
  </acceptance_criteria>
  <done>
NamedDocCategory and RootNamedDocKey exports added; putNamedDoc/getNamedDoc
interface signatures replaced with discriminated overloads; adapters tsc
emits 0 errors; adapters/types.test.ts has a new describe covering D-13/D-14
including a @ts-expect-error negative case for 'root' + arbitrary string.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test scaffolds (5 new test files; no tests yet — all it.todo placeholders)</name>
  <files>tests/conformance/section-depth.test.ts, tests/conformance/named-doc.test.ts, tests/conformance/binary-asset.test.ts, sdk/src/query/sidecar.test.ts, sdk/src/query/scratch.test.ts</files>
  <read_first>
    - tests/conformance/write-transaction.test.ts (harness pattern lines 7-27)
    - tests/conformance/adapter.conformance.ts (factory shape lines 24-55)
    - sdk/src/query/pipeline.test.ts (SDK-side tmpDir harness)
    - .planning/phases/05-foundational-primitive-lift/05-VALIDATION.md §Wave 0 Requirements
    - .planning/phases/05-foundational-primitive-lift/05-PATTERNS.md §tests/conformance/*
  </read_first>
  <action>
Create each file using the harness pattern from `tests/conformance/write-transaction.test.ts:7-27`. Every file uses `it.todo(...)` placeholders — NO live tests yet, because the primitives they target are still stubs. Later plans (02/03/04) flip each `it.todo` to a real `it`.

**File 1: tests/conformance/section-depth.test.ts** (covers PRIMITIVES-01, PRIMITIVES-02, SC#2)
```typescript
/**
 * Phase 5 Plan 02 target — updateSection/getSection heading-depth walker.
 * Wave 0 scaffold (Plan 01): it.todo placeholders until Plan 02 implements D-06.
 */

import { describe, it, beforeEach, afterEach } from 'vitest';
import { mkdtemp, rm, mkdir } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { MarkdownAdapter } from '../../adapters/markdown/index.js';
import type { StorageAdapter } from '../../adapters/types.js';

describe('section-depth walker (PRIMITIVES-01, PRIMITIVES-02)', () => {
  let tmpDir: string;
  let adapter: StorageAdapter;

  beforeEach(async () => {
    tmpDir = await mkdtemp(join(tmpdir(), 'gsd-section-depth-'));
    await mkdir(join(tmpDir, '.planning'), { recursive: true });
    adapter = new MarkdownAdapter(tmpDir);
  });
  afterEach(async () => { await rm(tmpDir, { recursive: true, force: true }); });

  // Silence unused-variable lints until placeholders are filled.
  void adapter; void tmpDir;

  it.todo('L2 anchor "## Foo" extracts/replaces body (regression from Phase 1 L2-only)');
  it.todo('L3 anchor "### Evidence" under "## Investigation" extracts only the subsection');
  it.todo('L4 anchor "#### Sub-point" terminates at next L4/L3/L2');
  it.todo('getSection L3 returns body at deeper anchor');
  it.todo('getSection returns null when anchor missing');
  it.todo('Document-order first-match when two "### Evidence" share name (D-07)');
  it.todo('Fenced-code block containing "## Not a heading" does NOT split section (D-08)');
  it.todo('HTML-comment "<!-- ## Not a heading -->" does NOT split (D-08)');
  it.todo('Setext detected: console.warn emitted; no throw (D-08)');
  it.todo('three-author concurrency: 3 concurrent updateSection serialize, final doc has all three bodies (SC#2, PRIMITIVES-06)');
});
```

**File 2: tests/conformance/named-doc.test.ts** (covers PRIMITIVES-04, SC#3)
```typescript
/**
 * Phase 5 Plan 04 target — putNamedDoc/getNamedDoc with typed NamedDocCategory.
 * Wave 0 scaffold (Plan 01): it.todo placeholders until Plan 04 lifts stubs.
 */

import { describe, it, beforeEach, afterEach } from 'vitest';
import { mkdtemp, rm, mkdir } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { MarkdownAdapter } from '../../adapters/markdown/index.js';
import type { StorageAdapter, NamedDocCategory, RootNamedDocKey } from '../../adapters/types.js';

describe('putNamedDoc / getNamedDoc (PRIMITIVES-04)', () => {
  let tmpDir: string;
  let adapter: StorageAdapter;

  beforeEach(async () => {
    tmpDir = await mkdtemp(join(tmpdir(), 'gsd-named-doc-'));
    await mkdir(join(tmpDir, '.planning'), { recursive: true });
    adapter = new MarkdownAdapter(tmpDir);
  });
  afterEach(async () => { await rm(tmpDir, { recursive: true, force: true }); });

  void adapter; void tmpDir;
  // Type-level references so the imports are not pruned before Plan 04 fills tests.
  const _cats: NamedDocCategory[] = ['research', 'intel', 'codebase', 'archived-milestone', 'reports', 'sketches', 'tmp', 'root'];
  const _rootKeys: RootNamedDocKey[] = ['HANDOFF', 'CONTINUE-HERE', 'DECISIONS-INDEX'];
  void _cats; void _rootKeys;

  it.todo('round-trip for each of the 8 NamedDocCategory values');
  it.todo('putNamedDoc("root", "HANDOFF", body) writes to .planning/HANDOFF.md');
  it.todo('putNamedDoc("root", "CONTINUE-HERE", body) writes to .planning/CONTINUE-HERE.md');
  it.todo('putNamedDoc("root", "DECISIONS-INDEX", body) writes to .planning/DECISIONS-INDEX.md');
  it.todo('getNamedDoc("research", "nonexistent") returns null');
  it.todo('capabilities.namedDoc === true post-Plan 04');
  it.todo('@ts-expect-error — putNamedDoc("root", "ARBITRARY", body) rejected at compile (D-14)');
  it.todo('D-15 grep-zero: no legacy kind-tagged names in sdk/src/ or adapters/');
});
```

**File 3: tests/conformance/binary-asset.test.ts** (covers PRIMITIVES-05, SC#4)
```typescript
/**
 * Phase 5 Plan 04 target — writeBinaryAsset + capabilities.binaryAsset flip.
 * Wave 0 scaffold (Plan 01): it.todo placeholders until Plan 04 lifts stubs.
 */

import { describe, it, beforeEach, afterEach } from 'vitest';
import { mkdtemp, rm, mkdir } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { MarkdownAdapter } from '../../adapters/markdown/index.js';
import type { StorageAdapter } from '../../adapters/types.js';

describe('writeBinaryAsset (PRIMITIVES-05)', () => {
  let tmpDir: string;
  let adapter: StorageAdapter;

  beforeEach(async () => {
    tmpDir = await mkdtemp(join(tmpdir(), 'gsd-binary-asset-'));
    await mkdir(join(tmpDir, '.planning'), { recursive: true });
    adapter = new MarkdownAdapter(tmpDir);
  });
  afterEach(async () => { await rm(tmpDir, { recursive: true, force: true }); });

  void adapter; void tmpDir;

  it.todo('writeBinaryAsset("foo.png", bytes) writes bytes verbatim (readback with fs.readFile matches Buffer.from(bytes))');
  it.todo('capabilities.binaryAsset === true after Plan 04');
  it.todo('graceful-degradation: monkeypatch capabilities.binaryAsset=false; hasBinaryAsset returns false; caller skips write + warns (D-18)');
});
```

**File 4: sdk/src/query/sidecar.test.ts** (covers PRIMITIVES-08)
```typescript
/**
 * Phase 5 Plan 05 target — nextCallCountGet/Incr SDK verbs (D-19, D-21).
 * Wave 0 scaffold (Plan 01): it.todo placeholders until Plan 05 creates sidecar.ts.
 */

import { describe, it, beforeEach, afterEach } from 'vitest';
import { mkdtemp, rm, mkdir } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';

describe('sidecar — nextCallCountGet / nextCallCountIncr (PRIMITIVES-08)', () => {
  let tmpDir: string;

  beforeEach(async () => {
    tmpDir = await mkdtemp(join(tmpdir(), 'gsd-sidecar-'));
    await mkdir(join(tmpDir, '.planning'), { recursive: true });
  });
  afterEach(async () => { await rm(tmpDir, { recursive: true, force: true }); });

  void tmpDir;

  it.todo('nextCallCountGet returns 0 when .next-call-count missing');
  it.todo('nextCallCountIncr: 0 -> 1 -> 2');
  it.todo('route-next-action.ts:44 site uses nextCallCountGet helper — grep zero for raw adapter.getRecord(\'.next-call-count\')');
});
```

**File 5: sdk/src/query/scratch.test.ts** (covers PRIMITIVES-09)
```typescript
/**
 * Phase 5 Plan 05 target — discuss.checkpoint.* + discuss.questions.* SDK verbs (D-20).
 * Wave 0 scaffold (Plan 01): it.todo placeholders until Plan 05 creates scratch.ts.
 */

import { describe, it, beforeEach, afterEach } from 'vitest';
import { mkdtemp, rm, mkdir } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';

describe('scratch — discuss.checkpoint.* + discuss.questions.* (PRIMITIVES-09)', () => {
  let tmpDir: string;

  beforeEach(async () => {
    tmpDir = await mkdtemp(join(tmpdir(), 'gsd-scratch-'));
    await mkdir(join(tmpDir, '.planning', 'phases', '05-foundational-primitive-lift'), { recursive: true });
  });
  afterEach(async () => { await rm(tmpDir, { recursive: true, force: true }); });

  void tmpDir;

  it.todo('discuss.checkpoint round-trip at .planning/phases/05-foo/05-DISCUSS-CHECKPOINT.json');
  it.todo('discuss.checkpoint.delete removes the file; re-get returns null');
  it.todo('discuss.questions round-trip with .json and .html formats');
  it.todo('delete on nonexistent is no-op (no throw)');
});
```

All 5 files MUST end with a trailing newline. Do NOT add any live `it(...)` calls — only `it.todo(...)`.
  </action>
  <verify>
    <automated>cd /Volumes/code/get-shit-done && ls tests/conformance/section-depth.test.ts tests/conformance/named-doc.test.ts tests/conformance/binary-asset.test.ts sdk/src/query/sidecar.test.ts sdk/src/query/scratch.test.ts && npx vitest run --config vitest.conformance.config.ts tests/conformance/section-depth.test.ts tests/conformance/named-doc.test.ts tests/conformance/binary-asset.test.ts 2>&1 | tail -15 && cd sdk && npx vitest run src/query/sidecar.test.ts src/query/scratch.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist at the paths listed in `<files>`
    - Each file contains exactly one `describe(` block with the requirement-tagged name
    - Across all 5 files, `grep -c "it\.todo(" <file>` returns >= 3 per file
    - Across all 5 files, `grep -rn "^\s*it('" tests/conformance/section-depth.test.ts tests/conformance/named-doc.test.ts tests/conformance/binary-asset.test.ts sdk/src/query/sidecar.test.ts sdk/src/query/scratch.test.ts` returns 0 (no live tests yet)
    - `vitest run` against each file exits 0 and reports 0 failing tests (todos do not fail)
    - Each file imports `MarkdownAdapter` OR uses only path primitives (sidecar/scratch defer the SDK import to Plan 05)
    - Each file's beforeEach creates `.planning/` subdir via `mkdir({recursive:true})` — `grep -c "mkdir(join(tmpDir, '.planning'" <file>` returns 1 per file
  </acceptance_criteria>
  <done>
5 test scaffold files created at the exact paths in files_modified; each
contains only `it.todo(...)` placeholders; running vitest against each file
passes (0 failing, N todos); no file has live it() calls.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TypeScript compile boundary | The discriminated overload on `putNamedDoc`/`getNamedDoc` is the primary compile-time defense against `root` category taking arbitrary keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | `putNamedDoc('root', arbitrary_string, body)` | mitigate | Discriminated overload on interface (D-14) forces `key: RootNamedDocKey` for `'root'` — verified by `@ts-expect-error` test in adapters/types.test.ts; `tsc --noEmit` rejects drift at compile time |
| T-05-01-02 | Tampering | NamedDocCategory drift via typo in string literal | mitigate | Closed union (D-13) — any non-union member produces TS2322 at consumer call sites; no adapter changes needed here, but consumer Plans (02/04/05) inherit the guarantee |
| T-05-01-03 | Information Disclosure | Test scaffolds contain placeholder assertions that could leak fixtures | accept | it.todo placeholders have no executable body — cannot leak anything; later plans supply fixtures under `os.tmpdir()` (already isolated) |
</threat_model>

<verification>
1. `cd adapters && npx tsc --noEmit -p tsconfig.json` — 0 errors
2. `cd adapters && npx vitest run types.test.ts` — all new D-13/D-14 assertions pass
3. `npx vitest run --config vitest.conformance.config.ts tests/conformance/section-depth.test.ts tests/conformance/named-doc.test.ts tests/conformance/binary-asset.test.ts` — all pass (todos counted as passing)
4. `cd sdk && npx vitest run src/query/sidecar.test.ts src/query/scratch.test.ts` — all pass
5. `grep -c "it\.todo(" tests/conformance/section-depth.test.ts` >= 10 (covers L2/L3/L4, code fence, HTML comment, setext, three-author, getSection x2)
6. Full SDK suite unaffected: `cd sdk && npm test` — same pass count as pre-plan (regression check)
</verification>

<success_criteria>
- `adapters/types.ts` exports `NamedDocCategory` (8 members) and `RootNamedDocKey` (3 members); `putNamedDoc`/`getNamedDoc` use 4-signature discriminated overload
- All 5 Wave 0 test files exist with `it.todo` placeholders covering every Phase 5 requirement row in 05-VALIDATION.md
- adapters/ tsc passes with 0 errors; adapters/types.test.ts extends with D-13/D-14 describe
- Zero changes to adapters/markdown/index.ts (preserves wave serialization contract)
- Zero changes to any capability flag (capabilities remain at pre-Phase-5 values)
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundational-primitive-lift/05-01-SUMMARY.md`
with: files touched, D-13/D-14 assertions, test scaffold count per file, and a
list of the 5 Wave 0 test file paths that downstream plans will populate.
</output>
