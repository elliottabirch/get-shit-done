---
phase: 06-beadsadapter-implementation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - adapters/types.ts
  - adapters/markdown/index.ts
  - adapters/types.test.ts
  - tests/conformance/adapter.conformance.ts
  - ~/code/gsd-beads/package.json
  - ~/code/gsd-beads/tsconfig.json
  - ~/code/gsd-beads/vitest.config.ts
  - ~/code/gsd-beads/README.md
  - ~/code/gsd-beads/.gitignore
  - ~/code/gsd-beads/scripts/spike-bd-primitives.sh
  - ~/code/gsd-beads/scripts/SPIKE-RESULTS.md
  - ~/code/gsd-beads/src/index.ts
  - ~/code/gsd-beads/src/errors.ts
  - ~/code/gsd-beads/tests/conformance.test.ts
autonomous: true
requirements: [BEADS-01, BEADS-04]
must_haves:
  truths:
    - "Fork `Capabilities` interface declares `graphEdges: { semantic: boolean; dependency: boolean }`"
    - "MarkdownAdapter declares `graphEdges: { semantic: true, dependency: false }` and its test suite still passes"
    - "Fork `package.json` exports `./conformance` subpath resolvable via `require('get-shit-done/conformance')`"
    - "Sibling repo `~/code/gsd-beads` exists with npm-init scaffolding, vitest + TypeScript + file-linked fork dep"
    - "Sibling `BeadsAdapter` stub class is constructed from `src/index.ts`, declares capabilities per D-OQ06-CAPS/D-BINARY/D-TXN-CAPS/D-OQ06, and every StorageAdapter method throws `UnsupportedCapabilityError` or `NotYetImplementedError`"
    - "Sibling `BdManagedMismatchError` exported from `src/errors.ts` with `__brand`, `code`, `projectDir`, `hint` fields matching D-INIT-ERR"
    - "Sibling `tests/conformance.test.ts` imports `runAdapterConformanceSuite` via `get-shit-done/conformance` subpath and invokes it with `'beads'` + factory"
    - "Plan-01 spike script documents whether bd CLI + store-clone + bead-hash bookmark primitives are available; outcome written to `SPIKE-RESULTS.md` as A (in-memory buffer) or B (staging-store cutover)"
  artifacts:
    - path: "package.json"
      provides: "`exports` field with `./conformance` entry + `files` array includes `tests/conformance`"
      contains: "./conformance"
    - path: "adapters/types.ts"
      provides: "`Capabilities` interface extended with `graphEdges` (additive D-OQ06-CAPS)"
      contains: "graphEdges"
    - path: "adapters/markdown/index.ts"
      provides: "MarkdownAdapter capabilities object declares `graphEdges: { semantic: true, dependency: false }`"
      contains: "graphEdges"
    - path: "tests/conformance/adapter.conformance.ts"
      provides: "Header comment updated to document `'beads'` as an expected downstream consumer via the subpath export"
      contains: "get-shit-done/conformance"
    - path: "~/code/gsd-beads/package.json"
      provides: "npm package scaffold with `file:../get-shit-done` dev dep, `vitest` dev dep, test script, main entry `src/index.ts`"
      contains: "file:../get-shit-done"
    - path: "~/code/gsd-beads/tsconfig.json"
      provides: "TypeScript config extending fork's adapters/tsconfig.json pattern (strict, NodeNext, ES2022)"
      contains: "NodeNext"
    - path: "~/code/gsd-beads/vitest.config.ts"
      provides: "vitest config pointing at `src/` + `tests/` with --run discipline (no watch)"
      contains: "vitest"
    - path: "~/code/gsd-beads/src/index.ts"
      provides: "BeadsAdapter class skeleton implementing StorageAdapter; every method throws; capabilities declared per locked values"
      contains: "class BeadsAdapter"
    - path: "~/code/gsd-beads/src/errors.ts"
      provides: "`BdManagedMismatchError extends Error` with `__brand` + `[Symbol.hasInstance]` + `code: 'PROJECT_BD_MANAGED_MISMATCH'`"
      contains: "BdManagedMismatchError"
    - path: "~/code/gsd-beads/tests/conformance.test.ts"
      provides: "Imports `runAdapterConformanceSuite` via subpath export, invokes with `'beads'` + factory"
      contains: "get-shit-done/conformance"
    - path: "~/code/gsd-beads/scripts/spike-bd-primitives.sh"
      provides: "Executable spike script validating bd availability + probing store-clone + bead-hash primitives"
      contains: "bd"
    - path: "~/code/gsd-beads/scripts/SPIKE-RESULTS.md"
      provides: "Written decision: Outcome A (buffer) or Outcome B (staging-store), documenting primitives found/missing + rationale"
      contains: "Outcome"
  key_links:
    - from: "~/code/gsd-beads/src/index.ts"
      to: "adapters/types.ts"
      via: "import { StorageAdapter, Capabilities } from 'get-shit-done/...'"
      pattern: "import.*StorageAdapter"
    - from: "~/code/gsd-beads/tests/conformance.test.ts"
      to: "tests/conformance/adapter.conformance.ts"
      via: "import from 'get-shit-done/conformance' subpath export"
      pattern: "from 'get-shit-done/conformance'"
    - from: "adapters/markdown/index.ts"
      to: "adapters/types.ts"
      via: "Capabilities interface satisfaction — `graphEdges` field required"
      pattern: "graphEdges"
---

<objective>
Foundation plan for Phase 6: land the minimal fork-side contract changes, scaffold a fresh sibling repo at `~/code/gsd-beads`, and run the D-TXN spike so downstream plans know whether to implement Outcome A (in-memory buffer) or Outcome B (staging-store cutover). By end-of-plan every fork-side change required by downstream plans is in place, the sibling repo has a failing-but-structured test baseline, and the transaction-implementation path is locked in `SPIKE-RESULTS.md`.

Purpose: Unblock Plans 02–07. The subpath export + additive `graphEdges` capability are the ONLY fork-side changes in the entire phase; all subsequent plans operate in the sibling repo. The stub `BeadsAdapter` establishes the starting baseline that Waves 2–7 incrementally convert from red → green. The spike outcome is the primary gating decision for Plan 04's transaction implementation.

Output: Fork `package.json` extended with `./conformance` subpath export; fork `adapters/types.ts` + `adapters/markdown/index.ts` extended with additive `graphEdges` capability; fresh sibling repo scaffold at `~/code/gsd-beads` with npm init, TypeScript + vitest config, BeadsAdapter stub class, BdManagedMismatchError, conformance-harness invocation wiring, and a spike-outcome record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DECISIONS.md
@.planning/phases/06-beadsadapter-implementation/06-CONTEXT.md
@.planning/phases/06-beadsadapter-implementation/06-RESEARCH.md
@.planning/phases/06-beadsadapter-implementation/06-PATTERNS.md
@.planning/phases/06-beadsadapter-implementation/06-VALIDATION.md

<interfaces>
<!-- LOCKED StorageAdapter contract (Phase 1/5). BeadsAdapter implements this exact shape. -->
From adapters/types.ts (159 lines):
```typescript
export interface Capabilities {
  record: true;
  section: true;
  frontmatter: true;
  binaryAsset: boolean;
  snapshot: boolean;
  transaction: boolean;
  namedDoc: boolean;
  markdownLockfile: boolean;
  // NEW (Plan 01, D-OQ06-CAPS): additive graph-edges capability
  graphEdges: {
    semantic: boolean;
    dependency: boolean;
  };
}

export interface StorageAdapter {
  readonly name: string;
  readonly capabilities: Capabilities;
  getRecord(path: string): Promise<string | null>;
  putRecord(path: string, body: string): Promise<void>;
  removeRecord(path: string): Promise<void>;
  removeCollection(prefix: string): Promise<void>;
  listCollection(prefix: string, filter?: RecordFilter): Promise<RecordRef[]>;
  exists(path: string): Promise<boolean>;
  stat(path: string): Promise<{ kind: 'file' | 'dir'; mtime?: string } | null>;
  getSection(path: string, anchor: string): Promise<string | null>;
  updateSection(path: string, anchor: string, body: string, mode: SectionMode): Promise<void>;
  getFrontmatter(path: string, field?: string): Promise<unknown>;
  updateFrontmatter(path: string, field: string, value: unknown): Promise<void>;
  mergeFrontmatter(path: string, patch: Record<string, unknown>): Promise<void>;
  replaceInCurrentMilestone(pattern: string | RegExp, replacement: string): Promise<void>;
  readModifyWriteRoadmapMd(mutator: (content: string) => string): Promise<void>;
  writeBinaryAsset(path: string, bytes: Uint8Array): Promise<void>;
  snapshot(): Promise<string>;
  restore(snapshotId: string): Promise<void>;
  withTransaction<T>(fn: () => Promise<T>): Promise<T>;
  putNamedDoc(category: 'root', key: RootNamedDocKey, body: string, opts?: { workstream?: string }): Promise<void>;
  putNamedDoc(category: Exclude<NamedDocCategory, 'root'>, key: string, body: string, opts?: { workstream?: string }): Promise<void>;
  getNamedDoc(category: 'root', key: RootNamedDocKey, opts?: { workstream?: string }): Promise<string | null>;
  getNamedDoc(category: Exclude<NamedDocCategory, 'root'>, key: string, opts?: { workstream?: string }): Promise<string | null>;
  commitPlanningState(message: string, files?: string[]): Promise<void>;
  recordStateAppend(event: AppendEvent): Promise<StateWriteOutcome>;
  recordStateMutation(event: MutationEvent): Promise<StateWriteOutcome>;
  recordStateSignal(event: SignalEvent): Promise<StateWriteOutcome>;
}

export class UnsupportedCapabilityError extends Error {
  override readonly name = 'UnsupportedCapabilityError';
  readonly capability: string;
  readonly adapterName: string;
  readonly __brand = 'UnsupportedCapabilityError' as const;
  constructor(capability: string, adapterName: string) { ... }
  static [Symbol.hasInstance](instance: unknown): boolean { ... }
}
```

<!-- LOCKED conformance harness signature (Phase 1 D-15). Do NOT modify the signature. -->
From tests/conformance/adapter.conformance.ts:
```typescript
export function runAdapterConformanceSuite(
  adapterName: string,
  adapterFactory: (projectDir: string) => StorageAdapter,
): void
```

<!-- LOCKED MarkdownAdapter capabilities (reference; extend to include graphEdges). -->
From adapters/markdown/index.ts lines 120-130:
```typescript
readonly capabilities: Capabilities = {
  record: true,
  section: true,
  frontmatter: true,
  binaryAsset: true,
  snapshot: true,
  transaction: true,
  namedDoc: true,
  markdownLockfile: true,
  // After Plan 01: graphEdges: { semantic: true, dependency: false }
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fork-side — extend Capabilities with graphEdges + add ./conformance subpath export + compile adapter.conformance.ts</name>
  <files>package.json, adapters/types.ts, adapters/markdown/index.ts, adapters/types.test.ts, tests/conformance/adapter.conformance.ts</files>
  <read_first>
    adapters/types.ts (current Capabilities interface lines 54-65),
    adapters/markdown/index.ts (current capabilities object lines 120-130),
    adapters/types.test.ts (type-level test patterns for Capabilities),
    package.json (current `files` + `exports` — no `exports` field exists today),
    adapters/tsconfig.json (current outDir=dist, rootDir=.; adapter.conformance.ts is OUTSIDE adapters/ so this won't emit it),
    tests/conformance/adapter.conformance.ts (locked harness signature — DO NOT modify the signature),
    tests/conformance/markdown.conformance.test.ts (current consumer; imports via relative path — still works after subpath export lands)
  </read_first>
  <action>
Three fork-side edits in a single task; all must ship together so Plan 01 Task 3 (sibling scaffold) has a working subpath import.

(1) **Extend `Capabilities` interface** in `adapters/types.ts` (additive per D-OQ06-CAPS). Insert after `markdownLockfile: boolean;` on line 64, BEFORE the closing `}`:
```typescript
  graphEdges: {
    semantic: boolean;
    dependency: boolean;
  };
```
Do NOT touch any other field. Do NOT renumber D-05 / D-08 comments. The extension is purely additive — `graph: boolean` from SYNTHESIS §9 is REPLACED by this fine-grained shape (per D-OQ06-CAPS); no `graph` field exists today so no migration is needed.

(2) **Update MarkdownAdapter capabilities** in `adapters/markdown/index.ts` lines 120-130. Insert after `markdownLockfile: true,` on line 129:
```typescript
    graphEdges: {
      semantic: true,   // D-OQ06: graphify.cjs produces semantic edges from markdown
      dependency: false, // D-OQ06: markdown has no native dependency graph
    },
```
This is required for TypeScript to compile; the `true` literal in `record/section/frontmatter` forces compile-time presence of every Capabilities field.

(3) **Extend `adapters/types.test.ts`** (already present) with a type-level + runtime test for the new field. Append a new `describe('graphEdges capability (Phase 6 D-OQ06-CAPS)', ...)` block at the end of the file with at least:
   - A compile-time type assertion that `Capabilities['graphEdges']['semantic']` is `boolean` and `Capabilities['graphEdges']['dependency']` is `boolean`.
   - A runtime `expect(new MarkdownAdapter(tmpDir).capabilities.graphEdges).toEqual({ semantic: true, dependency: false })`.
   Use the existing test file's import + beforeEach + afterEach scaffolding as the template; the new describe block is one additional block, not a rewrite.

(4) **Add subpath export + extend files array** in root `package.json`. Current file has NO `exports` field and `files` is 14 entries (ending with `"adapters"` on line 23). After the `files` array closes (line 24) and BEFORE the `keywords` array, insert:
```json
  "exports": {
    ".": "./bin/install.js",
    "./conformance": "./tests/conformance/adapter.conformance.js"
  },
```
Also extend the `files` array to include `"tests/conformance"` as a new entry after `"adapters"`. The published npm tarball must include the conformance harness so the sibling's `file:../get-shit-done` dev-dep resolves both types and runtime.

(5) **Compile `adapter.conformance.ts` → `adapter.conformance.js`** so the subpath export resolves at require-time. Two options:
   (a) Preferred: extend `adapters/tsconfig.json` `include` to cover `"../tests/conformance/adapter.conformance.ts"` (paths must be relative to the tsconfig location). Verify `npm run build:sdk` (or an equivalent adapters-level compile) emits `adapter.conformance.js` at `tests/conformance/adapter.conformance.js`.
   (b) Fallback: add a tiny dedicated `tests/conformance/tsconfig.conformance.json` that emits `.js` in place (rootDir=`.`, outDir=`.`) and add `tsc -p tests/conformance/tsconfig.conformance.json` as a new npm script `build:conformance`. Wire `build:conformance` into `prepublishOnly` so the tarball ships the .js file.
   Choice is the executor's — pick (a) if it compiles cleanly; otherwise use (b). Document the choice in the task commit message.

(6) **Update the header comment** of `tests/conformance/adapter.conformance.ts` (DOC-ONLY; do NOT touch the signature or function body): add one sentence to the existing JSDoc block noting the sibling consumption path, e.g. "External adapters (e.g. `gsd-beads`) import this harness via the `get-shit-done/conformance` subpath export (package.json)." This is the documentation trace for D-CONFORM-EXPORT.

After all edits, run verification: (i) `npm run build:sdk` (or `npm run build:conformance` for option b) succeeds; (ii) `node -e "console.log(require('./tests/conformance/adapter.conformance.js').runAdapterConformanceSuite)"` prints `[Function: runAdapterConformanceSuite]`; (iii) `cd adapters && npm test -- --run` passes including the new `graphEdges` test block; (iv) `npm run test:conformance` passes (MarkdownAdapter conformance still green).
  </action>
  <acceptance_criteria>
- `grep -c "graphEdges" adapters/types.ts` ≥ 4 (interface field + 2 sub-fields + nothing else)
- `grep -c "graphEdges" adapters/markdown/index.ts` ≥ 2 (field name + semantic comment OR dependency comment)
- `grep -q "semantic: true" adapters/markdown/index.ts && grep -q "dependency: false" adapters/markdown/index.ts`
- `grep -c "\"./conformance\":" package.json` = 1
- `grep -q "tests/conformance" package.json` within the `"files"` array (line-scoped grep)
- `test -f tests/conformance/adapter.conformance.js` → exits 0
- `node -e "const m = require('./tests/conformance/adapter.conformance.js'); if (typeof m.runAdapterConformanceSuite !== 'function') process.exit(1)"` → exits 0
- `cd adapters && npm test -- --run 2>&1 | grep -v '^#' | grep -c "graphEdges"` ≥ 1 (new test block ran)
- `cd adapters && npm test -- --run` exits 0 (all adapter + markdown tests green)
- `npm run test:conformance` exits 0 (markdown conformance still green — additive change did not regress)
- `grep -q "get-shit-done/conformance" tests/conformance/adapter.conformance.ts` (header doc updated)
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/code/get-shit-done && npm run build:sdk && test -f tests/conformance/adapter.conformance.js && node -e "if (typeof require('./tests/conformance/adapter.conformance.js').runAdapterConformanceSuite !== 'function') process.exit(1)" && cd adapters && npm test -- --run && cd .. && npm run test:conformance</automated>
  </verify>
  <done>Capabilities interface extended with `graphEdges` (additive); MarkdownAdapter declares `{ semantic: true, dependency: false }`; types.test.ts covers the new field; adapter.conformance.ts compiles to .js; package.json `exports` includes `./conformance` subpath; fork test suites all green; subpath resolves at runtime.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sibling spike — validate bd CLI + probe store-clone and bead-hash bookmark primitives; write SPIKE-RESULTS.md locking D-TXN outcome</name>
  <files>~/code/gsd-beads/scripts/spike-bd-primitives.sh, ~/code/gsd-beads/scripts/SPIKE-RESULTS.md</files>
  <read_first>
    .planning/phases/06-beadsadapter-implementation/06-CONTEXT.md (D-TXN-SPIKE section — primitives to probe),
    .planning/phases/06-beadsadapter-implementation/06-RESEARCH.md §"D-TXN-SPIKE" + §"Environment Availability" + §"Assumptions Log A1/A2" (spike rationale + blocker escalation protocol),
    .planning/phases/06-beadsadapter-implementation/06-VALIDATION.md §"Wave 0 Requirements" (bd CLI gating),
    adapters/markdown/index.ts lines 563-657 (withTransaction + shadow-dir journal — the Outcome B analog to mirror)
  </read_first>
  <action>
**Prerequisite sanity check.** The sibling repo `~/code/gsd-beads/` does not yet exist. This task creates ONLY the `scripts/` subtree (so the spike can run before the full scaffold in Task 3). Run `mkdir -p ~/code/gsd-beads/scripts` first.

**Escalation protocol:** If `command -v bd` returns empty AND no bd-equivalent tool (e.g. `beads`, `bd-cli`) can be located via `which -a`, THE TASK OUTPUT IS `## PLANNING INCONCLUSIVE` per 06-RESEARCH §"Environment Availability" + 06-VALIDATION §"Wave 0 Requirements". Write `SPIKE-RESULTS.md` with status `BLOCKED` and stop — do NOT proceed to Task 3. Plan 01 cannot close until bd is installed.

**If bd IS available:** write an executable spike script at `~/code/gsd-beads/scripts/spike-bd-primitives.sh` that probes (in order, stopping on first hard-failure — but recording ALL outcomes even on failures):

1. **Availability:** `bd --version` (or `--help`) — record the installed version + CLI shape.
2. **Help surface:** `bd --help 2>&1 | head -80` — enumerate the top-level command list. Dump ALL lines to results.
3. **Init / managed-state probe (BEADS-04 pre-work):** Create a tmp dir (`mktemp -d`), `cd` into it, run `bd status` AND `ls -la .bd 2>&1` to determine the marker bd uses for "this dir is managed". Record exit code + marker path (is it `.bd/`? `.bdconfig`? something else?). This answer feeds Plan 02's init probe.
4. **Issue CRUD primitives (Bin A feasibility):** `bd init` (if supported), then `bd issue create` / `bd issue list` / `bd issue delete` — probe one-shot syntax for each. Record full CLI invocation + exit code + stdout.
5. **Sub-record primitive (D-MAPPING feasibility):** probe whether bd supports a JSON-field / sub-record shape (e.g. `bd issue set-field <id> <key> <json>`). If absent, record — this is a hard signal for the format module in Plan 03.
6. **Label primitive (D-MAPPING L3/L4 anchor-comments feasibility):** probe `bd issue add-label` / `bd issue list-labels` with long labels (e.g. `gsd:section:## Investigation 2026-05-01:### Evidence`) to confirm bd does not truncate or reject on colons/hashes/spaces. Record observed max label length.
7. **Comment primitive (event-family append feasibility):** probe `bd issue comment` and whether comments support label/tag attachment.
8. **Blocks-edge primitive (D-OQ06-DEP feasibility for Plan 06):** probe `bd issue block` / `bd issue blocks` / `bd issue blocked-by`. Record edge-query output shape for Plan 06's synthesizer.
9. **D-TXN Outcome B primitives — the CRITICAL spike:**
   - Store-clone: `bd clone <src> <dst>` (or equivalents: `bd store copy`, `bd export`, `bd snapshot`). Probe EVERY verb listed in Step 2's help output that could plausibly clone the store. Record outcomes.
   - Atomic bookmark-swap: `bd bookmark set <name> <hash>`, `bd branch`, `bd ref update-ref` — probe whatever bd's content-addressed / VCS-like primitives look like. Record which (if any) support atomic cutover.
10. **Node bindings check:** `npm view bd 2>&1 | head -20` and `npm view bd-bindings 2>&1 | head -5` to confirm whether a programmatic API exists. (06-RESEARCH Tertiary source says no; confirm.)

**Write `SPIKE-RESULTS.md`** following this exact section structure:

```markdown
# Plan 06-01 Spike: bd CLI Primitive Availability

**Date:** <YYYY-MM-DD>
**Environment:** <os>, bd <version-or-not-found>
**Script run:** ./scripts/spike-bd-primitives.sh

## 1. bd Availability
<OK|BLOCKED> — full `bd --version` output

## 2. Managed-State Marker (BEADS-04 input)
Marker path: <`.bd/` | other | missing>
`bd status` exit code on empty dir: <N>
Implications for `BeadsAdapter.init()`: <one sentence>

## 3. Issue CRUD (Bin A)
<commands tried + exit codes + one-line verdict>

## 4. Sub-Record JSON Fields (D-MAPPING L2)
<supported | not-supported | partial>
<If partial/unsupported: describe fallback — comments-everywhere is the only alternative>

## 5. Labels + Comments (D-MAPPING L3/L4, event families)
Max observed label length: <N chars>
Long label with colons/hashes accepted: <yes|no>
Comment labelling supported: <yes|no>

## 6. Blocks-Edges (D-OQ06 dependency-edge synthesizer)
<query command + output shape for Plan 06>

## 7. D-TXN Outcome Decision
**Outcome: <A | B>**

### If Outcome B (staging-store + bead-hash bookmark):
- store-clone command: `<exact invocation>`
- atomic bookmark swap: `<exact invocation>`
- Plan 04 implementation: staging bd store + bookmark cutover. `capabilities.snapshot: true`.

### If Outcome A (in-memory write-buffer fallback):
- Primitives probed: <list>
- Reason Outcome B unreachable: <one sentence>
- Plan 04 implementation: in-memory write buffer; sequential commit; partial-commit gap flagged as Phase 6.1 follow-up per D-TXN-SPIKE. `capabilities.snapshot: false`.

## 8. Node Bindings
npm view bd: <found|404>
Decision: <shell-out to CLI | node bindings | mix>

## 9. Carry-Forward Actions for Plan 02+
- <list of concrete bd CLI invocations Plan 02 will rely on, copy-paste ready>
```

The spike script and results document together constitute the transaction-implementation lock. All downstream plans reference `SPIKE-RESULTS.md §7` for the Outcome A/B branch.

**Do NOT implement any adapter code in this task.** Exploratory spike only. Every command run goes into `SPIKE-RESULTS.md` section 1-8; every implementation implication goes into section 9 for Plan 02+ consumption.
  </action>
  <acceptance_criteria>
- `test -d ~/code/gsd-beads/scripts` → exits 0
- `test -x ~/code/gsd-beads/scripts/spike-bd-primitives.sh` (executable bit set)
- `test -f ~/code/gsd-beads/scripts/SPIKE-RESULTS.md` → exits 0
- `grep -c "^## " ~/code/gsd-beads/scripts/SPIKE-RESULTS.md` ≥ 9 (all 9 sections present)
- `grep -qE "^\*\*Outcome: [AB]\*\*" ~/code/gsd-beads/scripts/SPIKE-RESULTS.md` (Outcome locked as exactly A or B) — OR — `grep -q "Status: BLOCKED" ~/code/gsd-beads/scripts/SPIKE-RESULTS.md` in which case the task returns `## PLANNING INCONCLUSIVE` via the execute-plan escalation path
- `grep -q "Managed-State Marker" ~/code/gsd-beads/scripts/SPIKE-RESULTS.md` (BEADS-04 answer captured)
- `grep -q "Blocks-Edges" ~/code/gsd-beads/scripts/SPIKE-RESULTS.md` (D-OQ06 answer captured)
- `grep -q "Carry-Forward Actions for Plan 02" ~/code/gsd-beads/scripts/SPIKE-RESULTS.md` (handoff section present)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test -x ~/code/gsd-beads/scripts/spike-bd-primitives.sh && test -f ~/code/gsd-beads/scripts/SPIKE-RESULTS.md && grep -c "^## " ~/code/gsd-beads/scripts/SPIKE-RESULTS.md | awk "{if (\$1 < 9) exit 1}" && (grep -qE "^\*\*Outcome: [AB]\*\*" ~/code/gsd-beads/scripts/SPIKE-RESULTS.md || grep -q "Status: BLOCKED" ~/code/gsd-beads/scripts/SPIKE-RESULTS.md)'</automated>
  </verify>
  <done>Spike script is executable and was run; SPIKE-RESULTS.md records bd availability, managed-state marker, CRUD/sub-record/label/comment/blocks-edge primitive probes, the D-TXN Outcome A/B decision, and concrete carry-forward commands Plan 02 can invoke verbatim. If bd unavailable, SPIKE-RESULTS marks BLOCKED and Plan 01 returns `## PLANNING INCONCLUSIVE`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sibling scaffold — npm init + TypeScript + vitest + BeadsAdapter stub + BdManagedMismatchError + conformance-invocation test</name>
  <files>~/code/gsd-beads/package.json, ~/code/gsd-beads/tsconfig.json, ~/code/gsd-beads/vitest.config.ts, ~/code/gsd-beads/README.md, ~/code/gsd-beads/.gitignore, ~/code/gsd-beads/src/index.ts, ~/code/gsd-beads/src/errors.ts, ~/code/gsd-beads/tests/conformance.test.ts</files>
  <read_first>
    adapters/markdown/index.ts lines 116-158 (MarkdownAdapter class skeleton pattern — constructor + capabilities + name declaration),
    adapters/types.ts (full StorageAdapter interface — every method the stub must declare) lines 67-121 + UnsupportedCapabilityError class lines 123-142,
    adapters/tsconfig.json (fork's TypeScript config — mirror the shape: NodeNext, strict, ES2022),
    adapters/vitest.config.ts (fork's vitest config — mirror the shape),
    tests/conformance/markdown.conformance.test.ts (exact 6-line pattern the sibling test mirrors),
    .planning/phases/06-beadsadapter-implementation/06-PATTERNS.md §"~/code/gsd-beads/src/index.ts" + §"~/code/gsd-beads/src/errors.ts" + §"Error Handling — UnsupportedCapabilityError",
    ~/code/gsd-beads/scripts/SPIKE-RESULTS.md (Outcome A or B — capabilities.snapshot value depends on this)
  </read_first>
  <action>
**This task depends on Task 2 having decided Outcome A or B.** Read `SPIKE-RESULTS.md §7` first; the value of `capabilities.snapshot` in the stub depends on it (`true` if Outcome B, `false` if Outcome A). Do NOT guess — if SPIKE-RESULTS.md is missing or ambiguous, halt and return to Task 2.

**(1) Fresh npm init at sibling root.** From `~/code/gsd-beads/`:
```bash
cd ~/code/gsd-beads
npm init -y
```
Then edit the generated `package.json` to:
- `"name": "gsd-beads"` (matches Phase 8 D-RUNTIME-RESOLUTION `require("gsd-{name}")` pattern).
- `"version": "0.1.0-alpha.0"` (pre-v1.0 sibling; bumps when BeadsAdapter is feature-complete).
- `"type": "module"` (ESM-first; matches fork's NodeNext setup).
- `"main": "src/index.ts"` for dev-loop via file-link; add `"exports": { ".": "./src/index.ts" }` as well (Claude's discretion on shape; stick with named class export `export class BeadsAdapter` — matches MarkdownAdapter pattern and `gsd-beads` resolver can `require("gsd-beads").BeadsAdapter`).
- `"scripts": { "test": "vitest run", "test:watch": "vitest", "typecheck": "tsc --noEmit" }` (no watch mode default — matches fork CI discipline per 06-VALIDATION).
- `"devDependencies": { "get-shit-done": "file:../get-shit-done", "vitest": "^1.0.0", "typescript": "^5.3.0", "@types/node": "^20.0.0" }`.
- `"engines": { "node": ">=22.0.0" }` (match fork).

Run `npm install` after editing so `file:../get-shit-done` resolves and the subpath export from Task 1 is reachable.

**(2) `tsconfig.json`** — mirror `adapters/tsconfig.json` but scoped to sibling:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "outDir": "dist",
    "rootDir": ".",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "tests/**/*.ts"],
  "exclude": ["dist", "node_modules"]
}
```

**(3) `vitest.config.ts`** — mirror fork's adapters/vitest.config.ts:
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    name: 'gsd-beads',
    include: ['src/**/*.test.ts', 'tests/**/*.test.ts'],
    exclude: ['dist/**', 'node_modules/**'],
    testTimeout: 30_000,
  },
});
```

**(4) `.gitignore`** — standard node+ts ignores: `node_modules`, `dist`, `.DS_Store`, `*.log`, `.env`.

**(5) `README.md`** — minimum viable README (Plan 07 will expand this). Minimum content:
   - Title: `# gsd-beads`
   - One-sentence description: "BeadsAdapter for the get-shit-done fork v1.0 — implements the locked StorageAdapter contract at `adapters/types.ts` against the `bd` issue-tracking CLI."
   - "Depends on" section linking to `../get-shit-done/adapters/types.ts`.
   - **Status: alpha** — "Plans 06-02..06-07 convert the stub class into a full implementation; Plan 07 ships the canonical README."
   - **Export shape:** "`export class BeadsAdapter` from `src/index.ts`. Phase 8's adapter-name runtime resolver will `require('gsd-beads').BeadsAdapter`."

**(6) `src/errors.ts`** — implement `BdManagedMismatchError` per D-INIT-ERR + 06-PATTERNS.md §"src/errors.ts":
```typescript
export class BdManagedMismatchError extends Error {
  override readonly name = 'BdManagedMismatchError';
  readonly code = 'PROJECT_BD_MANAGED_MISMATCH' as const;
  readonly projectDir: string;
  readonly hint: string;
  readonly __brand = 'BdManagedMismatchError' as const;

  constructor(projectDir: string, hint: string) {
    super(`Project at '${projectDir}' is not bd-managed. ${hint}`);
    this.projectDir = projectDir;
    this.hint = hint;
  }

  static [Symbol.hasInstance](instance: unknown): boolean {
    return (
      instance != null &&
      typeof instance === 'object' &&
      (instance as Record<string, unknown>).__brand === 'BdManagedMismatchError'
    );
  }
}
```
NOTE: Plan 02 will add the actual `init()` call that *throws* this error. Plan 01 only ensures the class exists and is importable.

**(7) `src/index.ts`** — BeadsAdapter STUB class. Every method throws `NotYetImplementedError` (to be converted to real impl in Plans 02-06) OR `UnsupportedCapabilityError` (for permanently unsupported capabilities). The capabilities object is LOCKED per the context directive:

```typescript
import type {
  StorageAdapter,
  Capabilities,
  RecordRef,
  RecordFilter,
  SectionMode,
  NamedDocCategory,
  RootNamedDocKey,
  StateWriteOutcome,
} from 'get-shit-done/adapters/types.js';  // OR the correct relative-to-file-linked path;
// if the subpath `get-shit-done/adapters/types` is not exported in package.json,
// import from the direct relative path `../../get-shit-done/adapters/types.js`.
// Try the subpath first; fall back to relative only if npm install rejects it.
import { UnsupportedCapabilityError } from 'get-shit-done/adapters/types.js';  // same fallback rule
import type { AppendEvent, MutationEvent, SignalEvent } from 'get-shit-done/adapters/state-event-types.js';
import { BdManagedMismatchError } from './errors.js';

class NotYetImplementedError extends Error {
  override readonly name = 'NotYetImplementedError';
  constructor(method: string) {
    super(`BeadsAdapter.${method}() is not yet implemented (Plan 06-01 stub). Subsequent plans in Phase 6 implement this method.`);
  }
}

export class BeadsAdapter implements StorageAdapter {
  readonly name = 'beads' as const;

  readonly capabilities: Capabilities = {
    record: true,
    section: true,
    frontmatter: true,
    binaryAsset: false,                                             // D-BINARY: skip-and-warn
    snapshot: /* INSERT spike outcome: true if Outcome B, false if Outcome A */,
    transaction: true,                                              // D-TXN-CAPS: true in BOTH outcomes
    namedDoc: true,
    markdownLockfile: false,                                        // OQ-08: bd has no markdown-lockfile primitives
    graphEdges: {
      semantic: false,                                              // D-OQ06: markdown-only pipeline; BeadsAdapter does not emit
      dependency: true,                                             // D-OQ06: bd blocks/blocked-by native
    },
  };

  private readonly projectDir: string;

  constructor(projectDir: string) {
    this.projectDir = projectDir;
    // Plan 02 adds the bd-managed probe here. Plan 01 stub does NOT throw — the
    // conformance test harness constructs the adapter before running any method,
    // and we want the stub to construct cleanly so downstream tests fail at
    // method-call time, not at construction time.
  }

  // Bin A record
  async getRecord(_path: string): Promise<string | null> { throw new NotYetImplementedError('getRecord'); }
  async putRecord(_path: string, _body: string): Promise<void> { throw new NotYetImplementedError('putRecord'); }
  async removeRecord(_path: string): Promise<void> { throw new NotYetImplementedError('removeRecord'); }
  async removeCollection(_prefix: string): Promise<void> { throw new NotYetImplementedError('removeCollection'); }
  async listCollection(_prefix: string, _filter?: RecordFilter): Promise<RecordRef[]> { throw new NotYetImplementedError('listCollection'); }
  async exists(_path: string): Promise<boolean> { throw new NotYetImplementedError('exists'); }
  async stat(_path: string): Promise<{ kind: 'file' | 'dir'; mtime?: string } | null> { throw new NotYetImplementedError('stat'); }

  // Bin A section
  async getSection(_path: string, _anchor: string): Promise<string | null> { throw new NotYetImplementedError('getSection'); }
  async updateSection(_path: string, _anchor: string, _body: string, _mode: SectionMode): Promise<void> { throw new NotYetImplementedError('updateSection'); }

  // Bin A frontmatter
  async getFrontmatter(_path: string, _field?: string): Promise<unknown> { throw new NotYetImplementedError('getFrontmatter'); }
  async updateFrontmatter(_path: string, _field: string, _value: unknown): Promise<void> { throw new NotYetImplementedError('updateFrontmatter'); }
  async mergeFrontmatter(_path: string, _patch: Record<string, unknown>): Promise<void> { throw new NotYetImplementedError('mergeFrontmatter'); }

  // markdownLockfile group — permanently unsupported on BeadsAdapter per OQ-08
  async replaceInCurrentMilestone(_pattern: string | RegExp, _replacement: string): Promise<void> {
    throw new UnsupportedCapabilityError('markdownLockfile', this.name);
  }
  async readModifyWriteRoadmapMd(_mutator: (content: string) => string): Promise<void> {
    throw new UnsupportedCapabilityError('markdownLockfile', this.name);
  }

  // Foundational
  async writeBinaryAsset(_path: string, _bytes: Uint8Array): Promise<void> {
    throw new UnsupportedCapabilityError('binaryAsset', this.name); // D-BINARY: permanently false
  }
  async snapshot(): Promise<string> {
    if (!this.capabilities.snapshot) throw new UnsupportedCapabilityError('snapshot', this.name);
    throw new NotYetImplementedError('snapshot'); // Plan 04 implements Outcome B
  }
  async restore(_snapshotId: string): Promise<void> {
    if (!this.capabilities.snapshot) throw new UnsupportedCapabilityError('snapshot', this.name);
    throw new NotYetImplementedError('restore');
  }
  async withTransaction<T>(_fn: () => Promise<T>): Promise<T> { throw new NotYetImplementedError('withTransaction'); }

  // Named-doc discriminated overloads (Plan 05 implements)
  async putNamedDoc(_category: NamedDocCategory, _key: string, _body: string, _opts?: { workstream?: string }): Promise<void> {
    throw new NotYetImplementedError('putNamedDoc');
  }
  async getNamedDoc(_category: NamedDocCategory, _key: string, _opts?: { workstream?: string }): Promise<string | null> {
    throw new NotYetImplementedError('getNamedDoc');
  }

  async commitPlanningState(_message: string, _files?: string[]): Promise<void> {
    throw new NotYetImplementedError('commitPlanningState'); // Plan 04 implements bead-hash bookmark
  }

  // Event families (Plan 05 implements)
  async recordStateAppend(_event: AppendEvent): Promise<StateWriteOutcome> { throw new NotYetImplementedError('recordStateAppend'); }
  async recordStateMutation(_event: MutationEvent): Promise<StateWriteOutcome> { throw new NotYetImplementedError('recordStateMutation'); }
  async recordStateSignal(_event: SignalEvent): Promise<StateWriteOutcome> { throw new NotYetImplementedError('recordStateSignal'); }
}

// Re-export BdManagedMismatchError so sibling consumers can catch by class
export { BdManagedMismatchError } from './errors.js';
```

**(8) `tests/conformance.test.ts`** — conformance invocation (mirrors `tests/conformance/markdown.conformance.test.ts` byte-for-byte modulo adapter name):
```typescript
import { runAdapterConformanceSuite } from 'get-shit-done/conformance';
import { BeadsAdapter } from '../src/index.js';

// Phase 6 runs against a stub that throws on every method — expected to be
// mostly red until Waves 2-7 land. Phase 7 promotes this to paired conformance.
runAdapterConformanceSuite('beads', (projectDir) => new BeadsAdapter(projectDir));
```

**After all files written:** from `~/code/gsd-beads/` run:
- `npm install` — confirm `file:../get-shit-done` resolves AND the `./conformance` subpath is reachable.
- `npm run typecheck` — must pass (stub class satisfies every method signature from the locked contract).
- `npm test -- --run` — expected to FAIL on most assertions (methods throw `NotYetImplementedError`) but MUST NOT fail at import/construction time. At least the `getRecord returns null for non-existent path` test should get far enough to fail inside the method body (not at resolution). The `StorageAdapter conformance: beads` describe block MUST appear in output.

**Expected baseline:** the test run produces many failures (that's the starting point Waves 2-7 convert to green). Confirm that:
(a) zero tests PASS (the stub throws on every call — the `getRecord null-on-miss` test would fail because the stub throws instead of returning null; that's expected and desirable for this wave);
(b) the vitest output contains the string `StorageAdapter conformance: beads` (proves the harness wired correctly via the subpath export);
(c) no test fails with `Cannot find module 'get-shit-done/conformance'` (proves the subpath resolves).
  </action>
  <acceptance_criteria>
- `test -f ~/code/gsd-beads/package.json && test -f ~/code/gsd-beads/tsconfig.json && test -f ~/code/gsd-beads/vitest.config.ts` → all exit 0
- `test -f ~/code/gsd-beads/src/index.ts && test -f ~/code/gsd-beads/src/errors.ts && test -f ~/code/gsd-beads/tests/conformance.test.ts` → all exit 0
- `grep -q "file:../get-shit-done" ~/code/gsd-beads/package.json`
- `grep -q "\"name\": \"gsd-beads\"" ~/code/gsd-beads/package.json`
- `grep -q "export class BeadsAdapter" ~/code/gsd-beads/src/index.ts`
- `grep -q "export class BdManagedMismatchError" ~/code/gsd-beads/src/errors.ts`
- `grep -q "'PROJECT_BD_MANAGED_MISMATCH' as const" ~/code/gsd-beads/src/errors.ts`
- `grep -q "__brand = 'BdManagedMismatchError' as const" ~/code/gsd-beads/src/errors.ts`
- `grep -q "Symbol.hasInstance" ~/code/gsd-beads/src/errors.ts`
- `grep -q "get-shit-done/conformance" ~/code/gsd-beads/tests/conformance.test.ts`
- `grep -q "runAdapterConformanceSuite('beads'" ~/code/gsd-beads/tests/conformance.test.ts`
- `grep -c "readonly capabilities: Capabilities" ~/code/gsd-beads/src/index.ts` ≥ 1
- Every field of Capabilities appears in BeadsAdapter's capabilities object: `grep -c -E "(record|section|frontmatter|binaryAsset|snapshot|transaction|namedDoc|markdownLockfile|graphEdges)" ~/code/gsd-beads/src/index.ts` ≥ 9
- `grep -q "binaryAsset: false" ~/code/gsd-beads/src/index.ts` (D-BINARY locked)
- `grep -q "transaction: true" ~/code/gsd-beads/src/index.ts` (D-TXN-CAPS locked)
- `grep -q "graphEdges" ~/code/gsd-beads/src/index.ts && grep -q "semantic: false" ~/code/gsd-beads/src/index.ts && grep -q "dependency: true" ~/code/gsd-beads/src/index.ts` (D-OQ06 locked)
- `cd ~/code/gsd-beads && npm run typecheck` → exits 0 (stub satisfies locked contract)
- `cd ~/code/gsd-beads && npm test -- --run 2>&1 | grep -q "StorageAdapter conformance: beads"` (harness wires via subpath export)
- `cd ~/code/gsd-beads && npm test -- --run 2>&1 | grep -cv "Cannot find module"` — zero module-resolution errors
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd ~/code/gsd-beads && npm install --silent && npm run typecheck && (npm test -- --run 2>&1 | tee /tmp/gsd-beads-test-baseline.log; grep -q "StorageAdapter conformance: beads" /tmp/gsd-beads-test-baseline.log && ! grep -q "Cannot find module" /tmp/gsd-beads-test-baseline.log)'</automated>
  </verify>
  <done>Sibling repo exists at `~/code/gsd-beads` with complete npm+TypeScript+vitest scaffold, `BeadsAdapter` stub class with locked capabilities (every field of Capabilities declared with correct value per D-BINARY/D-TXN-CAPS/D-OQ06), `BdManagedMismatchError` exported from `src/errors.ts` with brand + `[Symbol.hasInstance]`, and the conformance harness is invoked via the `get-shit-done/conformance` subpath. `npm run typecheck` passes; `npm test -- --run` runs the harness (most assertions fail — that's the starting baseline).</done>
</task>

</tasks>

<verification>
Run the following from the fork root AFTER all three tasks complete:

1. Fork-side sanity:
   - `npm run build:sdk` exits 0
   - `test -f tests/conformance/adapter.conformance.js`
   - `cd adapters && npm test -- --run` exits 0 (graphEdges block runs, MarkdownAdapter still green)
   - `npm run test:conformance` exits 0 (fork's own conformance suite still green)

2. Sibling-side sanity:
   - `cd ~/code/gsd-beads && npm run typecheck` exits 0
   - `cd ~/code/gsd-beads && npm test -- --run` emits `StorageAdapter conformance: beads` (harness wired)
   - `cd ~/code/gsd-beads && npm test -- --run` has ZERO `Cannot find module` errors

3. Spike outcome locked:
   - `grep -qE "^\*\*Outcome: [AB]\*\*" ~/code/gsd-beads/scripts/SPIKE-RESULTS.md`
   - `grep -q "Carry-Forward Actions for Plan 02" ~/code/gsd-beads/scripts/SPIKE-RESULTS.md`

4. No contract drift:
   - `grep -c "graphEdges" adapters/types.ts` ≥ 4 (additive field committed)
   - No other interface changes: `git diff adapters/types.ts | grep -c '^-'` should be 0 (additive only — no removals)
</verification>

<success_criteria>
- Fork-side changes shipped: package.json `exports` includes `./conformance`, files array includes `tests/conformance`, `adapters/types.ts` Capabilities extended with `graphEdges`, MarkdownAdapter capabilities object updated, adapter.conformance.js emitted, all fork tests green.
- Sibling repo exists at `~/code/gsd-beads` with full scaffold: package.json, tsconfig.json, vitest.config.ts, README.md, .gitignore, src/index.ts (stub BeadsAdapter), src/errors.ts (BdManagedMismatchError), tests/conformance.test.ts (invokes suite).
- `SPIKE-RESULTS.md` locks D-TXN Outcome A or B with concrete bd CLI invocations carry-forwarded for Plan 02.
- `capabilities.snapshot` value in `src/index.ts` matches spike outcome (true if B, false if A).
- The sibling conformance harness runs (even though most assertions fail — that's the intended baseline for Waves 2-7).
</success_criteria>

<output>
After completion, create `.planning/phases/06-beadsadapter-implementation/06-01-SUMMARY.md` documenting:
- Fork-side diffs (3 files touched)
- Sibling repo scaffold (files created)
- Spike outcome: A or B (reference SPIKE-RESULTS.md §7 line)
- Baseline test count (X failing assertions expected in sibling)
- Handoff notes for Plan 02 (specific bd CLI invocations to use; bd-managed-state marker path)
</output>