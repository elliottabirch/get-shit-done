---
phase: 04-plug-workflow-leaks-top-10-context-block-class
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-04]
files_modified:
  - get-shit-done/templates/phase-prompt.md
  - get-shit-done/templates/debug-subagent-prompt.md
  - get-shit-done/templates/planner-subagent-prompt.md
  - get-shit-done/references/tdd.md
  - get-shit-done/references/planner-antipatterns.md
  - get-shit-done/references/context-budget.md
  - get-shit-done/references/scout-codebase.md
  - get-shit-done/references/universal-anti-patterns.md
  - get-shit-done/commands/add-tests.md
  - get-shit-done/agents/gsd-planner.md
autonomous: true
requirements:
  - LEAKS-02
must_haves:
  truths:
    - "Templates no longer contain @.planning/ references"
    - "Templates use orchestrator-injected <project_context> blocks instead"
    - "References with Read-tool leaks use SDK queries instead"
    - "REWRITE-CANDIDATE context-block entries (add-tests, gsd-planner) are mitigated"
    - "OQ-04 resolved with uniform strategy across all affected files"
  artifacts:
    - path: "get-shit-done/templates/phase-prompt.md"
      provides: "Zero context-block leaks; orchestrator-injected context"
    - path: "get-shit-done/templates/planner-subagent-prompt.md"
      provides: "Zero context-block leaks; orchestrator-injected context"
    - path: "get-shit-done/references/context-budget.md"
      provides: "SDK query calls instead of Read-tool .planning/ refs"
  key_links:
    - from: "get-shit-done/templates/phase-prompt.md"
      to: "sdk/src/query/init.ts"
      via: "Orchestrator calls gsd-sdk query init.* and injects result"
      pattern: "project_context"
---

<objective>
Rewrite all template and reference context-block leaks per D-05/D-06, plus mitigate the
5 REWRITE-CANDIDATE entries from the Phase 2 context-block audit. This resolves OQ-04
(LEAKS-02).

Purpose: Per D-05, `@.planning/...` references in templates are replaced with
orchestrator-injected content. The orchestrator calls SDK queries and pastes results
directly into the subagent prompt at construction time. Per D-06, phase-prompt.md,
debug-subagent-prompt.md, planner-subagent-prompt.md, tdd.md, and planner-antipatterns.md
are all rewritten in this phase.

Output: 12 template/reference/command/agent files with zero context-block leaks; uniform
orchestrator-injection pattern established.
</objective>

<execution_context>
@.planning/phases/04-plug-workflow-leaks-top-10-context-block-class/04-CONTEXT.md
@.planning/phases/04-plug-workflow-leaks-top-10-context-block-class/04-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
Context-block mitigation pattern (D-05):

BEFORE (template with leak):
```markdown
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>
```

AFTER (orchestrator-injected):
```markdown
<project_context>
<!-- Orchestrator injects via: gsd-sdk query init.{workflow} -->
<!-- Content is available before agent starts (equally deterministic to @) -->
{project_context_placeholder}
</project_context>
```

The orchestrator (the parent workflow that spawns subagents) calls:
- `gsd-sdk query init.execute-phase` (for execution subagents)
- `gsd-sdk query init.plan-phase` (for planning subagents)
- `gsd-sdk query state.load` + `gsd-sdk query roadmap` (for general context)

And pastes the result into the template before passing it to the subagent.

REWRITE-CANDIDATE context-block register (from Phase 2 audit):
- commands/gsd/add-tests.md (2 @.planning/ refs)
- agents/gsd-planner.md (3 @.planning/ refs)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite template context-block leaks (phase-prompt, debug-subagent, planner-subagent)</name>
  <files>
    get-shit-done/templates/phase-prompt.md
    get-shit-done/templates/debug-subagent-prompt.md
    get-shit-done/templates/planner-subagent-prompt.md
  </files>
  <action>
**phase-prompt.md (5 context-block leaks):**
This template has multiple `@.planning/PROJECT.md` references (x4 in different examples) and `@.planning/phases/*/SUMMARY.md`. These are in `<context>` blocks that get auto-loaded by Claude Code's `@` file inclusion.

For each `<context>` block containing `@.planning/` references:
1. Replace the `<context>` block with a `<project_context>` block
2. Add a comment indicating the orchestrator SDK query that provides the data:
   ```markdown
   <project_context>
   <!-- Injected by orchestrator via: gsd-sdk query init.execute-phase -->
   <!-- Provides: PROJECT.md content, ROADMAP.md content, STATE.md content -->
   <!-- Prior SUMMARY refs resolved via: gsd-sdk query history-digest -->
   </project_context>
   ```
3. If the `@.planning/` reference is inside an EXAMPLE section (showing users what a plan looks like), change it to a non-activating form: use backtick-wrapped `\`@.planning/PROJECT.md\`` or describe it in prose instead of using the raw `@` syntax that triggers file loading

**debug-subagent-prompt.md (1 context-block leak):**
- Replace the `@.planning/` reference with orchestrator-injection comment
- The debug orchestrator calls `gsd-sdk query state.load` + relevant debug context

**planner-subagent-prompt.md (10 @.planning/ refs per RESEARCH):**
Note: RESEARCH.md mentions this file. Check the actual count. Replace all `@.planning/` references with orchestrator-injected `<project_context>` blocks. The planner orchestrator already calls `gsd-sdk query init.plan-phase` which bundles all planning context.

For ALL templates: ensure the rewritten version clearly documents which SDK query the orchestrator must call. Use HTML comments inside the `<project_context>` block so the information is visible to developers reading the template but doesn't affect agent behavior.
  </action>
  <verify>
    <automated>cd /Volumes/code/get-shit-done && node scripts/leak-grep.cjs get-shit-done/templates/phase-prompt.md get-shit-done/templates/debug-subagent-prompt.md get-shit-done/templates/planner-subagent-prompt.md 2>&1; echo "exit: $?"</automated>
  </verify>
  <done>All 3 template files report 0 leaks from leak-grep; each has orchestrator-injection documentation in <project_context> blocks</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite reference file leaks + REWRITE-CANDIDATE context-blocks</name>
  <files>
    get-shit-done/references/tdd.md
    get-shit-done/references/planner-antipatterns.md
    get-shit-done/references/context-budget.md
    get-shit-done/references/scout-codebase.md
    get-shit-done/references/universal-anti-patterns.md
    get-shit-done/commands/add-tests.md
    get-shit-done/agents/gsd-planner.md
  </files>
  <action>
**Reference files with context-block leaks (7 leaks total):**

**tdd.md (1 context-block leak):**
- Has `@.planning/PROJECT.md` and/or `@.planning/ROADMAP.md` in a `<context>` block
- Replace with `<project_context>` orchestrator-injected block
- Note: TDD reference is loaded by the planner workflow which already has init context

**planner-antipatterns.md (2 context-block leaks):**
- Has `@.planning/phases/*/SUMMARY.md` and `@.planning/PROJECT.md`
- Replace with `<project_context>` block; prior SUMMARYs are injected by orchestrator via `gsd-sdk query history-digest`

**context-budget.md (2 Read-tool leaks):**
- References `.planning/config.json` via Read tool
- Replace with `gsd-sdk query config-get` calls
- These are instructions to an agent to read config — change to SDK query instruction

**scout-codebase.md (1 Read-tool leak):**
- References `.planning/codebase/*.md` via Read
- Replace with `gsd-sdk query codebase.get` or `gsd-sdk query codebase.list`

**universal-anti-patterns.md (1 Read-tool leak):**
- References `.planning/config.json` via Read
- Replace with `gsd-sdk query config-get`

**REWRITE-CANDIDATE context-blocks (5 entries from Phase 2 audit):**

**commands/add-tests.md (2 @.planning/ refs):**
- These are `<context>` block `@.planning/` references that trigger file loading
- Replace with `<project_context>` injection pattern per D-05
- The add-tests command orchestrator injects context via SDK queries

**agents/gsd-planner.md (3 @.planning/ refs):**
- NOTE: This file may have already been partially addressed in Plan 04 Task 2 (agent leaks). If so, verify it's clean. If not, apply the context-block mitigation:
- Replace `@.planning/` references with `<project_context>` orchestrator injection
- The planner agent's parent workflow (plan-phase.md) already calls `gsd-sdk query init.plan-phase`

**Uniform strategy (OQ-04 resolution):**
All affected files use the same pattern:
1. `<context>` blocks with `@.planning/` -> `<project_context>` with orchestrator-injection comment
2. Read-tool instructions against `.planning/` -> `gsd-sdk query` instructions
3. No per-file ad-hoc handling — same treatment everywhere

Document the resolution in a brief comment at the top of any file that had significant changes, noting: "Context migrated to orchestrator injection per OQ-04 resolution (Phase 4, D-05/D-06)."
  </action>
  <verify>
    <automated>cd /Volumes/code/get-shit-done && node scripts/leak-grep.cjs get-shit-done/references/tdd.md get-shit-done/references/planner-antipatterns.md get-shit-done/references/context-budget.md get-shit-done/references/scout-codebase.md get-shit-done/references/universal-anti-patterns.md get-shit-done/commands/add-tests.md get-shit-done/agents/gsd-planner.md 2>&1; echo "exit: $?"</automated>
  </verify>
  <done>All 7 files report 0 leaks; REWRITE-CANDIDATE entries mitigated; OQ-04 resolved with uniform orchestrator-injection strategy</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator -> subagent prompt | Orchestrator injects SDK query output into prompt string |
| Template prose -> agent behavior | Rewritten templates must not lose context that agents depend on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Information Disclosure | Context injection completeness | mitigate | Document which SDK queries each template requires; orchestrator validates non-empty results before injection |
| T-04-12 | Denial of Service | Template broken by incomplete rewrite | mitigate | Verify each rewritten template by checking that all data previously available via @.planning/ is documented as available via a specific SDK query |
</threat_model>

<verification>
- `node scripts/leak-grep.cjs get-shit-done/templates/ get-shit-done/references/` reports 0 leaks
- Context-block audit script validates: `node scripts/audit-context-blocks.cjs 2>&1 | grep "REWRITE-CANDIDATE"` returns 0 entries
- Each template file contains clear orchestrator-injection documentation
</verification>

<success_criteria>
- 5 template leaks + 7 reference leaks + 5 REWRITE-CANDIDATE entries = 17 leaks eliminated
- LEAKS-02 fully satisfied
- OQ-04 resolved: uniform orchestrator-injection strategy documented and applied
- No template loses access to data it previously had (data source shifted from @ to SDK injection, not removed)
</success_criteria>

<output>
After completion, create `.planning/phases/04-plug-workflow-leaks-top-10-context-block-class/04-05-SUMMARY.md`
</output>
